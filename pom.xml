<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.9</version>
        <relativePath/>
    </parent>

    <groupId>br.com.erpkit</groupId>
    <artifactId>erp-modulos</artifactId>
    <version>1.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>ERP Kit - Módulos Plugáveis</name>
    <description>Monorepo de APIs Spring Boot standalone reutilizáveis entre ERPs</description>

    <modules>
        <module>lib-shared</module>
        <module>lib-consultas-client</module>
        <module>api-email</module>
        <module>api-storage</module>
        <module>api-consultas</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <springdoc.version>2.8.15</springdoc.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Módulo shared interno -->
            <dependency>
                <groupId>br.com.erpkit</groupId>
                <artifactId>lib-shared</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Cliente HTTP do módulo de consultas -->
            <dependency>
                <groupId>br.com.erpkit</groupId>
                <artifactId>lib-consultas-client</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- OpenAPI / Swagger -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- Resilience4j (circuit breaker + retry no client da lib) -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-circuitbreaker</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-retry</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
