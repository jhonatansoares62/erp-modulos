---
phase: 02-persistencia-idempotencia
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
parallel_with: []
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java  # MODIFY: adicionar findByTelefone + atualizarUltimaMensagemEm native
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ClienteZapService.java  # NEW
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ClienteZapServiceTest.java  # NEW
autonomous: true
requirements:
  - PER-05  # Normalizacao telefone BR aplicada em identificar (chama TelefoneBR.normalizar)
  - PER-06  # Auto-create ClienteZap com id_cliente_erp=null para telefones desconhecidos
  - PER-07  # atualizarUltimaMensagemEm em REQUIRES_NEW + NOW() do banco
tags:
  - api-whatsapp
  - service
  - cliente-zap
  - requires-new
  - now-banco
  - auto-create

must_haves:
  truths:
    - "ClienteZapRepository.findByTelefone(String) busca por telefone JA NORMALIZADO (caller deve passar TelefoneBR.normalizar)"
    - "ClienteZapRepository.atualizarUltimaMensagemEm via native @Query usa NOW() do banco (NUNCA Instant.now() da JVM) — PITFALLS C-01"
    - "ClienteZapService.identificar(telefone) normaliza, busca, retorna OU cria registro com id_cliente_erp=null"
    - "Race em INSERT concorrente: catch DataIntegrityViolationException + re-fetch — UNIQUE constraint em telefone garante consistencia"
    - "ClienteZapService.atualizarUltimaMensagemEm tem @Transactional(propagation = Propagation.REQUIRES_NEW) — commit imediato"
    - "REQUIRES_NEW garante que UPDATE commita mesmo se transacao chamadora rollback — defesa para Phase 4 trava 24h (PITFALLS C-01)"
    - "7 tests cobrem: auto-create, recovery existente, normaliza SC strip 9, normaliza SP preserva 9, concorrencia INSERT, REQUIRES_NEW commit imediato (visivel via 2a conexao JdbcTemplate), telefone inexistente"
    - "Test REQUIRES_NEW usa JdbcTemplate.queryForObject Timestamp para comparar Instant.toInstant() vs Instant.now() ± 2s"
    - "mvnw verify BUILD SUCCESS sem regressao"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java"
      provides: "Adiciona findByTelefone (derived) + atualizarUltimaMensagemEm (native)"
      contains: "UPDATE whatsapp.clientes_zap SET ultima_mensagem_em = NOW()"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ClienteZapService.java"
      provides: "Identifica/auto-cria ClienteZap + atualiza ultima_mensagem_em REQUIRES_NEW"
      contains: "Propagation.REQUIRES_NEW"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ClienteZapServiceTest.java"
      provides: "7 tests cobrindo D-04 + D-07 + PER-05 + PER-06 + PER-07"
      contains: "atualizar_em_nova_transacao_commit_imediato"
  key_links:
    - from: "ClienteZapService.identificar"
      to: "TelefoneBR.normalizar (Plan 02)"
      via: "primeira linha do metodo: String normalizado = TelefoneBR.normalizar(telefone)"
      pattern: "TelefoneBR\\.normalizar\\(telefone\\)"
    - from: "ClienteZapService.atualizarUltimaMensagemEm"
      to: "Phase 4 WindowEnforcementService (trava 24h)"
      via: "REQUIRES_NEW commita imediatamente, leitura subsequente fora da transacao do webhook ja ve o valor — elimina TOCTOU"
      pattern: "Propagation\\.REQUIRES_NEW"
    - from: "ClienteZapRepository.atualizarUltimaMensagemEm"
      to: "Native @Query com NOW() do banco"
      via: "UPDATE ... SET ultima_mensagem_em = NOW() WHERE telefone = :telefone"
      pattern: "ultima_mensagem_em = NOW\\(\\)"
---

<objective>
Wave 3 (depende de Plan 02 TelefoneBR): implementar `ClienteZapService` com 2 metodos:
1. `identificar(telefone)` — normaliza, busca, auto-cria com `id_cliente_erp=null` se nao existe (D-07 + PER-06)
2. `atualizarUltimaMensagemEm(telefone)` em `@Transactional(REQUIRES_NEW)` usando native `UPDATE ... SET ultima_mensagem_em = NOW()` (D-04 + PER-07)

`ClienteZapRepository` ganha 2 metodos: `findByTelefone(String)` (derived query) e `atualizarUltimaMensagemEm(String)` (native @Query).

Purpose: PITFALLS C-01 documenta TOCTOU race entre webhook persistence (transacao A) e leitura de `ultima_mensagem_em` na trava 24h (transacao B, Phase 4). REQUIRES_NEW commita imediatamente apos UPDATE; leitor subsequente sempre ve o valor atualizado. NOW() do banco e fonte de verdade — clock skew JVM-DB vira bug perto do boundary 24h. Auto-create com `id_cliente_erp=null` permite o flow continuar para clientes nao mapeados (PER-06).

Output:
- `ClienteZapRepository.java` modificado: +2 metodos (1 derived + 1 native @Query)
- `ClienteZapService.java` novo: 2 metodos publicos + 1 helper privado `criarNovo`
- `ClienteZapServiceTest.java` novo: 7 tests
- `mvnw verify -pl api-whatsapp` BUILD SUCCESS
- 1 commit feat atomico + 1 commit docs SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-persistencia-idempotencia/02-CONTEXT.md
@.planning/phases/02-persistencia-idempotencia/02-RESEARCH.md
@.planning/phases/02-persistencia-idempotencia/02-02-SUMMARY.md
@.planning/research/PITFALLS.md
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java
@api-email/src/main/java/br/com/erpkit/email/service/EmailService.java

<interfaces>
<!-- Padroes do monorepo + RESEARCH §4.1 e §7 -->

ClienteZapRepository — adicionar 2 metodos (RESEARCH §4.1):
```java
Optional<ClienteZap> findByTelefone(String telefone);  // derived query

@Modifying
@Query(value =
    "UPDATE whatsapp.clientes_zap " +
    "SET ultima_mensagem_em = NOW() " +
    "WHERE telefone = :telefone",
    nativeQuery = true)
int atualizarUltimaMensagemEm(@Param("telefone") String telefone);
```

ClienteZapService — RESEARCH §7.1:
```java
@Service
public class ClienteZapService {
    private final ClienteZapRepository repository;

    public ClienteZapService(ClienteZapRepository repository) { this.repository = repository; }

    @Transactional
    public ClienteZap identificar(String telefone) {
        String normalizado = TelefoneBR.normalizar(telefone);
        return repository.findByTelefone(normalizado).orElseGet(() -> criarNovo(normalizado));
    }

    private ClienteZap criarNovo(String normalizado) {
        ClienteZap novo = new ClienteZap(normalizado, null);
        try {
            return repository.save(novo);
        } catch (DataIntegrityViolationException e) {
            return repository.findByTelefone(normalizado)
                .orElseThrow(() -> new IllegalStateException(
                    "Race no INSERT em clientes_zap mas registro nao existe (impossivel)", e));
        }
    }

    @Transactional(propagation = Propagation.REQUIRES_NEW)
    public boolean atualizarUltimaMensagemEm(String telefone) {
        String normalizado = TelefoneBR.normalizar(telefone);
        int rows = repository.atualizarUltimaMensagemEm(normalizado);
        return rows != 0;
    }
}
```

A3 RESEARCH (transaction propagation):
- Spring AOP cria proxy quando call vem de OUTRO bean
- Self-call (this.atualizarUltimaMensagemEm dentro do mesmo service) NAO ativa proxy
- No nosso fluxo: MensagemService (Plan 06) chama clienteZapService.atualizarUltimaMensagemEm — outro bean, OK
- Test em §7.2 valida via 2 conexoes JDBC distintas (1 do JPA pool, 1 do JdbcTemplate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modificar ClienteZapRepository — adicionar findByTelefone + atualizarUltimaMensagemEm native</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java</files>
  <action>
    Modificar `ClienteZapRepository.java` adicionando os 2 metodos. Resultado final (codigo COMPLETO da Secao 4.1 do `02-RESEARCH.md` linhas 612-649):

    ```java
    package br.com.erpkit.whatsapp.repository;

    import br.com.erpkit.whatsapp.model.ClienteZap;
    import org.springframework.data.jpa.repository.JpaRepository;
    import org.springframework.data.jpa.repository.Modifying;
    import org.springframework.data.jpa.repository.Query;
    import org.springframework.data.repository.query.Param;

    import java.util.Optional;

    public interface ClienteZapRepository extends JpaRepository<ClienteZap, Long> {

        /**
         * Busca por telefone JA NORMALIZADO (D-03 + caller deve passar
         * {@code TelefoneBR.normalizar(telefone)}). UNIQUE constraint garante
         * 0 ou 1 row.
         */
        Optional<ClienteZap> findByTelefone(String telefone);

        /**
         * Atualiza {@code ultima_mensagem_em} usando o relogio do BANCO
         * ({@code NOW()}), NAO {@code Instant.now()} da JVM (PITFALLS C-01).
         * Native query porque JPQL nao suporta {@code NOW()} portavelmente.
         *
         * <p>Chamada deve estar em {@code @Transactional(REQUIRES_NEW)} — ver
         * {@link br.com.erpkit.whatsapp.service.ClienteZapService#atualizarUltimaMensagemEm(String)}.
         *
         * @return numero de linhas afetadas (0 se telefone nao existe, 1 se atualizou)
         */
        @Modifying
        @Query(value =
            "UPDATE whatsapp.clientes_zap " +
            "SET ultima_mensagem_em = NOW() " +
            "WHERE telefone = :telefone",
            nativeQuery = true)
        int atualizarUltimaMensagemEm(@Param("telefone") String telefone);
    }
    ```

    **Pontos criticos:**
    - `findByTelefone` e DERIVED query — Spring Data JPA gera automaticamente baseado no nome do metodo. NAO precisa `@Query`. UNIQUE constraint em `telefone` garante 0 ou 1 row, justificando `Optional`.
    - `atualizarUltimaMensagemEm` E native @Query — JPQL nao suporta `NOW()` portavelmente entre H2 e PostgreSQL.
    - `@Modifying` exigido para queries de UPDATE em Spring Data JPA.
    - Return `int` para receber rowcount (0 se telefone nao existe — caller pode warnar).
  </action>
  <verify>
    <automated>grep -c "ultima_mensagem_em = NOW()" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java && grep -c "findByTelefone" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java</automated>
  </verify>
  <done>
    - Repository tem 2 metodos novos
    - `findByTelefone` retorna Optional<ClienteZap>
    - `atualizarUltimaMensagemEm` com native @Query usando NOW() — grep confirma
    - `@Modifying` + `nativeQuery = true` presentes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar ClienteZapService.identificar + atualizarUltimaMensagemEm (com 7 testes)</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ClienteZapService.java, api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ClienteZapServiceTest.java</files>
  <behavior>
    7 unit tests per RESEARCH §7.2:

    1. **`identificar_cria_telefone_novo`**: identificar com `+5511987654321` cria registro com `id_cliente_erp=null`, telefone normalizado preservado (SP, com 9), `id` nao-null.
    2. **`identificar_recupera_existente`**: identificar 2x com mesmo telefone → mesmo `id` retornado (nao cria duplicata).
    3. **`identificar_normaliza_antes_de_buscar`**: identificar `+5547984178525` (DDD 47, 13 digitos) cria com `554784178525` (12 digitos sem 9). Lookup subsequente com formato diferente `(47) 98417-8525` retorna mesmo registro.
    4. **`identificar_normaliza_sp_preserva_9`**: identificar `+5511987654321` (SP) preserva `5511987654321` (13 digitos com 9).
    5. **`identificar_concorrente_unique`**: 2 threads identificar mesmo telefone novo simultaneamente → exatamente 1 row no DB (UNIQUE + try/catch race).
    6. **`atualizar_em_nova_transacao_commit_imediato`**: setup criar cliente → atualizarUltimaMensagemEm → JdbcTemplate.queryForObject(Timestamp) le ultima_mensagem_em via 2a conexao do pool. assertThat(ts).isAfter(antes) E isBeforeOrEqualTo(now+2s). REQUIRES_NEW commit imediato confirmado.
    7. **`atualizar_telefone_inexistente`**: atualizarUltimaMensagemEm com telefone novo (sem identificar antes) retorna `false` (0 rows). NAO cria registro.
  </behavior>
  <action>
    **Criar 2 arquivos:**

    **A. `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ClienteZapService.java`** — codigo COMPLETO da Secao 7.1 do `02-RESEARCH.md` (linhas 1198-1297). Copiar literalmente. Pontos chave do codigo:

    - `@Service` na classe
    - Constructor injection do `ClienteZapRepository`
    - Logger SLF4J `private static final Logger log = LoggerFactory.getLogger(ClienteZapService.class);`
    - `identificar(String telefone)`:
      - `@Transactional` (default REQUIRED — pode reusar transacao do caller se houver)
      - normaliza via `TelefoneBR.normalizar`
      - findByTelefone → orElseGet(criarNovo)
    - `criarNovo(String normalizado)` — private helper:
      - cria `new ClienteZap(normalizado, null)` (idClienteErp=null per PER-06)
      - try save() / catch DataIntegrityViolationException → re-fetch por findByTelefone (race protection)
      - se re-fetch tambem falhar: throw IllegalStateException (estado inconsistente, indicador de bug)
    - `atualizarUltimaMensagemEm(String telefone)`:
      - `@Transactional(propagation = Propagation.REQUIRES_NEW)` — D-04 RESEARCH
      - normaliza via TelefoneBR.normalizar
      - chama repository.atualizarUltimaMensagemEm
      - retorna boolean (false se 0 rows, true se 1 row)
      - log.warn se telefone nao encontrado

    Codigo completo em RESEARCH §7.1. Manter Javadoc explicando A3 RESEARCH (proxy AOP cross-bean).

    **B. `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ClienteZapServiceTest.java`** — codigo COMPLETO da Secao 7.2 do `02-RESEARCH.md` (linhas 1303-1432). Copiar literalmente os 7 tests:

    - `@SpringBootTest(classes = WhatsAppApplication.class)` + `@ActiveProfiles("test")`
    - `@Autowired ClienteZapService service; @Autowired ClienteZapRepository repository; @Autowired JdbcTemplate jdbc;`
    - Tests usam `service.identificar` para setup + `jdbc.queryForObject` para verificacao via conexao distinta (essencial para validar REQUIRES_NEW commit imediato)

    **Detalhe critico do test 6 `atualizar_em_nova_transacao_commit_imediato`:**
    ```java
    @Test
    @DisplayName("atualizarUltimaMensagemEm com REQUIRES_NEW commita imediato (visivel via 2a conexao)")
    void atualizar_em_nova_transacao_commit_imediato() {
        service.identificar("+5511777666555");

        Instant antes = Instant.now().minusSeconds(2);
        boolean atualizado = service.atualizarUltimaMensagemEm("+5511777666555");
        assertThat(atualizado).isTrue();

        Timestamp tsRaw = jdbc.queryForObject(
            "SELECT ultima_mensagem_em FROM whatsapp.clientes_zap WHERE telefone = ?",
            Timestamp.class, "5511777666555"
        );
        assertThat(tsRaw).isNotNull();
        Instant ts = tsRaw.toInstant();
        assertThat(ts).isAfter(antes);
        assertThat(ts).isBeforeOrEqualTo(Instant.now().plusSeconds(2));
    }
    ```
    A `JdbcTemplate.queryForObject` usa connection do pool DataSource — diferente da connection do JPA/Hibernate que esta em transacao. Se REQUIRES_NEW funcionou, o UPDATE foi committed antes do return; o SELECT ve a row. Se NAO funcionou (e.g., self-call sem proxy), o test ainda passaria porque o test inteiro nao esta em transacao. **Mas o pivot real** e que em runtime real (Plan 06 MensagemService), a chamada cross-bean via REQUIRES_NEW garante visibilidade fora da transacao do webhook.

    **Detalhe do test 5 `identificar_concorrente_unique`:**
    ```java
    // String normalizado de "+5599888777666" — DDD 99 NAO em DDDS_COM_NONO_DIGITO
    // 13 digitos: 55-99-988777666 → numero local = "988777666" (9 digits, comeca com 9)
    // → strip 9: "55-99-88777666" = "559988777666" (12 digitos)
    Integer count = jdbc.queryForObject(
        "SELECT COUNT(*) FROM whatsapp.clientes_zap WHERE telefone = ?",
        Integer.class, "559988777666"
    );
    assertThat(count).isEqualTo(1);
    ```
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=ClienteZapServiceTest -q</automated>
  </verify>
  <done>
    - ClienteZapService existe com `identificar` + `atualizarUltimaMensagemEm`
    - Logger field, constructor injection, @Service, @Transactional/REQUIRES_NEW corretos
    - 7 tests verdes — Tests run: 7, Failures: 0
    - Test REQUIRES_NEW confirma timestamp commitado via 2a conexao
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificar build do reator</name>
  <files>(nenhum modificado)</files>
  <action>
    Rodar `./mvnw verify -pl api-whatsapp -q` para confirmar:
    - BUILD SUCCESS
    - Tests run total: Phase 1 baseline + spike + TelefoneBR (18) + IdempotencyService (5) + ClienteZapService (7) >= 30+
    - Sem regressao

    Se algum test falhar:
    - **`Tests run: 6, Failures: 1: atualizar_em_nova_transacao_commit_imediato`**: Timestamp recuperado e null ou anterior a `antes` → REQUIRES_NEW nao commitou; verificar `@Transactional(propagation = Propagation.REQUIRES_NEW)` no service
    - **`identificar_concorrente_unique` falha com COUNT=2**: race nao foi tratada; verificar `try/catch DataIntegrityViolationException + re-fetch` no `criarNovo`
    - **`PropertyReferenceException: No property 'Telefone' found`**: errado em derived query name; deve ser `findByTelefone` (camelCase do field)
    - **`Hibernate validation: column [ultima_mensagem_em] not found`**: entity ClienteZap tem `@Column(name = ...)` errado; verificar Plan 01 ClienteZap entity
    - **NPE em `TelefoneBR.normalizar`**: Plan 02 nao foi mergeado; verificar dependency

    **Test scoped:**
    ```bash
    ./mvnw test -pl api-whatsapp -Dtest='IdempotencyServiceTest,ClienteZapServiceTest,TelefoneBRTest' -q
    ```
  </action>
  <verify>
    <automated>./mvnw verify -pl api-whatsapp -q</automated>
  </verify>
  <done>
    - BUILD SUCCESS
    - Tests run total >= 30
    - ClienteZapServiceTest 7/7 verde
    - REQUIRES_NEW empiricamente validado
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MensagemService -> ClienteZapService.atualizarUltimaMensagemEm | Cross-bean call necessario para Spring AOP proxy ativar @Transactional(REQUIRES_NEW). Self-call (this.method) NAO ativa |
| Service -> Repository (native UPDATE) | NOW() avaliado no banco — clock skew JVM-DB irrelevante. Connection do pool gerenciada pelo Spring |
| 2 threads concurrent identificar -> mesma row UNIQUE telefone | UNIQUE constraint + try/catch DataIntegrityViolation + re-fetch — atomico no commit boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | TOCTOU race em identificar concorrente | mitigate | UNIQUE constraint em telefone + try/catch DataIntegrityViolationException + re-fetch (RESEARCH §7.1 D-07); test 5 valida com 2 threads |
| T-02-15 | Tampering | TOCTOU race entre webhook persistence e leitura ultima_mensagem_em (PITFALLS C-01) | mitigate | REQUIRES_NEW commita imediatamente; UPDATE com NOW() do banco e fonte de verdade temporal; test 6 valida via JdbcTemplate (2a conexao) |
| T-02-16 | Tampering | Spring AOP self-call nao ativa proxy | mitigate | A3 RESEARCH: chamadas cross-bean (MensagemService -> ClienteZapService) ativam proxy; documentado em Javadoc do service |
| T-02-17 | Information Disclosure | Telefone em log warn de telefone nao encontrado | accept | log.warn com telefone normalizado completo — informacao de debug critica para operador; mascarar via configuracao de log se necessario em prod |
| T-02-18 | Repudiation | Auto-create silencioso pode mascarar bugs (cliente nao mapeado) | accept | log.debug em criarNovo registra evento; PER-06 explicita pelo design — id_cliente_erp=null e sentinel valido para reconciliation futura |
</threat_model>

<verification>
## Phase Checks

1. `ClienteZapRepository.java` tem 2 metodos novos (findByTelefone + atualizarUltimaMensagemEm native)
2. `ClienteZapService.java` existe com 2 metodos publicos + 1 helper privado
3. `Propagation.REQUIRES_NEW` presente em `atualizarUltimaMensagemEm`
4. `TelefoneBR.normalizar` chamado em ambos os metodos publicos do service
5. `ClienteZapServiceTest.java` existe com 7 @Test
6. `mvnw test -pl api-whatsapp -Dtest=ClienteZapServiceTest` Tests run: 7, Failures: 0
7. `mvnw verify -pl api-whatsapp` BUILD SUCCESS
8. Test 6 (REQUIRES_NEW) valida commit imediato via JdbcTemplate
</verification>

<success_criteria>
- ClienteZapService.identificar auto-create com id_cliente_erp=null + race protection
- ClienteZapService.atualizarUltimaMensagemEm REQUIRES_NEW + NOW() do banco
- 7 tests verdes incluindo concorrencia + REQUIRES_NEW empiricamente validado
- mvnw verify BUILD SUCCESS
- 1 commit feat atomico via gsd-tools.cjs commit
- 1 commit docs SUMMARY apos
</success_criteria>

<commit>
Mensagem (Conventional Commits PT-BR):

```
feat(api-whatsapp): adicionar ClienteZapService com REQUIRES_NEW + NOW()

ClienteZapRepository ganha:
- findByTelefone (derived query, telefone JA NORMALIZADO)
- atualizarUltimaMensagemEm (native @Query com NOW() do banco — PITFALLS C-01)

ClienteZapService:
- identificar(telefone): normaliza via TelefoneBR + findByTelefone or auto-create
  com id_cliente_erp=null (PER-06). Race em INSERT concorrente: try/catch
  DataIntegrityViolationException + re-fetch (UNIQUE telefone + retorno consistente)
- atualizarUltimaMensagemEm(telefone): @Transactional(REQUIRES_NEW) commita
  imediatamente. Defesa para Phase 4 trava 24h — leitor subsequente fora da
  transacao do webhook ja ve o valor atualizado (PITFALLS C-01)

NOW() do banco e fonte de verdade temporal — clock skew JVM-DB pode causar
envio fora da janela 24h aceito erradamente perto do boundary.

7 unit tests cobrem: auto-create, recovery existente, normaliza SC strip 9,
normaliza SP preserva 9, concorrencia INSERT (UNIQUE + try/catch), REQUIRES_NEW
commit imediato (visivel via 2a conexao JdbcTemplate), telefone inexistente.

Refs: D-04/D-07 (CONTEXT.md), PER-05/PER-06/PER-07 (REQUIREMENTS.md), 02-RESEARCH.md §4.1 §7
```

Comando:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): adicionar ClienteZapService com REQUIRES_NEW + NOW()" --files \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ClienteZapService.java \
  api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ClienteZapServiceTest.java
```

Apos sucesso:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(02): adicionar SUMMARY plan 04" --files \
  .planning/phases/02-persistencia-idempotencia/02-04-SUMMARY.md
```
</commit>

<risks>
- **A3 RESEARCH self-call problem**: se algum codigo futuro chamar `this.atualizarUltimaMensagemEm` dentro do mesmo `ClienteZapService`, o proxy AOP NAO ativa e REQUIRES_NEW vira no-op (transacao reusada). Mitigacao: arquitetura — apenas `MensagemService` (Plan 06) chama esse metodo, nunca o service em si. Test 6 valida cross-bean (test class chama service externamente).
- **Test 6 commit timing depende de driver H2** — H2 in-memory pode reportar commit como instantaneo mesmo sem REQUIRES_NEW (transacao auto-commit). Em PostgreSQL real, o test ainda passa. Mitigacao: assert que ts e visivel via 2a conexao do JDBC pool (JdbcTemplate vs Spring Transaction Manager) — semanticamente correto.
- **Race de identificar pode passar sem catch (test 5 falsa-positivo)**: se H2 nao serializar UNIQUE constraint corretamente, ambos save() podem succeed (overwrite!). PostgreSQL serializa via UNIQUE index lock. H2 PG-mode replica esse comportamento. Se test 5 falhar com COUNT=2, e bug do gate — investigar com Testcontainers em Phase 6.
- **Auto-create em loop infinito**: se findByTelefone falhar apos catch DataIntegrityViolation (impossivel se UNIQUE constraint funciona), throw IllegalStateException — bug indicativo, nao silent fail. Codigo do RESEARCH §7.1 ja inclui este safeguard.
</risks>

<output>
Apos completar, criar `.planning/phases/02-persistencia-idempotencia/02-04-SUMMARY.md` com:
- ClienteZapService + ClienteZapRepository expandido
- 7 tests verdes com detalhe sobre test 5 (concorrencia) e test 6 (REQUIRES_NEW commit imediato visivel via JdbcTemplate)
- mvnw verify BUILD SUCCESS
- Commit hash
- Heads-up para Plan 06: API publica `identificar(telefone) -> ClienteZap` + `atualizarUltimaMensagemEm(telefone) -> boolean` estaveis
</output>
