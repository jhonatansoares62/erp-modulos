---
phase: 02-persistencia-idempotencia
plan: 05
type: execute
wave: 2
depends_on:
  - "02-01"
parallel_with:
  - "02-02"
  - "02-03"
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/WebhookPayloadDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EntryDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ChangeDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ValueDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MessageDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/TextDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/InteractiveDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ReplyDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/DocumentDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MensagemEntranteDTO.java  # NEW (record)
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusEntranteDTO.java  # NEW (record)
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ParsedWebhook.java  # NEW (record)
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/text-portugues.json  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/button-reply.json  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/list-reply.json  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/document-pdf.json  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/status-delivered.json  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/tipo-desconhecido.json  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/empty-entry.json  # NEW
  - api-whatsapp/src/test/resources/fixtures/webhook/multiple-messages.json  # NEW
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/WebhookPayloadParserTest.java  # NEW
autonomous: true
requirements:
  - WEB-07  # Parser do payload Meta entende text, button_reply, list_reply, document, statuses + desconhecido
tags:
  - api-whatsapp
  - parser
  - jackson
  - dto
  - fixtures
  - meta-webhook

must_haves:
  truths:
    - "11 DTOs Jackson (classe, NAO record — A8 RESEARCH) com @JsonIgnoreProperties(ignoreUnknown=true) — tolerantes a campos extras do Meta"
    - "3 records Java 21 (MensagemEntranteDTO, StatusEntranteDTO, ParsedWebhook) — output do parser, sem (de)serializacao Jackson"
    - "WebhookPayloadParser.extrair(byte[]) retorna ParsedWebhook(mensagens, statuses) — tolerante a entry vazio, messages ausente, interactive sem button/list"
    - "Parser aplica TelefoneBR.normalizar em msg.from + status.recipient_id antes de retornar (defesa em profundidade)"
    - "Tipos desconhecidos do Meta (ex: 'ephemeral_message', 'sticker'): tipo=DESCONHECIDO, conteudo=null, mediaId=null — sem erro (WEB-07)"
    - "interactive sem button_reply nem list_reply: tipo=DESCONHECIDO"
    - "8 fixtures JSON em src/test/resources/fixtures/webhook/ cobrem text/button/list/document/status/desconhecido/empty/multiple"
    - "9 unit tests cobrem 1 fixture cada + 1 erro de parse (JSON malformado lanca IOException)"
    - "Fixture text-portugues tem caracteres com acentos (Olá, gostaria de um orçamento) — UTF-8 roundtrip valida (A6 RESEARCH)"
    - "mvnw test -Dtest=WebhookPayloadParserTest BUILD SUCCESS Tests run: 9, Failures: 0"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java"
      provides: "Service que parseia bytes do webhook Meta em ParsedWebhook"
      contains: "extrair(byte[]"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ParsedWebhook.java"
      provides: "Record Java 21 — output do parser"
      contains: "public record ParsedWebhook"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MensagemEntranteDTO.java"
      provides: "Record com (wamid, telefone, tipo, conteudo, mediaId)"
      contains: "public record MensagemEntranteDTO"
    - path: "api-whatsapp/src/test/resources/fixtures/webhook/text-portugues.json"
      provides: "Fixture com texto UTF-8 acentuado"
      contains: "Olá, gostaria de um orçamento"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/WebhookPayloadParserTest.java"
      provides: "9 tests cobrindo cada fixture + JSON malformado"
      contains: "json_malformado"
  key_links:
    - from: "WebhookPayloadParser.extrair"
      to: "TelefoneBR.normalizar (Plan 02)"
      via: "parser normaliza msg.from antes de retornar MensagemEntranteDTO"
      pattern: "TelefoneBR\\.normalizar"
    - from: "WebhookPayloadParser.mapTipo"
      to: "TipoMensagem constants (Plan 01)"
      via: "switch retorna TipoMensagem.TEXT/INTERACTIVE_BUTTON/.../DESCONHECIDO"
      pattern: "TipoMensagem\\.(TEXT|INTERACTIVE_BUTTON|INTERACTIVE_LIST|DOCUMENT|IMAGE|AUDIO|DESCONHECIDO)"
    - from: "DTOs Jackson"
      to: "Tolerancia a campos novos do Meta"
      via: "@JsonIgnoreProperties(ignoreUnknown = true) em todas as DTOs"
      pattern: "@JsonIgnoreProperties\\(ignoreUnknown = true\\)"
---

<objective>
Wave 2 (paralelo com Plans 02 e 03): implementar 11 DTOs Jackson + 3 records de saida + `WebhookPayloadParser` que extrai mensagens entrantes e statuses do envelope JSON do Meta. 8 fixtures JSON cobrem o espectro (text, button_reply, list_reply, document, status, desconhecido, empty, multiple). 9 tests validam cada fixture + 1 erro de parse.

Purpose: WEB-07 + D-05. Parser e o componente que transforma `byte[] rawBody` (do `CachedBodyHttpServletRequest` da Phase 1) em estrutura tipada utilizavel pelo orquestrador (Plan 06). Tolerancia a campos novos/desconhecidos e essencial — Meta evolui o payload sem release nosso. DTOs Jackson sao classes (A8 RESEARCH) por compatibilidade ampla; records (saida do parser) sao apenas instanciados pelo Java, sem (de)serializacao. Independente de entities — pode rodar em paralelo com Plans 02 e 03 (nao depende de Plan 04 tambem; depende apenas do Plan 01 para `TipoMensagem` constants).

Output:
- 11 DTOs Jackson em `dto/` package (classes, getters/setters, `@JsonIgnoreProperties`)
- 3 records de saida em `dto/` package (Java 21 records)
- 1 service `WebhookPayloadParser` (`@Service`, constructor injection do `ObjectMapper`)
- 8 fixtures JSON em `src/test/resources/fixtures/webhook/`
- 1 test class com 9 @Test
- `mvnw verify -pl api-whatsapp` BUILD SUCCESS
- 1 commit feat atomico + 1 commit docs SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-persistencia-idempotencia/02-CONTEXT.md
@.planning/phases/02-persistencia-idempotencia/02-RESEARCH.md
@.planning/phases/02-persistencia-idempotencia/02-01-SUMMARY.md
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java

<interfaces>
<!-- Estrutura Meta payload (RESEARCH §5 do CONTEXT — simplificada) -->

```json
{
  "object": "whatsapp_business_account",
  "entry": [{
    "id": "WABA-ID",
    "changes": [{
      "value": {
        "messaging_product": "whatsapp",
        "messages": [{
          "from": "5547999999999",
          "id": "wamid.HBgN...",
          "timestamp": "1735689600",
          "type": "text" | "interactive" | "document" | "image" | "audio",
          "text": { "body": "..." },
          "interactive": {
            "type": "button_reply" | "list_reply",
            "button_reply": { "id": "...", "title": "..." },
            "list_reply": { "id": "...", "title": "..." }
          },
          "document": { "id": "...", "mime_type": "...", "filename": "...", "sha256": "..." },
          "image": { "id": "...", "mime_type": "...", "sha256": "..." },
          "audio": { "id": "...", "mime_type": "...", "sha256": "..." }
        }],
        "statuses": [{
          "id": "wamid.HBgN...",
          "status": "sent" | "delivered" | "read" | "failed",
          "recipient_id": "5547...",
          "timestamp": "..."
        }]
      },
      "field": "messages"
    }]
  }]
}
```

DTOs (classe, NAO record — A8): WebhookPayloadDTO -> EntryDTO -> ChangeDTO -> ValueDTO -> MessageDTO/StatusDTO + sub-DTOs (TextDTO, InteractiveDTO, ReplyDTO, DocumentDTO, MediaDTO).

Records (Java 21, output): MensagemEntranteDTO, StatusEntranteDTO, ParsedWebhook(mensagens, statuses).

Parser tolerancia (RESEARCH §8.4):
- payload null ou entry == null → retorna ParsedWebhook vazio
- entry.changes == null → continue
- value == null → continue
- value.messages != null → loop e extrai
- value.statuses != null → loop e extrai
- mapTipo: switch em msg.type — desconhecidos viram DESCONHECIDO
- extrairConteudo: text -> body, interactive_button -> id+"|"+title, document -> filename, etc.

Fixtures cobrem: text com acentos (UTF-8), button_reply (DDD 11 SP preserva 9), list_reply (DDD 21 RJ preserva), document-pdf (DDD 31 MG strip 9), status delivered (recipient_id DDD 47 strip 9), tipo-desconhecido (type=ephemeral_message), empty-entry, multiple-messages (2 mensagens DDD 47).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar 11 DTOs Jackson em dto/ package</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/WebhookPayloadDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EntryDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ChangeDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ValueDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MessageDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/TextDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/InteractiveDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ReplyDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/DocumentDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusDTO.java</files>
  <action>
    Criar 11 DTOs Jackson copiando o codigo COMPLETO da Secao 8.1 do `02-RESEARCH.md` (linhas 1444-1696). Cada DTO:
    - Package `br.com.erpkit.whatsapp.dto`
    - `@JsonIgnoreProperties(ignoreUnknown = true)` na classe (tolerancia Meta evolution)
    - `@JsonProperty` para snake_case → camelCase (`messaging_product`, `mime_type`, `recipient_id`, `button_reply`, `list_reply`)
    - Construtor padrao implicito (Jackson exige) — NAO declarar
    - Getters + setters publicos para todos os campos
    - Sem Lombok (CONVENTIONS.md)

    **Por que classe e NAO record (A8 RESEARCH):**
    - Jackson 2.x suporta records, mas tem caveats com `@JsonProperty` em construtor
    - Spring Boot 3.5.9 BOM tem Jackson 2.18.x recente, mas a robustez de classe + setters elimina a superficie de bugs
    - Custo: mais linhas de codigo. Ganho: zero risco de runtime deserialization failure
    - **Decisao deliberada da RESEARCH** — manter

    **11 DTOs:**
    1. **WebhookPayloadDTO**: `String object`, `List<EntryDTO> entry`
    2. **EntryDTO**: `String id`, `List<ChangeDTO> changes`
    3. **ChangeDTO**: `ValueDTO value`, `String field`
    4. **ValueDTO**: `String messagingProduct (@JsonProperty("messaging_product"))`, `List<MessageDTO> messages`, `List<StatusDTO> statuses`
    5. **MessageDTO**: `String from`, `String id`, `String timestamp`, `String type`, `TextDTO text`, `InteractiveDTO interactive`, `DocumentDTO document`, `MediaDTO image`, `MediaDTO audio`
    6. **TextDTO**: `String body`
    7. **InteractiveDTO**: `String type`, `ReplyDTO buttonReply (@JsonProperty("button_reply"))`, `ReplyDTO listReply (@JsonProperty("list_reply"))`
    8. **ReplyDTO**: `String id`, `String title` — usado para button_reply E list_reply (mesma estrutura)
    9. **DocumentDTO**: `String id`, `String mimeType (@JsonProperty("mime_type"))`, `String filename`, `String sha256`
    10. **MediaDTO**: `String id`, `String mimeType (@JsonProperty("mime_type"))`, `String sha256` — compartilhado por image e audio
    11. **StatusDTO**: `String id`, `String status`, `String recipientId (@JsonProperty("recipient_id"))`, `String timestamp`

    Codigo completo de cada DTO em RESEARCH §8.1 (linhas 1444-1696). **Copiar literalmente** sem modificacao — ja esta validado contra fixtures.
  </action>
  <verify>
    <automated>ls api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/*.java 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - 11 arquivos `.java` existem em `dto/` (incluindo os 3 records criados na Task 2)
    - Cada DTO tem `@JsonIgnoreProperties(ignoreUnknown = true)`
    - Snake_case → camelCase via `@JsonProperty` em ValueDTO/InteractiveDTO/DocumentDTO/MediaDTO/StatusDTO
    - Compila sem warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar 3 records Java 21 (MensagemEntranteDTO, StatusEntranteDTO, ParsedWebhook)</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MensagemEntranteDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusEntranteDTO.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ParsedWebhook.java</files>
  <action>
    Criar 3 records Java 21 copiando RESEARCH §8.2 (linhas 1700-1753):

    ```java
    // MensagemEntranteDTO.java
    package br.com.erpkit.whatsapp.dto;

    /**
     * Mensagem ja extraida e normalizada pelo parser — pronta para
     * {@link br.com.erpkit.whatsapp.service.MensagemService} processar.
     *
     * <p>Usar {@code record} aqui e seguro porque NAO ha (de)serializacao Jackson —
     * apenas instanciacao Java por
     * {@link br.com.erpkit.whatsapp.service.WebhookPayloadParser}.
     *
     * @param wamid    ID unico do Meta (UNIQUE em mensagens_log)
     * @param telefone telefone JA NORMALIZADO via TelefoneBR
     * @param tipo     "text", "interactive_button", "interactive_list",
     *                 "document", "image", "audio", "desconhecido"
     * @param conteudo conteudo extraido (texto, button_reply.id+title,
     *                 filename, etc.) ou null
     * @param mediaId  ID do media no Meta para document/image/audio, ou null
     */
    public record MensagemEntranteDTO(
        String wamid,
        String telefone,
        String tipo,
        String conteudo,
        String mediaId
    ) { }
    ```

    ```java
    // StatusEntranteDTO.java
    package br.com.erpkit.whatsapp.dto;

    /**
     * Status callback do Meta (sent/delivered/read/failed) — Phase 2 parseia mas
     * NAO persiste (D-05 + D-06). Phase 4 pode adicionar.
     */
    public record StatusEntranteDTO(
        String wamid,
        String status,
        String telefone   // recipient_id (JA NORMALIZADO)
    ) { }
    ```

    ```java
    // ParsedWebhook.java
    package br.com.erpkit.whatsapp.dto;

    import java.util.List;

    public record ParsedWebhook(
        List<MensagemEntranteDTO> mensagens,
        List<StatusEntranteDTO> statuses
    ) { }
    ```

    **Records OK aqui (NAO sao Jackson DTOs)** — o parser instancia direto via constructor Java; nunca passa por `ObjectMapper.readValue`. Eliminacao do risco A8 RESEARCH.
  </action>
  <verify>
    <automated>grep -l "public record" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/*.java | wc -l</automated>
  </verify>
  <done>
    - 3 records existem (grep retorna 3)
    - MensagemEntranteDTO com 5 fields, StatusEntranteDTO com 3 fields, ParsedWebhook com 2 fields
    - Compila sem warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar 8 fixtures JSON em src/test/resources/fixtures/webhook/</name>
  <files>api-whatsapp/src/test/resources/fixtures/webhook/text-portugues.json, api-whatsapp/src/test/resources/fixtures/webhook/button-reply.json, api-whatsapp/src/test/resources/fixtures/webhook/list-reply.json, api-whatsapp/src/test/resources/fixtures/webhook/document-pdf.json, api-whatsapp/src/test/resources/fixtures/webhook/status-delivered.json, api-whatsapp/src/test/resources/fixtures/webhook/tipo-desconhecido.json, api-whatsapp/src/test/resources/fixtures/webhook/empty-entry.json, api-whatsapp/src/test/resources/fixtures/webhook/multiple-messages.json</files>
  <action>
    Criar 8 arquivos JSON copiando o conteudo COMPLETO da Secao 8.5 do `02-RESEARCH.md` (linhas 1947-2125). **Salvar em UTF-8 sem BOM.**

    **1. `text-portugues.json`** — RESEARCH linhas 1947-1968 — text com acentos UTF-8:
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": [{
        "id": "WABA-ID",
        "changes": [{
          "value": {
            "messaging_product": "whatsapp",
            "messages": [{
              "from": "5547984178525",
              "id": "wamid.HBgN.text.001",
              "timestamp": "1735689600",
              "type": "text",
              "text": { "body": "Olá, gostaria de um orçamento" }
            }]
          },
          "field": "messages"
        }]
      }]
    }
    ```

    **2. `button-reply.json`** — RESEARCH linhas 1971-1993:
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": [{
        "id": "WABA-ID",
        "changes": [{
          "value": {
            "messaging_product": "whatsapp",
            "messages": [{
              "from": "5511987654321",
              "id": "wamid.HBgN.btn.001",
              "timestamp": "1735689601",
              "type": "interactive",
              "interactive": {
                "type": "button_reply",
                "button_reply": { "id": "aprovar_1234", "title": "Aprovar" }
              }
            }]
          },
          "field": "messages"
        }]
      }]
    }
    ```

    **3. `list-reply.json`** — RESEARCH linhas 1996-2019 — DDD 21 RJ:
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": [{
        "id": "WABA-ID",
        "changes": [{
          "value": {
            "messaging_product": "whatsapp",
            "messages": [{
              "from": "5521987654321",
              "id": "wamid.HBgN.list.001",
              "timestamp": "1735689602",
              "type": "interactive",
              "interactive": {
                "type": "list_reply",
                "list_reply": { "id": "boleto", "title": "Ver boleto" }
              }
            }]
          },
          "field": "messages"
        }]
      }]
    }
    ```

    **4. `document-pdf.json`** — RESEARCH linhas 2022-2048 — DDD 31 MG strip 9:
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": [{
        "id": "WABA-ID",
        "changes": [{
          "value": {
            "messaging_product": "whatsapp",
            "messages": [{
              "from": "5531987654321",
              "id": "wamid.HBgN.doc.001",
              "timestamp": "1735689603",
              "type": "document",
              "document": {
                "id": "media-id-12345",
                "mime_type": "application/pdf",
                "filename": "comprovante.pdf",
                "sha256": "abc123def456"
              }
            }]
          },
          "field": "messages"
        }]
      }]
    }
    ```

    **5. `status-delivered.json`** — RESEARCH linhas 2051-2070:
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": [{
        "id": "WABA-ID",
        "changes": [{
          "value": {
            "messaging_product": "whatsapp",
            "statuses": [{
              "id": "wamid.HBgN.status.001",
              "status": "delivered",
              "recipient_id": "5547984178525",
              "timestamp": "1735689604"
            }]
          },
          "field": "messages"
        }]
      }]
    }
    ```

    **6. `tipo-desconhecido.json`** — RESEARCH linhas 2073-2091:
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": [{
        "id": "WABA-ID",
        "changes": [{
          "value": {
            "messaging_product": "whatsapp",
            "messages": [{
              "from": "5511987654321",
              "id": "wamid.HBgN.unknown.001",
              "timestamp": "1735689605",
              "type": "ephemeral_message"
            }]
          },
          "field": "messages"
        }]
      }]
    }
    ```

    **7. `empty-entry.json`** — RESEARCH linhas 2094-2099:
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": []
    }
    ```

    **8. `multiple-messages.json`** — RESEARCH linhas 2102-2125 (2 mensagens DDD 47 SC):
    ```json
    {
      "object": "whatsapp_business_account",
      "entry": [{
        "id": "WABA-ID",
        "changes": [{
          "value": {
            "messaging_product": "whatsapp",
            "messages": [
              {
                "from": "5547984178525", "id": "wamid.multi.001", "timestamp": "1735689610",
                "type": "text", "text": { "body": "primeira" }
              },
              {
                "from": "5547984178525", "id": "wamid.multi.002", "timestamp": "1735689611",
                "type": "text", "text": { "body": "segunda" }
              }
            ]
          },
          "field": "messages"
        }]
      }]
    }
    ```

    **Critico:** salvar em UTF-8 SEM BOM. No Windows, alguns editores adicionam BOM (`EF BB BF`) que pode quebrar Jackson. Usar Write tool diretamente — sem BOM.
  </action>
  <verify>
    <automated>ls api-whatsapp/src/test/resources/fixtures/webhook/*.json 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - 8 arquivos JSON existem em fixtures/webhook/
    - text-portugues.json contem "Olá, gostaria de um orçamento" (UTF-8 acentos OK)
    - Cada fixture e JSON valido (passa em Jackson — Task 5 valida)
    - Sem BOM nos arquivos
  </done>
</task>

<task type="auto">
  <name>Task 4: Criar WebhookPayloadParser service</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java` copiando o codigo COMPLETO da Secao 8.4 do `02-RESEARCH.md` (linhas 1782-1941). Pontos-chave:

    - `@Service` na classe
    - Constructor injection do `ObjectMapper` (Spring Boot ja registra default)
    - Logger SLF4J
    - Metodo publico `extrair(byte[] rawBody) throws IOException` retorna `ParsedWebhook`
    - Metodos privados:
      - `extrairMensagem(MessageDTO msg)` retorna `MensagemEntranteDTO`
      - `mapTipo(MessageDTO msg)` retorna `String` (constants `TipoMensagem`)
      - `extrairConteudo(MessageDTO msg, String tipo)` retorna `String`
      - `extrairMediaId(MessageDTO msg)` retorna `String`
      - `extrairStatus(StatusDTO st)` retorna `StatusEntranteDTO`

    **Logica do mapTipo (RESEARCH §8.4):**
    ```java
    switch (msg.getType()) {
        case "text":     return TipoMensagem.TEXT;
        case "document": return TipoMensagem.DOCUMENT;
        case "image":    return TipoMensagem.IMAGE;
        case "audio":    return TipoMensagem.AUDIO;
        case "interactive":
            if (msg.getInteractive() == null || msg.getInteractive().getType() == null)
                return TipoMensagem.DESCONHECIDO;
            return switch (msg.getInteractive().getType()) {
                case "button_reply" -> TipoMensagem.INTERACTIVE_BUTTON;
                case "list_reply"   -> TipoMensagem.INTERACTIVE_LIST;
                default              -> TipoMensagem.DESCONHECIDO;
            };
        default:
            log.debug("Tipo desconhecido do Meta: {} (wamid={})", msg.getType(), msg.getId());
            return TipoMensagem.DESCONHECIDO;
    }
    ```

    **Logica do extrairConteudo (RESEARCH §8.4):**
    - `text` → `msg.getText().getBody()` (null-safe)
    - `interactive_button` → `buttonReply.id + "|" + buttonReply.title`
    - `interactive_list` → `listReply.id + "|" + listReply.title`
    - `document` → `doc.filename`
    - `image` → `img.mimeType`
    - `audio` → `audio.mimeType`
    - `desconhecido` → `null`

    **Logica do extrairMediaId:**
    - document.id, image.id, ou audio.id (cada um se nao-null)
    - text/interactive: null

    **Tolerancia (entry vazio, messages ausente):**
    ```java
    if (payload == null || payload.getEntry() == null) {
        log.debug("Webhook payload sem 'entry' — ignorando (heartbeat ou keepalive)");
        return new ParsedWebhook(mensagens, statuses);  // listas vazias
    }
    ```

    **TelefoneBR.normalizar aplicado:**
    - `extrairMensagem`: `String telefone = TelefoneBR.normalizar(msg.getFrom());`
    - `extrairStatus`: `TelefoneBR.normalizar(st.getRecipientId())`

    Codigo completo em RESEARCH §8.4 — copiar inteiro sem modificacao.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java && grep -c "TelefoneBR.normalizar" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java</automated>
  </verify>
  <done>
    - Arquivo existe com `@Service`
    - `extrair(byte[] rawBody) throws IOException` e o metodo publico
    - `TelefoneBR.normalizar` aplicado em 2 lugares (msg.from e status.recipient_id)
    - 4 metodos privados de extracao (mapTipo, extrairConteudo, extrairMediaId, extrairStatus)
    - Tolerancia a campos null (entry, changes, value, messages, statuses)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Criar WebhookPayloadParserTest com 9 tests usando fixtures</name>
  <files>api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/WebhookPayloadParserTest.java</files>
  <behavior>
    9 unit tests (RESEARCH §8.6 linhas 2131-2255):

    1. **`text_portugues`**: extrair `text-portugues.json` → 1 mensagem, wamid `wamid.HBgN.text.001`, telefone `554784178525` (DDD 47 strip 9), tipo `TipoMensagem.TEXT`, conteudo `"Olá, gostaria de um orçamento"`, mediaId null.
    2. **`button_reply`**: extrair `button-reply.json` → 1 mensagem, tipo `INTERACTIVE_BUTTON`, conteudo `"aprovar_1234|Aprovar"`, telefone `5511987654321` (SP preserva 9).
    3. **`list_reply`**: extrair `list-reply.json` → tipo `INTERACTIVE_LIST`, conteudo `"boleto|Ver boleto"`.
    4. **`document`**: extrair `document-pdf.json` → tipo `DOCUMENT`, conteudo `"comprovante.pdf"`, mediaId `"media-id-12345"`, telefone `553187654321` (MG strip 9).
    5. **`status_delivered`**: extrair `status-delivered.json` → mensagens vazia, 1 status com `delivered`, telefone `554784178525`.
    6. **`tipo_desconhecido`**: extrair `tipo-desconhecido.json` → tipo `DESCONHECIDO`, conteudo null, mediaId null.
    7. **`empty_entry`**: extrair `empty-entry.json` → mensagens e statuses ambos vazios (sem erro).
    8. **`multiple_messages`**: extrair `multiple-messages.json` → 2 mensagens com wamids `wamid.multi.001` e `wamid.multi.002`, conteudos `primeira` e `segunda`.
    9. **`json_malformado`**: `parser.extrair("{ invalid json".getBytes())` lanca `IOException`.
  </behavior>
  <action>
    Criar `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/WebhookPayloadParserTest.java` copiando codigo COMPLETO da Secao 8.6 do `02-RESEARCH.md` (linhas 2131-2255):

    ```java
    package br.com.erpkit.whatsapp.service;

    import br.com.erpkit.whatsapp.dto.MensagemEntranteDTO;
    import br.com.erpkit.whatsapp.dto.ParsedWebhook;
    import br.com.erpkit.whatsapp.util.TipoMensagem;
    import com.fasterxml.jackson.databind.ObjectMapper;
    import org.junit.jupiter.api.BeforeEach;
    import org.junit.jupiter.api.DisplayName;
    import org.junit.jupiter.api.Test;
    import org.springframework.core.io.ClassPathResource;
    import org.springframework.util.StreamUtils;

    import java.io.IOException;
    import java.io.InputStream;

    import static org.assertj.core.api.Assertions.assertThat;

    class WebhookPayloadParserTest {

        private WebhookPayloadParser parser;

        @BeforeEach
        void setUp() {
            parser = new WebhookPayloadParser(new ObjectMapper());
        }

        private byte[] fixture(String nome) throws IOException {
            try (InputStream in = new ClassPathResource("fixtures/webhook/" + nome).getInputStream()) {
                return StreamUtils.copyToByteArray(in);
            }
        }

        // ... [seguir RESEARCH §8.6 linhas 2164-2253 — 9 tests literais]
    }
    ```

    **Pontos criticos:**
    - **NAO usar `@SpringBootTest`** — pure JUnit + new ObjectMapper(). Mais rapido, sem context overhead.
    - `@BeforeEach setUp()` cria parser com ObjectMapper default (mesmo que Spring Boot configura)
    - Helper `fixture(String nome)` carrega bytes via `ClassPathResource` — validar que fixtures estao em `src/test/resources/fixtures/webhook/`
    - Test 9 (`json_malformado`) usa `assertThatThrownBy(() -> parser.extrair(...)).isInstanceOf(IOException.class)` — Jackson levanta `JsonProcessingException extends IOException`

    **Validacao UTF-8 (test 1):**
    ```java
    assertThat(m.conteudo()).isEqualTo("Olá, gostaria de um orçamento");
    ```
    Se Jackson interpretar UTF-8 errado, o conteudo vira `"Ol??, gostaria..."` e o assertion falha — gate empirico para A6 RESEARCH.

    **Validacao TelefoneBR integration (tests 1, 2, 4, 5):**
    - Test 1: from=`5547984178525` (13 digits) → telefone=`554784178525` (12 digits, strip 9 SC)
    - Test 2: from=`5511987654321` (13 digits) → telefone=`5511987654321` (preserve, SP)
    - Test 4: from=`5531987654321` → telefone=`553187654321` (strip 9, MG)
    - Test 5: recipient_id=`5547984178525` → telefone=`554784178525` (strip 9 no status tambem)
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=WebhookPayloadParserTest -q</automated>
  </verify>
  <done>
    - Tests run: 9, Failures: 0
    - Test text_portugues passa (UTF-8 OK)
    - Tests com TelefoneBR integration passam (parser normaliza)
    - Test json_malformado lanca IOException
  </done>
</task>

<task type="auto">
  <name>Task 6: Verificar build do reator</name>
  <files>(nenhum modificado)</files>
  <action>
    Rodar `./mvnw verify -pl api-whatsapp -q` para confirmar:
    - BUILD SUCCESS
    - Tests run total: Phase 1 baseline + spike + (Plans 02/03/04 se ja rodaram) + WebhookPayloadParser (9) >= 17+
    - Sem regressao

    Se algum test falhar:
    - **`Tests run: 8, Failures: 1: text_portugues` com `expected "Ola, gostaria..." but was "Olá, gostaria..."`** → fixture salvo com BOM ou encoding errado; salvar de novo em UTF-8 sem BOM
    - **`UnrecognizedPropertyException: messaging_product`** → falta `@JsonProperty` em ValueDTO (Jackson tentou casar com `messagingProduct` field)
    - **`InvalidDefinitionException: Cannot construct instance ... no Creators`** → Jackson tentando desserializar record DTO; verificar que `WebhookPayloadDTO` e classe (nao record)
    - **NPE em `extrairConteudo` com `interactive` null** → falta null-check no parser
    - **`Tests run: 9, Failures: 1: json_malformado` esperando IOException** → Jackson pode lancar `JsonProcessingException` que extends IOException; assertion deve aceitar isInstanceOf(IOException.class)

    **Verificar fixture encoding (Windows defensivo):**
    ```bash
    # Detectar BOM em fixtures (deve retornar 0 — sem BOM)
    head -c 3 api-whatsapp/src/test/resources/fixtures/webhook/text-portugues.json | od -c | head -1
    # Esperado: "{    "  (sem \357 \273 \277 = BOM)
    ```
  </action>
  <verify>
    <automated>./mvnw verify -pl api-whatsapp -q</automated>
  </verify>
  <done>
    - BUILD SUCCESS
    - WebhookPayloadParserTest 9/9 verde
    - UTF-8 acentos preservados em fixture text
    - Sem regressao em outros testes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta payload (untrusted) -> WebhookPayloadParser | HMAC ja foi validado pelo Filter Phase 1; bytes sao confiaveis quanto a origem mas estrutura pode ter campos novos do Meta |
| ObjectMapper deserialization -> Java DTOs | @JsonIgnoreProperties(ignoreUnknown=true) tolera campos novos sem quebrar; @JsonProperty mapeia snake_case |
| Parser -> downstream services | Records imutaveis (MensagemEntranteDTO, etc) — sem mutation possivel apos parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Tampering | Meta evolui payload com novo tipo (ex: ephemeral_message) | mitigate | tipo=DESCONHECIDO + persiste sem erro (WEB-07); fixture tipo-desconhecido.json valida; @JsonIgnoreProperties tolera campos extras |
| T-02-20 | DoS | Payload muito grande consome memoria | accept | HMAC ja foi computado sobre todos os bytes (Phase 1 cacheou); Jackson stream-decode mas constroi arvore. Em on-premise volume baixo, OK. Phase 6 pode adicionar limit |
| T-02-21 | Tampering | Fixture com BOM (Windows) quebra Jackson | mitigate | Verificacao explicita em Task 6 (`head -c 3`); Write tool gera arquivos sem BOM |
| T-02-22 | Information Disclosure | log.debug com conteudo no parser | mitigate | log.debug do parser registra apenas count (`{} mensagens, {} statuses`) e tipo desconhecido com wamid — NUNCA conteudo do body (PII) |
| T-02-23 | Tampering | UTF-8 4-byte chars (emoji) sobrevivencia (A6 RESEARCH) | accept | PostgreSQL TEXT + H2 PG-mode + Jackson default UTF-8 — confianca alta; fixture text-portugues valida acentos (3-byte UTF-8). Se necessario, Phase 6 adiciona fixture com emoji |
</threat_model>

<verification>
## Phase Checks

1. 11 DTOs Jackson em `dto/` package
2. 3 records (MensagemEntranteDTO, StatusEntranteDTO, ParsedWebhook) em `dto/`
3. WebhookPayloadParser com `@Service` + `extrair(byte[]) throws IOException`
4. 8 fixtures JSON em `src/test/resources/fixtures/webhook/`
5. WebhookPayloadParserTest com 9 @Test
6. `mvnw test -pl api-whatsapp -Dtest=WebhookPayloadParserTest` Tests run: 9, Failures: 0
7. `mvnw verify -pl api-whatsapp` BUILD SUCCESS
8. UTF-8 acentos preservados (fixture text + assert no test)
9. Tipo desconhecido persiste sem erro (test 6 valida)
10. Telefone normalizado em mensagens E em statuses (testes 1, 2, 4, 5)
</verification>

<success_criteria>
- WebhookPayloadParser parseia 8 fixtures cobrindo o espectro Meta (text, button_reply, list_reply, document, status, desconhecido, empty, multiple)
- 9 tests verdes incluindo UTF-8 acentos + JSON malformado + tipo desconhecido
- TelefoneBR.normalizar integrado no parser (defesa em profundidade)
- mvnw verify BUILD SUCCESS
- 1 commit feat atomico via gsd-tools.cjs commit
- 1 commit docs SUMMARY apos
</success_criteria>

<commit>
Mensagem (Conventional Commits PT-BR):

```
feat(api-whatsapp): adicionar WebhookPayloadParser + DTOs Jackson + fixtures Meta

11 DTOs Jackson (classe com @JsonIgnoreProperties — A8 RESEARCH) cobrem o
envelope Meta: WebhookPayload -> Entry -> Change -> Value -> Message/Status
+ sub-DTOs (Text, Interactive, Reply, Document, Media).

3 records Java 21 (MensagemEntranteDTO, StatusEntranteDTO, ParsedWebhook)
sao output do parser — instanciados via Java, nao deserializados por Jackson.

WebhookPayloadParser.extrair(byte[]) tolerante a:
- entry vazio, messages ausente, interactive sem button/list reply
- tipos novos do Meta (ephemeral_message etc.) → tipo=DESCONHECIDO sem erro (WEB-07)
- Aplica TelefoneBR.normalizar em msg.from + status.recipient_id (defesa em profundidade)

8 fixtures JSON cobrem: text com acentos UTF-8, button_reply, list_reply,
document-pdf, status-delivered, tipo-desconhecido (ephemeral_message), empty-entry,
multiple-messages.

9 unit tests (sem @SpringBootTest, pure JUnit + ObjectMapper):
1 fixture cada + 1 erro de parse (IOException).

Refs: D-05 (CONTEXT.md), WEB-07 (REQUIREMENTS.md), 02-RESEARCH.md §8
```

Comando:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): adicionar WebhookPayloadParser + DTOs Jackson + fixtures Meta" --files \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/WebhookPayloadDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EntryDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ChangeDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ValueDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MessageDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/TextDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/InteractiveDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ReplyDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/DocumentDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MensagemEntranteDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusEntranteDTO.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ParsedWebhook.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java \
  api-whatsapp/src/test/resources/fixtures/webhook/text-portugues.json \
  api-whatsapp/src/test/resources/fixtures/webhook/button-reply.json \
  api-whatsapp/src/test/resources/fixtures/webhook/list-reply.json \
  api-whatsapp/src/test/resources/fixtures/webhook/document-pdf.json \
  api-whatsapp/src/test/resources/fixtures/webhook/status-delivered.json \
  api-whatsapp/src/test/resources/fixtures/webhook/tipo-desconhecido.json \
  api-whatsapp/src/test/resources/fixtures/webhook/empty-entry.json \
  api-whatsapp/src/test/resources/fixtures/webhook/multiple-messages.json \
  api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/WebhookPayloadParserTest.java
```

Apos sucesso:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(02): adicionar SUMMARY plan 05" --files \
  .planning/phases/02-persistencia-idempotencia/02-05-SUMMARY.md
```
</commit>

<risks>
- **A6 RESEARCH UTF-8 4-byte chars (emoji)**: confianca alta mas nao testada em fixture; se necessario, Phase 6 adiciona regressao com emoji 4-byte.
- **A8 RESEARCH Jackson record support**: contornado deliberadamente — classes para DTOs Jackson, records para output. Caso futuro queira usar records Jackson, precisaria `@JsonCreator` ou Jackson 2.18+ com config — fora do escopo.
- **Fixture BOM em Windows**: Write tool nao adiciona BOM por default; verificacao em Task 6 confirma. Se algum dev usar editor que adicione BOM, Jackson lanca `JsonParseException: Unexpected character`. Salvaguarda: `head -c 3` check.
- **interactive sem button_reply nem list_reply**: parser retorna `tipo=DESCONHECIDO`. Fixture nao testa este caso especifico (apenas tipo novo `ephemeral_message`); se Meta enviar `interactive` quebrado, o parser nao quebra mas log.debug nao registra. Aceitavel para Phase 2 — Phase 6 pode adicionar fixture especifico.
- **Records vs classes confusion**: ja documentado no Javadoc dos records (`Usar record aqui e seguro porque NAO ha (de)serializacao Jackson`). Risk de futuro dev tentar deserializar record com Jackson e quebrar — Phase 6 pode adicionar javadoc warning ou test.
</risks>

<output>
Apos completar, criar `.planning/phases/02-persistencia-idempotencia/02-05-SUMMARY.md` com:
- 11 DTOs + 3 records + WebhookPayloadParser + 8 fixtures + WebhookPayloadParserTest criados
- Tests run: 9, Failures: 0
- UTF-8 acentos preservados (gate empirico A6)
- mvnw verify BUILD SUCCESS
- Commit hash
- Heads-up para Plan 06: API publica `WebhookPayloadParser.extrair(byte[]) -> ParsedWebhook` estavel; ParsedWebhook contem List<MensagemEntranteDTO> e List<StatusEntranteDTO>
</output>
