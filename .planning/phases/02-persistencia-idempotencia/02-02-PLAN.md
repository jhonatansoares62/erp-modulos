---
phase: 02-persistencia-idempotencia
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
parallel_with:
  - "02-03"
  - "02-05"
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java  # NEW
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java  # NEW
autonomous: true
requirements:
  - PER-05  # Normalizacao telefone BR — DDDs SP/RJ/ES preservam 9o digito; demais strip
tags:
  - api-whatsapp
  - utility
  - telefone-br
  - normalizacao
  - tdd

must_haves:
  truths:
    - "TelefoneBR.normalizar(String) e um pure utility (private constructor) — testavel sem Spring"
    - "DDDs SP (11-19), RJ (21,22,24), ES (27,28) PRESERVAM o 9o digito (regra ANATEL 2010 + WhatsApp manteve formato antigo nesses estados)"
    - "Demais DDDs strip o 9o digito quando numero local tem 9 digitos comecando com 9 (8 digitos finais — formato pre-2010)"
    - "Numeros nao-Brasil (sem prefixo 55, ou len fora 12-13): retorna sanitizado sem alteracao"
    - "null retorna null; string vazia retorna string vazia; sem digitos retorna empty"
    - "Strip de TODOS os nao-digitos (parenteses, espacos, hifens, plus) antes de normalizar"
    - "18 test cases cobrem: 4 DDDs strip + 5 DDDs preserve + 3 sanitizacao + 4 edge null/empty/USA/curto + 2 exotic"
    - "TelefoneBRTest passa com 0 falhas (mvnw test -Dtest=TelefoneBRTest)"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java"
      provides: "Pure utility de normalizacao de telefone brasileiro"
      contains: "DDDS_COM_NONO_DIGITO"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java"
      provides: "18 test cases cobrindo a matriz completa de DDDs + edge cases"
      contains: "sc_dd47_com_9"
  key_links:
    - from: "TelefoneBR.normalizar"
      to: "ClienteZapService.identificar (Plan 04)"
      via: "Plan 04 chama TelefoneBR.normalizar antes de findByTelefone/save"
      pattern: "TelefoneBR\\.normalizar"
    - from: "TelefoneBR.normalizar"
      to: "WebhookPayloadParser.extrairMensagem (Plan 05)"
      via: "Parser normaliza msg.from antes de retornar MensagemEntranteDTO"
      pattern: "TelefoneBR\\.normalizar"
    - from: "DDDS_COM_NONO_DIGITO Set"
      to: "PITFALLS C-13 (regra ANATEL 2010 + WhatsApp formato antigo)"
      via: "14 DDDs literais: SP (11-19), RJ (21,22,24), ES (27,28)"
      pattern: "\"11\", \"12\", \"13\", \"14\", \"15\", \"16\", \"17\", \"18\", \"19\""
---

<objective>
Wave 2 (paralelo com Plans 03 e 05): implementar `TelefoneBR.normalizar(String)` como pure utility — strip 9o digito condicional por DDD brasileiro (PER-05 + PITFALLS C-13). Cobrir com 18 test cases incluindo casos do bug 131026 (DDDs fora SP/RJ/ES) que sao silently broken sem normalizacao.

Purpose: PITFALLS C-13 documenta que enviar texto para um numero "errado" (com 9 num DDD que nao guardou 9, ou sem 9 num DDD que guardou) retorna error 131026 silenciosamente do Meta — mensagem NAO entregue, sem retry, sem feedback util. Plan 04 (`ClienteZapService`) e Plan 05 (`WebhookPayloadParser`) dependem desta normalizacao para operar corretamente. Pure utility (sem Spring) e independente — pode rodar em paralelo com Plans 03 e 05.

Output:
- 1 utility class `TelefoneBR` com private constructor + 1 metodo `static String normalizar(String)`
- 1 Set imutavel `DDDS_COM_NONO_DIGITO` com 14 DDDs (SP/RJ/ES)
- 1 test class `TelefoneBRTest` com 18 cases cobrindo a matriz completa
- `mvnw test -pl api-whatsapp -Dtest=TelefoneBRTest` BUILD SUCCESS Tests run: 18, Failures: 0
- 1 commit feat atomico + 1 commit docs SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-persistencia-idempotencia/02-CONTEXT.md
@.planning/phases/02-persistencia-idempotencia/02-RESEARCH.md
@.planning/phases/02-persistencia-idempotencia/02-01-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Tabela de DDDs (PITFALLS C-13 + RESEARCH §5.1) -->

DDDs que MANTEM o 9o digito no WhatsApp:
- SP: 11, 12, 13, 14, 15, 16, 17, 18, 19  (9 DDDs)
- RJ: 21, 22, 24  (3 DDDs)
- ES: 27, 28  (2 DDDs)
Total: 14 DDDs

DDDs que STRIP o 9o digito (todos os outros): SC (47, 48, 49), MG (31-38), RS (51-55), PR (41-46), BA, PE, etc.

Algoritmo (RESEARCH §5):
1. Strip todos os nao-digitos (replaceAll("\\D", ""))
2. Se nao comeca com "55" OU len fora [12, 13]: retorna sanitizado sem alteracao
3. ddd = digitos[2..4]; numero = digitos[4..]
4. Se ddd in DDDS_COM_NONO_DIGITO: retorna sanitizado sem mudanca (preserve 9)
5. Se numero.length()==9 e numero.startsWith("9"): strip o 9 (retorna 12 digitos finais)
6. Caso contrario (8 digitos local, ou nao comeca com 9): retorna sanitizado sem mudanca

Casos pivot:
- "+5547984178525" (DDD 47 SC, 13 digitos com 9) → "554784178525" (12 digitos sem 9)
- "+5511987654321" (DDD 11 SP, 13 digitos com 9) → "5511987654321" (preserva — SP)
- "+554784178525" (DDD 47 SC, 12 digitos ja sem 9) → "554784178525" (passa direto)
- "+1 (415) 555-1212" → "14155551212" (USA — sanitiza, nao toca)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar TelefoneBRTest com 18 test cases (RED — escrevendo testes antes da implementacao)</name>
  <files>api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java</files>
  <behavior>
    18 test cases per RESEARCH §5.2 cobrindo:

    **Strip 9o digito (4 cases — DDDs fora SP/RJ/ES):**
    1. `sc_dd47_com_9`: `+5547984178525` → `554784178525` (SC)
    2. `mg_ddd31_com_9`: `+5531987654321` → `553187654321` (MG)
    3. `rs_ddd51_com_9`: `+5551987654321` → `555187654321` (RS)
    4. `pr_ddd41_com_9`: `+5541987654321` → `554187654321` (PR)

    **Preserve 9o digito (5 cases — DDDs SP/RJ/ES):**
    5. `sp_ddd11_com_9_preserva`: `+5511987654321` → `5511987654321`
    6. `rj_ddd21_com_9_preserva`: `+5521987654321` → `5521987654321`
    7. `rj_ddd24_com_9_preserva`: `+5524987654321` → `5524987654321`
    8. `es_ddd27_com_9_preserva`: `+5527987654321` → `5527987654321`
    9. `sp_ddd19_com_9_preserva`: `+5519987654321` → `5519987654321`

    **Sanitizacao (3 cases):**
    10. `formatado_humano`: `+55 (47) 98417-8525` → `554784178525`
    11. `sem_plus`: `5547984178525` → `554784178525`
    12. `ja_sem_nono`: `+554784178525` → `554784178525` (passa direto, ja em formato Meta)

    **Edge null/empty/USA/curto (5 cases):**
    13. `null_retorna_null`: `null` → `null`
    14. `empty_retorna_empty`: `""` → `""`
    15. `sem_digitos_retorna_empty`: `"()-+ "` → `""`
    16. `usa_nao_brasil`: `+1 (415) 555-1212` → `14155551212` (sanitiza, nao toca)
    17. `numero_curto`: `4784178525` → `4784178525` (preserve, nao tenta normalizar)

    **Exotic (2 cases — limite do contrato):**
    18. `ddd_inexistente_99_strip_9`: `+5599987654321` → `559987654321` (strip baseado em Set lookup, nao em validacao real)
    19. `numero_muito_longo`: `+555111987654321` (14 digitos) → `555111987654321` (preserve sanitizado)
  </behavior>
  <action>
    Criar `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java` copiando o codigo COMPLETO da Secao 5.2 do `02-RESEARCH.md` (linhas 845-993). Codigo abaixo (sem `@SpringBootTest` — pure JUnit, sem context):

    ```java
    package br.com.erpkit.whatsapp.util;

    import org.junit.jupiter.api.DisplayName;
    import org.junit.jupiter.api.Test;

    import static org.assertj.core.api.Assertions.assertThat;

    class TelefoneBRTest {

        // ============================================================
        // Casos OUTROS DDDs (strip 9o digito) — pivot do bug 131026
        // ============================================================

        @Test
        @DisplayName("DDD 47 (SC) com 9o digito strip o 9")
        void sc_dd47_com_9() {
            assertThat(TelefoneBR.normalizar("+5547984178525")).isEqualTo("554784178525");
        }

        // ... [seguir RESEARCH §5.2 linhas 855-993 — total 18+ tests]
    }
    ```

    **Copiar literalmente** todos os 18 (ou 19, contando exotic) tests do RESEARCH §5.2. Os tests sao auto-documented com `@DisplayName` em portugues. SEM Spring — pure JUnit + AssertJ.

    **Caminho RED:** este test NAO PODE PASSAR ainda — `TelefoneBR.java` ainda nao existe. Tentativa de compilar do test class FALHARA com "cannot resolve symbol TelefoneBR" — esperado e correto. Task 2 implementa o codigo.

    **Compilation gate:** se quiser garantir o RED literal:
    ```bash
    ./mvnw test-compile -pl api-whatsapp -q
    # Esperado: BUILD FAILURE com "cannot find symbol" para TelefoneBR
    ```
    OU pode pular o gate RED e fazer ambas as tasks juntas — o gate da Task 3 (verify) confirma o GREEN final.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java && grep -c "@Test" api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java</automated>
  </verify>
  <done>
    - Arquivo de test existe
    - Numero de @Test >= 18 (grep retorna >= 18)
    - DisplayName em portugues conforme RESEARCH §5.2
    - SEM @SpringBootTest (pure unit test)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar TelefoneBR.normalizar (GREEN — fazer os 18 tests passarem)</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java` copiando o codigo COMPLETO da Secao 5.1 do `02-RESEARCH.md` (linhas 740-840):

    ```java
    package br.com.erpkit.whatsapp.util;

    import java.util.Set;

    /**
     * Normalizacao de telefone brasileiro para o formato armazenado em
     * {@code clientes_zap} (D-03 do CONTEXT.md + PITFALLS C-13).
     *
     * <p><b>Regra ANATEL 2010 (resumida):</b>
     * <ul>
     *   <li>Numeros moveis adicionaram um 9o digito (numero local de 8 → 9 digitos).</li>
     *   <li>WhatsApp registrou os numeros ANTES dessa mudanca SEM o 9o digito para
     *       a maioria dos DDDs FORA de SP (11-19), RJ (21, 22, 24) e ES (27, 28).</li>
     *   <li>Enviar texto para um numero "errado" (com 9 num DDD que nao guardou 9,
     *       ou sem 9 num DDD que guardou) retorna error 131026 silenciosamente —
     *       mensagem NAO entregue, sem retry, sem feedback util do Meta.</li>
     * </ul>
     *
     * <p><b>Algoritmo:</b>
     * <ol>
     *   <li>Strip todos os nao-digitos (parenteses, espacos, hifens, plus).</li>
     *   <li>Se nao comeca com {@code "55"} ou tem comprimento fora 12-13: retorna
     *       sanitizado sem alteracao (numero nao-Brasil).</li>
     *   <li>Extrai DDD = caracteres 2-3 (apos "55").</li>
     *   <li>Se DDD em {@link #DDDS_COM_NONO_DIGITO}: retorna sem mudanca.</li>
     *   <li>Se DDD fora desse Set e numero local tem 9 digitos comecando com 9:
     *       strip o 9 — resultado tem 8 digitos locais (formato pre-2010).</li>
     *   <li>Caso contrario: retorna sanitizado sem mudanca.</li>
     * </ol>
     *
     * <p><b>Politica:</b> normalizar SEMPRE no INSERT em {@code clientes_zap} E
     * em qualquer lookup ({@code findByTelefone}). UNIQUE constraint funciona
     * naturalmente porque ambos caminhos passam pelo mesmo normalizador.
     *
     * <p><b>Pure utility</b> (private constructor) — testavel sem Spring.
     */
    public final class TelefoneBR {

        /**
         * DDDs que mantiveram o 9o digito no WhatsApp:
         * <ul>
         *   <li>SP: 11, 12, 13, 14, 15, 16, 17, 18, 19</li>
         *   <li>RJ: 21, 22, 24</li>
         *   <li>ES: 27, 28</li>
         * </ul>
         */
        private static final Set<String> DDDS_COM_NONO_DIGITO = Set.of(
            "11", "12", "13", "14", "15", "16", "17", "18", "19",  // SP
            "21", "22", "24",                                       // RJ
            "27", "28"                                              // ES
        );

        private TelefoneBR() {
            // Pure utility — sem instancias
        }

        public static String normalizar(String telefone) {
            if (telefone == null) {
                return null;
            }
            String digitos = telefone.replaceAll("\\D", "");
            if (digitos.isEmpty()) {
                return digitos;
            }

            if (!digitos.startsWith("55") || digitos.length() < 12 || digitos.length() > 13) {
                return digitos;
            }

            String ddd = digitos.substring(2, 4);
            String numero = digitos.substring(4);

            if (DDDS_COM_NONO_DIGITO.contains(ddd)) {
                return digitos;
            }

            if (numero.length() == 9 && numero.startsWith("9")) {
                return "55" + ddd + numero.substring(1);
            }

            return digitos;
        }
    }
    ```

    **Pontos criticos:**
    - `final class` + `private TelefoneBR()` — pure utility pattern, sem instancias
    - `Set.of(...)` — imutavel, performance O(1) lookup
    - `replaceAll("\\D", "")` — strip TODOS os nao-digitos (regex `\D` = nao-digito)
    - Branch order critico: numero nao-Brasil PRIMEIRO (early return), depois Set lookup, depois strip condicional
    - **NAO** adicionar 9o digito quando vier sem (caso Edge: SP DDD com 8 digitos locais nao causa erro — pode ser fixo, nao mobile)
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java && grep -c "DDDS_COM_NONO_DIGITO" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java</automated>
  </verify>
  <done>
    - Arquivo existe em `util/TelefoneBR.java`
    - `final class` com `private` constructor
    - `DDDS_COM_NONO_DIGITO` Set imutavel com 14 DDDs
    - Metodo `static String normalizar(String)` publico
    - Algoritmo segue exatamente RESEARCH §5.1
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificar GREEN — TelefoneBRTest 18+ tests passam</name>
  <files>(nenhum modificado — apenas validacao)</files>
  <action>
    Rodar `./mvnw test -pl api-whatsapp -Dtest=TelefoneBRTest -q` para confirmar:
    - Tests run: 18 (ou 19 se incluir os 2 exotic), Failures: 0, Errors: 0
    - Compilacao limpa (sem warnings)

    Se algum test falhar:
    - **`expected: "554784178525" but was: "5547984178525"`** → algoritmo nao strip 9o; verificar branch DDD fora do Set + numero.length()==9 + startsWith("9")
    - **`expected: "5511987654321" but was: "551187654321"`** → algoritmo strip 9 erradamente em SP; verificar `DDDS_COM_NONO_DIGITO.contains(ddd)` retorna early
    - **NPE em `null` test** → falta o `if (telefone == null) return null;` no inicio
    - **`expected: "" but was: null`** ou inverso → branch ordering errado em null vs empty

    **Verificar que NAO afeta build agregado:**
    ```bash
    ./mvnw verify -pl api-whatsapp -q
    # BUILD SUCCESS — Phase 1 baseline + spike + 18 TelefoneBR tests verdes
    ```
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=TelefoneBRTest -q</automated>
  </verify>
  <done>
    - Tests run: >= 18, Failures: 0, Errors: 0
    - Build agregado `mvnw verify -pl api-whatsapp` continua BUILD SUCCESS
    - Sem regressao em testes Phase 1 ou spike
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TelefoneBR.normalizar(input bruto) -> formato Meta | Pure function — sem I/O, sem state. Erro de logica = mensagens nao entregues silenciosamente (PITFALLS C-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | DDDs com/sem 9o digito (PITFALLS C-13) | mitigate | Set imutavel `DDDS_COM_NONO_DIGITO` com 14 DDDs literais (SP+RJ+ES); 18 tests cobrem matriz; entradas em set unitario (constants verificavel via grep) |
| T-02-07 | Repudiation | Numero nao-Brasil tratado como BR | mitigate | Branch early-return: `!digitos.startsWith("55") || len fora [12,13]` retorna sanitizado sem alteracao; test `usa_nao_brasil` valida +1 415 nao-altered |
| T-02-08 | Information Disclosure | Telefone em logs do Logger no chamador | accept | Pure utility nao loga; chamadores (Plan 04 ClienteZapService, Plan 05 parser) ja mascaram conforme padrao do monorepo |
</threat_model>

<verification>
## Phase Checks

1. `TelefoneBR.java` existe em `util/`
2. `TelefoneBRTest.java` existe com 18+ tests
3. `grep -c "@Test" api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java` >= 18
4. `mvnw test -pl api-whatsapp -Dtest=TelefoneBRTest` Tests run: >=18, Failures: 0
5. `mvnw verify -pl api-whatsapp` BUILD SUCCESS (sem regressao Phase 1)
6. Set DDDS_COM_NONO_DIGITO contem exatamente 14 DDDs (grep `\"\\d{2}\"` no arquivo)
</verification>

<success_criteria>
- TelefoneBR pure utility implementado com algoritmo exato do RESEARCH §5.1
- 18+ test cases cobrindo matriz de DDDs + sanitizacao + edge cases (null/empty/USA/curto/exotic)
- mvnw test -Dtest=TelefoneBRTest BUILD SUCCESS
- mvnw verify -pl api-whatsapp BUILD SUCCESS (sem regressao)
- 1 commit feat atomico via gsd-tools.cjs commit
- 1 commit docs SUMMARY apos
</success_criteria>

<commit>
Mensagem (Conventional Commits PT-BR):

```
feat(api-whatsapp): adicionar TelefoneBR.normalizar com 18 tests

Pure utility (sem Spring, private constructor) que normaliza telefones brasileiros
para o formato armazenado pelo WhatsApp Cloud API (PER-05 + PITFALLS C-13).

Regra ANATEL 2010 + WhatsApp formato antigo:
- DDDs SP (11-19), RJ (21,22,24), ES (27,28) PRESERVAM o 9o digito
- Demais DDDs strip o 9o digito (formato pre-2010)
- Numeros nao-Brasil: sanitiza sem alteracao
- null/empty: pass-through

Sem normalizacao, error 131026 silencioso do Meta entrega zero mensagens.

18 test cases cobrem: 4 DDDs strip (SC/MG/RS/PR) + 5 DDDs preserve (SP/RJ/ES)
+ 3 sanitizacao (formatado humano, sem +, ja sem 9) + 5 edge (null/empty/sem-digitos/USA/curto)
+ 2 exotic (DDD inexistente, numero >13 digitos).

Refs: D-03 (CONTEXT.md), PER-05 (REQUIREMENTS.md), 02-RESEARCH.md §5
```

Comando:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): adicionar TelefoneBR.normalizar com 18 tests" --files \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TelefoneBR.java \
  api-whatsapp/src/test/java/br/com/erpkit/whatsapp/util/TelefoneBRTest.java
```

Apos sucesso:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(02): adicionar SUMMARY plan 02" --files \
  .planning/phases/02-persistencia-idempotencia/02-02-SUMMARY.md
```
</commit>

<risks>
- **Algoritmo cobre apenas mobile com 9o digito** — numeros fixos brasileiros (8 digitos locais comecando NAO com 9) passam direto, o que esta correto. Mas se um cliente cadastrou numero fixo em formato esquisito (ex: "+55 11 3000-1234" — 12 digitos com 8 locais nao-9), o algoritmo retorna sanitizado sem alteracao (correto). Sem regressao.
- **DDD nao-existente (99, 00) ainda passa pelo Set lookup** — politica deliberada do RESEARCH §5.1: o algoritmo e baseado em Set, nao em validacao real de DDD. Se o input for invalido, fica invalido normalizado (Meta vai rejeitar com codigo proprio). Documentado em test exotic.
- **A6 RESEARCH UTF-8 4-byte chars em conteudo (NAO em telefone)** — TelefoneBR opera em ASCII digits, sem questao de charset. Para conteudo de mensagem (separado), Plans 03-05 confiam no Postgres TEXT + Hibernate UTF-8 default.
</risks>

<output>
Apos completar, criar `.planning/phases/02-persistencia-idempotencia/02-02-SUMMARY.md` com:
- TelefoneBR + TelefoneBRTest criados
- Tests run: 18+ Failures: 0
- Confirmacao da matriz de DDDs (SP/RJ/ES preserve, demais strip)
- Build agregado verde (sem regressao)
- Commit hash da mudanca
- Notas: utility usado por Plans 04 (ClienteZapService) e 05 (WebhookPayloadParser)
</output>
