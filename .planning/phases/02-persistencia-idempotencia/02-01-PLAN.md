---
phase: 02-persistencia-idempotencia
plan: 01
type: execute
wave: 1
depends_on: []
parallel_with: []
files_modified:
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/OnConflictSpikeTest.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MensagemLog.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/Direcao.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MensagemLogRepository.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MediaCacheRepository.java  # NEW
autonomous: true
requirements:
  - PER-02  # Migration V1 clientes_zap mapeada via @Entity ClienteZap
  - PER-03  # Migration V2 mensagens_log mapeada via @Entity MensagemLog + Direcao enum
  - PER-04  # Migration V3 media_cache mapeada via @Entity MediaCache
tags:
  - api-whatsapp
  - jpa
  - entities
  - repositories
  - spike
  - idempotency

must_haves:
  truths:
    - "Spike OnConflictSpikeTest valida que H2 v2.x modo PG aceita INSERT ... ON CONFLICT (col) DO NOTHING e retorna rowcount correto (1 se inseriu, 0 se conflito)"
    - "Se spike passar: comprometer com ON CONFLICT no Plan 03; se falhar: ativar fallback documentado (Secao 2.4 RESEARCH) com try/catch DataIntegrityViolationException"
    - "3 entities JPA (ClienteZap, MensagemLog, MediaCache) mapeiam 1:1 com schema whatsapp deployado (V1-V4) via @Table(schema = whatsapp, name = ...)"
    - "Enum Direcao { in, out } em LOWERCASE bate com CHECK constraint do DB e mapeia via @Enumerated(EnumType.STRING)"
    - "TipoMensagem como String constants (TEXT, INTERACTIVE_BUTTON, INTERACTIVE_LIST, DOCUMENT, IMAGE, AUDIO, DESCONHECIDO) em vez de enum estrito"
    - "3 repositories esqueleto (sem queries customizadas ainda) extends JpaRepository<Entity, IdType>"
    - "Hibernate ddl-auto: validate aceita as 3 entities contra o schema da Phase 1 — boot da app NAO falha"
    - "criadoEm com insertable=false, updatable=false respeita DEFAULT NOW() do banco — A4 RESEARCH validado"
    - "Sem Lombok, sem novas dependencias, getters/setters explicitos (CONVENTIONS.md)"
    - "mvnw verify -pl api-whatsapp BUILD SUCCESS — todos os tests Phase 1 + spike + smoke verdes"
  artifacts:
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/OnConflictSpikeTest.java"
      provides: "Spike validando ON CONFLICT em H2 PG-mode (2 testes)"
      contains: "ON CONFLICT (telefone) DO NOTHING"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java"
      provides: "@Entity mapeando whatsapp.clientes_zap"
      contains: "@Table(schema = \"whatsapp\", name = \"clientes_zap\")"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MensagemLog.java"
      provides: "@Entity mapeando whatsapp.mensagens_log com @Enumerated(STRING) Direcao"
      contains: "@Enumerated(EnumType.STRING)"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java"
      provides: "@Entity mapeando whatsapp.media_cache (PK String arquivoHash CHAR(64))"
      contains: "@Table(schema = \"whatsapp\", name = \"media_cache\")"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/Direcao.java"
      provides: "Enum Direcao { in, out } em lowercase"
      contains: "public enum Direcao"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java"
      provides: "String constants para tipo de mensagem (D-05)"
      contains: "DESCONHECIDO"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java"
      provides: "JpaRepository<ClienteZap, Long> esqueleto"
      contains: "extends JpaRepository<ClienteZap, Long>"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MensagemLogRepository.java"
      provides: "JpaRepository<MensagemLog, Long> esqueleto"
      contains: "extends JpaRepository<MensagemLog, Long>"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MediaCacheRepository.java"
      provides: "JpaRepository<MediaCache, String> esqueleto"
      contains: "extends JpaRepository<MediaCache, String>"
  key_links:
    - from: "Spike OnConflictSpikeTest"
      to: "Decisao tecnica do Plan 03 (ON CONFLICT vs fallback)"
      via: "passa = ON CONFLICT; falha = fallback DataIntegrityViolationException"
      pattern: "ON CONFLICT \\(\\w+\\) DO NOTHING"
    - from: "@Entity ClienteZap/MensagemLog/MediaCache"
      to: "Schema whatsapp deployado V1-V4"
      via: "@Table(schema=whatsapp, name=...) + Hibernate ddl-auto: validate"
      pattern: "@Table\\(schema = \"whatsapp\""
    - from: "Direcao { in, out }"
      to: "CHECK constraint chk_mensagens_log_direcao"
      via: "@Enumerated(EnumType.STRING) + Enum.name() == constant spelling"
      pattern: "@Enumerated\\(EnumType\\.STRING\\)"
---

<objective>
Wave 1 desta phase: validar empiricamente que H2 v2.x modo PostgreSQL aceita `INSERT ... ON CONFLICT (col) DO NOTHING` retornando rowcount correto (spike de 5 min, mesmo padrao da Wave 4 da Phase 1 com `BIGINT GENERATED`), depois comprometer com 3 entities JPA + 3 repositories esqueleto + enum `Direcao` + constants `TipoMensagem`.

Purpose: Spike primeiro para nao construir 6 services em cima de uma sintaxe podre. Entities/repos esqueleto fixam o contrato JPA contra o schema deployado da Phase 1 (V1-V4) — Hibernate `ddl-auto: validate` confirma que tipos/colunas batem. Plans 02..06 dependem deste plan; Plans 02 e 03 podem rodar em paralelo apos este.

Output:
- 1 spike test (`OnConflictSpikeTest`) com 2 cenarios — passa ou falha visivelmente
- 3 entities (`ClienteZap`, `MensagemLog`, `MediaCache`) com `@Table(schema=whatsapp)` + `Instant` timestamps + sem Lombok
- 1 enum `Direcao { in, out }` em LOWERCASE
- 1 constants class `TipoMensagem`
- 3 repositories esqueleto (extends `JpaRepository`, sem `@Query` customizadas ainda — adicionados em Plans 03 e 04)
- `mvnw verify -pl api-whatsapp` BUILD SUCCESS — entities batem com schema, spike passa
- 1 commit feat atomico + 1 commit docs SUMMARY

Decisao tecnica registrada no SUMMARY: ON CONFLICT funcionou (esperado MEDIUM-HIGH confidence) ou fallback ativado (Plan 03 muda design).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-persistencia-idempotencia/02-CONTEXT.md
@.planning/phases/02-persistencia-idempotencia/02-RESEARCH.md
@.planning/phases/01-fundacao-hmac-webhook/01-04-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@api-whatsapp/src/main/resources/db/migration/V1__criar_tabela_clientes_zap.sql
@api-whatsapp/src/main/resources/db/migration/V2__criar_tabela_mensagens_log.sql
@api-whatsapp/src/main/resources/db/migration/V3__criar_tabela_media_cache.sql
@api-whatsapp/src/main/resources/db/migration/V4__criar_tabela_estado_conversa.sql
@api-whatsapp/src/main/resources/application.yml
@api-whatsapp/src/test/resources/application-test.yml
@api-email/src/main/java/br/com/erpkit/email/model/Email.java
@api-email/src/main/java/br/com/erpkit/email/repository/EmailRepository.java

<interfaces>
<!-- Schema deployado em Phase 1 (V1-V4) — entities devem bater com estes tipos/nomes -->

V1 whatsapp.clientes_zap:
```sql
CREATE TABLE whatsapp.clientes_zap (
    id                  BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    id_cliente_erp      BIGINT,
    telefone            VARCHAR(20) NOT NULL UNIQUE,
    ultima_mensagem_em  TIMESTAMP,
    criado_em           TIMESTAMP NOT NULL DEFAULT NOW()
);
```

V2 whatsapp.mensagens_log:
```sql
CREATE TABLE whatsapp.mensagens_log (
    id          BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    wamid       VARCHAR(255) NOT NULL UNIQUE,
    telefone    VARCHAR(20) NOT NULL,
    direcao     VARCHAR(3) NOT NULL,
    tipo        VARCHAR(50),
    conteudo    TEXT,
    media_id    VARCHAR(255),
    criado_em   TIMESTAMP NOT NULL DEFAULT NOW(),
    CONSTRAINT chk_mensagens_log_direcao CHECK (direcao IN ('in', 'out'))
);
```

V3 whatsapp.media_cache:
```sql
CREATE TABLE whatsapp.media_cache (
    arquivo_hash    CHAR(64) PRIMARY KEY,
    media_id        VARCHAR(255) NOT NULL,
    criado_em       TIMESTAMP NOT NULL DEFAULT NOW(),
    expira_em       TIMESTAMP NOT NULL
);
```

Padrao @Entity do monorepo (api-email/Email.java):
- Sem Lombok — getters/setters explicitos
- @Id @GeneratedValue(strategy = GenerationType.IDENTITY) para BIGINT GENERATED
- @Column(name="...") com snake_case
- equals/hashCode minimos via id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STEP 0 — Spike OnConflictSpikeTest (5 min, gate da phase)</name>
  <files>api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/OnConflictSpikeTest.java</files>
  <behavior>
    2 testes empiricos validando que H2 v2.x modo PostgreSQL aceita `INSERT ... ON CONFLICT (col) DO NOTHING` e retorna rowcount correto:

    1. `on_conflict_do_nothing_em_clientes_zap_telefone`: 1a insercao retorna `1`; 2a insercao com mesmo telefone retorna `0` SEM lancar excecao; SELECT COUNT confirma 1 linha.
    2. `on_conflict_do_nothing_em_mensagens_log_wamid`: mesma logica para `mensagens_log.wamid` (a tabela real do gate Phase 2). Confirma que telefone original e preservado (DO NOTHING — nao update).
  </behavior>
  <action>
    Criar `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/OnConflictSpikeTest.java` copiando o codigo COMPLETO da Secao 2.2 do `02-RESEARCH.md` (linhas 146-232). Codigo abaixo (copiar literalmente):

    ```java
    package br.com.erpkit.whatsapp.spike;

    import br.com.erpkit.whatsapp.WhatsAppApplication;
    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.jdbc.core.JdbcTemplate;
    import org.springframework.test.context.ActiveProfiles;

    import static org.assertj.core.api.Assertions.assertThat;

    /**
     * SPIKE da Wave 1 — Phase 2 (mesmo padrao da Wave 4 Phase 1).
     * Valida empiricamente que H2 v2.x modo PostgreSQL aceita
     * INSERT ... ON CONFLICT (col) DO NOTHING e retorna rowcount correto.
     *
     * <p>Se passar: prossegue Plans 02..07 com ON CONFLICT (D-02 RESEARCH §6).
     * Se falhar: aciona fallback (RESEARCH §2.4) — Plan 03 muda design para
     * try/catch DataIntegrityViolationException apos save().
     */
    @SpringBootTest(classes = WhatsAppApplication.class)
    @ActiveProfiles("test")
    class OnConflictSpikeTest {

        @Autowired
        JdbcTemplate jdbc;

        @Test
        void on_conflict_do_nothing_em_clientes_zap_telefone() {
            int primeira = jdbc.update(
                "INSERT INTO whatsapp.clientes_zap (telefone) VALUES (?) " +
                "ON CONFLICT (telefone) DO NOTHING",
                "5511spike001"
            );
            assertThat(primeira).as("Primeira insercao deve afetar 1 linha").isEqualTo(1);

            int segunda = jdbc.update(
                "INSERT INTO whatsapp.clientes_zap (telefone) VALUES (?) " +
                "ON CONFLICT (telefone) DO NOTHING",
                "5511spike001"
            );
            assertThat(segunda).as("Conflict deve retornar 0 sem lancar excecao").isZero();

            Integer count = jdbc.queryForObject(
                "SELECT COUNT(*) FROM whatsapp.clientes_zap WHERE telefone = ?",
                Integer.class, "5511spike001"
            );
            assertThat(count).isEqualTo(1);
        }

        @Test
        void on_conflict_do_nothing_em_mensagens_log_wamid() {
            int primeira = jdbc.update(
                "INSERT INTO whatsapp.mensagens_log (wamid, telefone, direcao) " +
                "VALUES (?, ?, ?) ON CONFLICT (wamid) DO NOTHING",
                "wamid.spike.001", "5511spike002", "in"
            );
            assertThat(primeira).isEqualTo(1);

            int segunda = jdbc.update(
                "INSERT INTO whatsapp.mensagens_log (wamid, telefone, direcao) " +
                "VALUES (?, ?, ?) ON CONFLICT (wamid) DO NOTHING",
                "wamid.spike.001", "5511spike003", "in"
            );
            assertThat(segunda).isZero();

            String telefone = jdbc.queryForObject(
                "SELECT telefone FROM whatsapp.mensagens_log WHERE wamid = ?",
                String.class, "wamid.spike.001"
            );
            assertThat(telefone).isEqualTo("5511spike002");
        }
    }
    ```

    **Como rodar SPIKE isoladamente** (antes de criar entities — gate da phase):
    ```bash
    ./mvnw test -pl api-whatsapp -Dtest=OnConflictSpikeTest -q
    ```

    **Decisao apos spike:**
    - **Se PASSAR (esperado, MEDIUM-HIGH confidence):** prosseguir com Tasks 2-9. Plan 03 usara `INSERT ... ON CONFLICT` native query.
    - **Se FALHAR:** registrar no SUMMARY o erro exato (ex: "Syntax error in SQL statement [...] expected SET, ALL"), notificar via `## ALERTA — fallback acionado` e prosseguir Tasks 2-9 (entities + repos esqueleto NAO dependem da decisao). Plan 03 entao implementara o fallback da Secao 2.4 RESEARCH (try/catch DataIntegrityViolationException apos save()).

    **Importante:** o spike test NAO deve ser deletado apos passar — fica como regression test. RESEARCH §2.3 sugere renomear para `OnConflictPgModeSupportTest` em `db/`, mas para Phase 2 mantenha em `spike/` para localizacao clara da decisao.
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=OnConflictSpikeTest -q</automated>
  </verify>
  <done>
    - Arquivo `OnConflictSpikeTest.java` existe em `spike/` package
    - Tests run: 2, Failures: 0 (se passou — caminho feliz, comprometer com ON CONFLICT)
    - OU: failure documentado no SUMMARY + decisao de fallback registrada (caminho alternativo)
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar enum Direcao { in, out } em lowercase</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/Direcao.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/Direcao.java` com codigo COMPLETO da Secao 3.2 do `02-RESEARCH.md` (linhas 375-394):

    ```java
    package br.com.erpkit.whatsapp.model;

    /**
     * Direcao da mensagem em {@code mensagens_log}. Mapeada via {@code @Enumerated(STRING)}.
     * Constants em LOWERCASE deliberadamente — Hibernate {@code STRING} mode usa
     * {@link Enum#name()}, que em Java e o exato spelling do constant.
     * {@code Direcao.in.name()} = {@code "in"}.
     *
     * <p>O CHECK constraint na V2 migration ({@code direcao IN ('in', 'out')}) bate
     * com essa convencao. Spike STEP 0 da Phase 1 confirmou que H2 PG-mode NAO
     * silencia o CHECK.
     *
     * <p>Identificadores em lowercase violam convencao Java (UPPER_SNAKE_CASE).
     * Trade-off deliberado: simplicidade do mapping JPA vs convencao. Documentado
     * em CONVENTIONS.md per D-01 do CONTEXT.md.
     */
    public enum Direcao {
        in,
        out
    }
    ```

    **Critico:** lowercase NAO E TYPO — e D-01 do CONTEXT.md + A7 do RESEARCH. Hibernate `@Enumerated(STRING)` usa `Enum.name()` que e o exato spelling. `Direcao.in.name()` = `"in"`, casando com `CHECK (direcao IN ('in', 'out'))` da V2.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/Direcao.java && grep -E "^\s+(in|out)," api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/Direcao.java | wc -l</automated>
  </verify>
  <done>
    - Arquivo existe em package `br.com.erpkit.whatsapp.model`
    - Contem 2 constants: `in` e `out` (lowercase exatamente, sem `IN`/`OUT`)
    - Compila sem warnings de naming convention (suprimir checkstyle se aparecer)
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar @Entity ClienteZap mapeando whatsapp.clientes_zap</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java` com codigo COMPLETO da Secao 3.1 do `02-RESEARCH.md` (linhas 273-369). Copiar literalmente — incluindo:
    - Package `br.com.erpkit.whatsapp.model`
    - Imports jakarta.persistence + java.time.Instant + java.util.Objects
    - `@Entity @Table(schema = "whatsapp", name = "clientes_zap")`
    - 5 colunas: `id` (Long, IDENTITY), `idClienteErp` (Long, nullable), `telefone` (String, unique=true, length=20), `ultimaMensagemEm` (Instant), `criadoEm` (Instant, insertable=false, updatable=false)
    - Construtor padrao + construtor `ClienteZap(String telefone, Long idClienteErp)`
    - Getters/setters explicitos para todos exceto `criadoEm` (so getter — campo gerenciado pelo banco)
    - `equals/hashCode/toString` baseados em `id` (toString mascara telefone PII)

    **Pontos criticos:**
    - `@GeneratedValue(strategy = GenerationType.IDENTITY)` casa com `BIGINT GENERATED ALWAYS AS IDENTITY` da V1 (D-01 + A7 RESEARCH)
    - `Instant` (NAO `LocalDateTime`) — UTC-anchored, alinhado com Phase 4 trava 24h
    - `criadoEm insertable=false, updatable=false` — A4 RESEARCH: Hibernate ignora INSERT/UPDATE, le no SELECT, banco preenche via DEFAULT NOW()
    - `toString()` MASCARA telefone (mostra so primeiros 4 digitos + `***`) — defesa em profundidade contra leak de PII em logs

    Codigo completo em RESEARCH §3.1 — copiar inteiro sem modificacao.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java && grep -c "@Table(schema = \"whatsapp\", name = \"clientes_zap\")" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java</automated>
  </verify>
  <done>
    - Arquivo existe em package model/
    - `@Table(schema = "whatsapp", name = "clientes_zap")` presente (grep retorna 1)
    - 5 campos com tipos corretos (Long id, Long idClienteErp, String telefone, Instant ultimaMensagemEm, Instant criadoEm)
    - `@GeneratedValue(strategy = GenerationType.IDENTITY)` no id
    - criadoEm com `insertable = false, updatable = false`
    - getters/setters explicitos sem Lombok
  </done>
</task>

<task type="auto">
  <name>Task 4: Criar @Entity MensagemLog com @Enumerated(STRING) Direcao + @Lob conteudo</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MensagemLog.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MensagemLog.java` com codigo COMPLETO da Secao 3.2 do `02-RESEARCH.md` (linhas 396-518). Copiar literalmente — incluindo:
    - Package `br.com.erpkit.whatsapp.model`
    - Imports jakarta.persistence (incluindo EnumType, Enumerated, Lob) + java.time.Instant + java.util.Objects
    - `@Entity @Table(schema = "whatsapp", name = "mensagens_log")`
    - 8 colunas: `id` (Long, IDENTITY), `wamid` (String, unique=true, nullable=false, length=255), `telefone` (String, length=20), `direcao` (Direcao, `@Enumerated(EnumType.STRING)`, length=3), `tipo` (String — NAO enum, flexivel para "desconhecido"), `conteudo` (String, `@Lob`), `mediaId` (String, length=255), `criadoEm` (Instant, insertable=false)
    - Construtor padrao + construtor `MensagemLog(String wamid, String telefone, Direcao direcao, String tipo, String conteudo, String mediaId)`
    - Getters/setters explicitos
    - `equals/hashCode/toString` — toString NAO inclui `conteudo` (PII)

    **Pontos criticos:**
    - `@Enumerated(EnumType.STRING)` em `direcao` — A7 RESEARCH: Hibernate persiste `Direcao.in.name()` = `"in"`, casando com CHECK constraint
    - `tipo` como `String` (NAO enum) — D-05 + A8 RESEARCH: flexivel para tipos novos do Meta + "desconhecido"
    - `@Lob` em `conteudo` — payloads de texto/lista interactive podem exceder VARCHAR; mapeia para TEXT no Postgres / CLOB no H2

    Codigo completo em RESEARCH §3.2 — copiar inteiro sem modificacao.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MensagemLog.java && grep -c "@Enumerated(EnumType.STRING)" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MensagemLog.java</automated>
  </verify>
  <done>
    - Arquivo existe
    - `@Table(schema = "whatsapp", name = "mensagens_log")` presente
    - `@Enumerated(EnumType.STRING)` em direcao (grep retorna 1)
    - `@Lob` em conteudo
    - `tipo` declarado como `String` (NAO `TipoMensagem` enum)
    - 8 campos com tipos corretos
  </done>
</task>

<task type="auto">
  <name>Task 5: Criar @Entity MediaCache com PK String arquivoHash CHAR(64)</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java` com codigo COMPLETO da Secao 3.3 do `02-RESEARCH.md` (linhas 522-604). Copiar literalmente — incluindo:
    - `@Entity @Table(schema = "whatsapp", name = "media_cache")`
    - 4 colunas: `arquivoHash` (String, `@Id` PK, length=64), `mediaId` (String, nullable=false, length=255), `criadoEm` (Instant, insertable=false), `expiraEm` (Instant, nullable=false)
    - SEM `@GeneratedValue` no id — `arquivoHash` e a propria PK (sha256 hex, valor da aplicacao)
    - Construtor padrao + `MediaCache(String arquivoHash, String mediaId, Instant expiraEm)`
    - Getters/setters explicitos
    - `equals/hashCode` baseados em `arquivoHash` (e a PK natural)

    **Pontos criticos:**
    - PK e String (NAO Long) — `arquivoHash` e sha256 hex de exatamente 64 caracteres
    - Phase 2 cria entity + repo mas NAO usa em runtime (MediaCacheService e Phase 4) — entity disponivel ja agrupada coesivamente
    - Smoke test apenas: confirmar que repository carrega no contexto Spring (Task 9)

    Codigo completo em RESEARCH §3.3.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java && grep -c "@Id" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java</automated>
  </verify>
  <done>
    - Arquivo existe
    - `@Table(schema = "whatsapp", name = "media_cache")` presente
    - PK e `arquivoHash` String com `@Id` (sem `@GeneratedValue`)
    - `length = 64` em arquivoHash (CHAR(64) sha256 hex)
    - 4 campos: arquivoHash, mediaId, criadoEm, expiraEm
  </done>
</task>

<task type="auto">
  <name>Task 6: Criar TipoMensagem String constants</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java` com codigo COMPLETO da Secao 8.3 do `02-RESEARCH.md` (linhas 1758-1778):

    ```java
    package br.com.erpkit.whatsapp.util;

    /**
     * String constants para o campo {@code tipo} de {@code mensagens_log}.
     *
     * <p>Constants em vez de enum — flexibilidade para tipos novos do Meta sem
     * precisar release. Tipos desconhecidos persistem com {@link #DESCONHECIDO}
     * (WEB-07 + D-05 do CONTEXT.md).
     */
    public final class TipoMensagem {
        public static final String TEXT = "text";
        public static final String INTERACTIVE_BUTTON = "interactive_button";
        public static final String INTERACTIVE_LIST = "interactive_list";
        public static final String DOCUMENT = "document";
        public static final String IMAGE = "image";
        public static final String AUDIO = "audio";
        public static final String DESCONHECIDO = "desconhecido";

        private TipoMensagem() {}
    }
    ```

    **Pontos criticos:**
    - `final class` + private constructor — utility pattern, sem instancias
    - Constants UPPER_SNAKE_CASE (Java convention) com VALORES lowercase (matching strings que vem do Meta)
    - 7 constants cobrindo o espectro do RESEARCH §8.4 mapTipo()
    - Phase 5 (parser) e Phase 6 (orquestrador) usam estes constants
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java && grep -c "public static final String" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java</automated>
  </verify>
  <done>
    - Arquivo existe em package `br.com.erpkit.whatsapp.util`
    - 7 constants (grep retorna 7): TEXT, INTERACTIVE_BUTTON, INTERACTIVE_LIST, DOCUMENT, IMAGE, AUDIO, DESCONHECIDO
    - Constructor private (utility pattern)
  </done>
</task>

<task type="auto">
  <name>Task 7: Criar ClienteZapRepository esqueleto</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java</files>
  <action>
    Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java` ESQUELETO — apenas extends `JpaRepository<ClienteZap, Long>` SEM `findByTelefone` ou `atualizarUltimaMensagemEm` (esses entram no Plan 04):

    ```java
    package br.com.erpkit.whatsapp.repository;

    import br.com.erpkit.whatsapp.model.ClienteZap;
    import org.springframework.data.jpa.repository.JpaRepository;

    /**
     * Repository do {@link ClienteZap}. Phase 2 Plan 01 — esqueleto apenas
     * (extends JpaRepository com CRUD basico).
     *
     * <p>Plan 04 adiciona:
     * <ul>
     *   <li>{@code Optional<ClienteZap> findByTelefone(String telefone)}</li>
     *   <li>{@code int atualizarUltimaMensagemEm(String telefone)} — native @Query com NOW()</li>
     * </ul>
     */
    public interface ClienteZapRepository extends JpaRepository<ClienteZap, Long> {
    }
    ```

    **Por que esqueleto:** Plan 04 (`ClienteZapService`) adiciona `findByTelefone` + `atualizarUltimaMensagemEm` quando o service consumir esses metodos. Mantem coesao no commit do Plan 04.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java && grep -c "extends JpaRepository<ClienteZap, Long>" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java</automated>
  </verify>
  <done>
    - Arquivo existe
    - Interface extends JpaRepository<ClienteZap, Long>
    - Sem `@Query` ou metodos custom ainda (esqueleto)
  </done>
</task>

<task type="auto">
  <name>Task 8: Criar MensagemLogRepository + MediaCacheRepository esqueletos</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MensagemLogRepository.java, api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MediaCacheRepository.java</files>
  <action>
    Criar 2 repositories esqueleto:

    **MensagemLogRepository.java** — esqueleto SEM `inserirSeNovo` (Plan 03 adiciona):
    ```java
    package br.com.erpkit.whatsapp.repository;

    import br.com.erpkit.whatsapp.model.MensagemLog;
    import org.springframework.data.domain.Page;
    import org.springframework.data.domain.Pageable;
    import org.springframework.data.jpa.repository.JpaRepository;

    import java.util.Optional;

    /**
     * Repository do {@link MensagemLog}. Phase 2 Plan 01 — esqueleto apenas com
     * findByWamid + findByTelefoneOrderByCriadoEmDesc (helpers para tests + futuras
     * consultas).
     *
     * <p>Plan 03 adiciona o gate atomico de idempotencia:
     * {@code int inserirSeNovo(...)} via native @Query com ON CONFLICT (wamid)
     * DO NOTHING (ou fallback save() + try/catch DataIntegrityViolationException
     * dependendo do resultado do spike Wave 1).
     */
    public interface MensagemLogRepository extends JpaRepository<MensagemLog, Long> {

        /** Helper para tests + futuras consultas (Phase 4 historico). */
        Optional<MensagemLog> findByWamid(String wamid);

        /** Helper para tests + listagem cronologica futura. */
        Page<MensagemLog> findByTelefoneOrderByCriadoEmDesc(String telefone, Pageable pageable);
    }
    ```

    **MediaCacheRepository.java** — esqueleto com 1 helper (RESEARCH §4.3):
    ```java
    package br.com.erpkit.whatsapp.repository;

    import br.com.erpkit.whatsapp.model.MediaCache;
    import org.springframework.data.jpa.repository.JpaRepository;

    import java.time.Instant;
    import java.util.Optional;

    /**
     * Repository do {@link MediaCache}. Phase 2 NAO usa em runtime — Phase 4
     * ({@code MediaCacheService}) consome com TTL 30d.
     *
     * <p>Helper {@code findByArquivoHashAndExpiraEmAfter} declarado para Phase 4
     * ja achar a superficie pronta. Smoke test do Plan 01 valida que o repository
     * carrega no contexto Spring.
     */
    public interface MediaCacheRepository extends JpaRepository<MediaCache, String> {

        Optional<MediaCache> findByArquivoHashAndExpiraEmAfter(String arquivoHash, Instant agora);
    }
    ```

    **Por que esqueletos com helpers:** RESEARCH §4 ja define os helpers (Spring Data JPA derived queries). Eles nao requerem `@Query` customizada — sao derivados do nome do metodo. Custo zero. Plan 03 adiciona apenas o `inserirSeNovo` native.
  </action>
  <verify>
    <automated>test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MensagemLogRepository.java && test -f api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MediaCacheRepository.java && grep -c "findByWamid" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MensagemLogRepository.java</automated>
  </verify>
  <done>
    - 2 arquivos existem em repository/
    - MensagemLogRepository extends JpaRepository<MensagemLog, Long> com 2 derived queries (findByWamid, findByTelefoneOrderByCriadoEmDesc)
    - MediaCacheRepository extends JpaRepository<MediaCache, String> com 1 derived query
    - SEM `inserirSeNovo` ainda (Plan 03 adiciona)
  </done>
</task>

<task type="auto">
  <name>Task 9: Verificar build do reator + smoke test JPA</name>
  <files>(nenhum modificado — apenas validacao)</files>
  <action>
    Rodar `./mvnw verify -pl api-whatsapp` para confirmar que:

    1. **Hibernate ddl-auto: validate aceita as 3 entities** contra o schema deployado V1-V4. Se Hibernate detectar mismatch (ex: tipo errado, coluna missing), boot do Spring falha com `SchemaManagementException` — sinal de que entity nao bate com schema; corrigir entity.

    2. **Spike test passou** — `OnConflictSpikeTest` Tests run: 2, Failures: 0.

    3. **Tests preexistentes da Phase 1 continuam verdes** — `WhatsAppPropertiesTest`, `HmacValidatorTest`, `WebhookControllerIntegrationTest`, `FlywayMigrationTest`, etc. NAO devem regredir.

    4. **Repositories carregam no contexto** — quando Spring boota o test context, ele faz scan de `@Repository`/`JpaRepository` interfaces. Se algum tipo de retorno for invalido (ex: derived query name nao matchando coluna), boot falha com `PropertyReferenceException`.

    Se algum test falhar:
    - **"Schema validation: missing column [X] in table [whatsapp.Y]"** → entity tem nome de coluna errado (`@Column(name="...")` nao bate com migration); corrigir entity.
    - **"Schema validation: wrong column type encountered in column [criado_em]"** → tipo Java incompativel com TIMESTAMP; usar `Instant` (NAO `LocalDateTime`).
    - **"PropertyReferenceException: No property 'X' found"** → derived query no repo tem nome errado; corrigir method name.
    - **"Syntax error in SQL statement: ON CONFLICT"** (no spike) → fallback acionado; documentar no SUMMARY e prosseguir; Plan 03 entao implementara fallback.
    - **DataIntegrityViolationException no spike INSERT direcao='in'** → CHECK constraint falhando; verificar V2 e Direcao em lowercase.

    Output esperado:
    ```
    Tests run: ~12-15
    BUILD SUCCESS in ~30s
    ```
    (10 tests Phase 1 + 2 spike + smoke from JPA boot)
  </action>
  <verify>
    <automated>./mvnw verify -pl api-whatsapp -q</automated>
  </verify>
  <done>
    - BUILD SUCCESS
    - Tests run >= 12 (Phase 1 baseline + spike +2)
    - Failures: 0, Errors: 0
    - Spike test status registrado no SUMMARY: PASSOU (esperado) ou FALHOU + decisao de fallback para Plan 03
    - Hibernate validate aceita as 3 entities (boot OK)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hibernate ddl-auto: validate -> schema whatsapp | Validacao no boot impede entity drift do schema deployado |
| H2 (test) vs PostgreSQL (prod) | SQL deve produzir resultado identico nos dois — ON CONFLICT spike valida especificamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Sintaxe ON CONFLICT em H2 PG-mode (A1 RESEARCH) | mitigate | Spike Wave 1 OnConflictSpikeTest valida empiricamente; fallback documentado §2.4 (try/catch DataIntegrityViolationException) |
| T-02-02 | Tampering | Direcao enum lowercase vs CHECK constraint (A7) | mitigate | `@Enumerated(STRING)` usa Enum.name() — Phase 1 spike STEP 0 ja confirmou que H2 PG nao silencia CHECK; Task 9 boot valida que insert via JPA funciona |
| T-02-03 | Information Disclosure | Telefone/conteudo em toString() (logs, exceptions) | mitigate | ClienteZap.toString() mascara telefone (4 digits + ***); MensagemLog.toString() omite conteudo completo |
| T-02-04 | Tampering | criadoEm com insertable=false e DEFAULT NOW() (A4) | mitigate | Hibernate doc confirma que campos com insertable=false sao excluidos do INSERT mas mapeados em SELECT; Task 9 boot valida via FlywayMigrationTest existente que faz INSERT direto |
| T-02-05 | Schema drift | Entity nao bate com migration (Hibernate validate) | mitigate | ddl-auto: validate (configurado Phase 1) falha rapido no boot se houver mismatch — Task 9 e o gate empirico |
</threat_model>

<verification>
## Phase Checks

1. `OnConflictSpikeTest` existe e roda — Tests run: 2 (decisao tecnica registrada no SUMMARY: PASSOU ou fallback)
2. 3 entities existem em `model/` — `ClienteZap.java`, `MensagemLog.java`, `MediaCache.java`
3. `Direcao` enum existe com 2 constants em LOWERCASE
4. `TipoMensagem` constants class existe com 7 constants
5. 3 repositories existem em `repository/` — esqueletos sem `inserirSeNovo` ainda
6. `grep -c "@Table(schema = \"whatsapp\"" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/*.java` retorna 3
7. `grep -c "GenerationType.IDENTITY" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/*.java` retorna 2 (ClienteZap, MensagemLog — MediaCache usa String PK natural)
8. `./mvnw verify -pl api-whatsapp` BUILD SUCCESS — Tests run >= 12
9. Hibernate `validate` aceita as 3 entities (boot OK)
</verification>

<success_criteria>
- Spike OnConflictSpikeTest 2 testes passam (caminho feliz) OU falham com decisao registrada (caminho fallback)
- 3 entities JPA com `@Table(schema = "whatsapp", ...)` mapeiam V1-V4 sem mismatch
- Enum Direcao em lowercase + TipoMensagem constants
- 3 repositories esqueleto extends JpaRepository
- Hibernate ddl-auto: validate boot OK
- mvnw verify -pl api-whatsapp BUILD SUCCESS
- 1 commit feat atomico via gsd-tools.cjs commit
- 1 commit docs SUMMARY apos
</success_criteria>

<commit>
Mensagem (Conventional Commits PT-BR):

```
feat(api-whatsapp): adicionar entities + repos esqueleto + spike ON CONFLICT

Spike OnConflictSpikeTest valida ON CONFLICT (col) DO NOTHING em H2 PG-mode (decisao
tecnica para Plan 03 idempotency: ON CONFLICT direto vs fallback save() + catch).

3 entities mapeiam schema whatsapp deployado pela Phase 1 V1-V4:
- ClienteZap (id BIGINT IDENTITY, telefone UNIQUE, ultima_mensagem_em, criado_em)
- MensagemLog (wamid UNIQUE, direcao @Enumerated STRING, conteudo @Lob, criado_em)
- MediaCache (arquivo_hash CHAR(64) PK, expira_em)

Enum Direcao { in, out } em lowercase para casar com CHECK constraint
(@Enumerated(STRING) usa Enum.name()).

TipoMensagem como String constants (D-05) — flexivel para tipos novos do Meta + DESCONHECIDO.

3 repositories esqueleto — Plans 03/04 adicionam queries customizadas (inserirSeNovo,
findByTelefone, atualizarUltimaMensagemEm).

Hibernate ddl-auto: validate aceita as 3 entities — schema bate.

Refs: D-01/D-02 (CONTEXT.md), PER-02/03/04 (REQUIREMENTS.md), 02-RESEARCH.md §2 §3 §4 §8.3
```

Comando:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): adicionar entities + repos esqueleto + spike ON CONFLICT" --files \
  api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/OnConflictSpikeTest.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/ClienteZap.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MensagemLog.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/Direcao.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/ClienteZapRepository.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MensagemLogRepository.java \
  api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MediaCacheRepository.java
```

Apos sucesso, segundo commit docs:
```bash
node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(02): adicionar SUMMARY plan 01" --files \
  .planning/phases/02-persistencia-idempotencia/02-01-SUMMARY.md
```
</commit>

<risks>
- **A1 (RESEARCH §13): ON CONFLICT pode falhar em H2 v2.3.232 modo PG** — Spike e o gate empirico (Task 1). Se falhar: fallback `save()` + catch `DataIntegrityViolationException` documentado em §2.4; impacto: Plan 03 muda o design de `inserirSeNovo` (mas API publica `IdempotencyService.tentarPersistir` permanece identica). Tests do Plan 03 sao identicos.
- **A4 (RESEARCH §13): criadoEm com insertable=false pode nao popular apos save()** — Hibernate doc confirma que e suportado, mas se algum test do Plan 03 falhar com `criadoEm IS NULL` apos save+refresh, fallback: usar `@Generated(GenerationTime.INSERT)` (Hibernate 6+) ou `@PrePersist` setando `Instant.now()` da JVM (custo: clock skew JVM-DB, contradiz NOW() do banco).
- **A7 (RESEARCH §13): Direcao lowercase pode disparar CHECK constraint** — Spike STEP 0 da Phase 1 ja confirmou que H2 PG-mode NAO silencia CHECK; Task 9 boot + spike Task 1 (que faz INSERT direto com 'in') validam empiricamente.
- **Hibernate validate em campos novos** — se Phase 1 V4 estado_conversa nao tem entity correspondente, Hibernate pode reclamar (entity esperada para tabela existente). Phase 2 NAO cria EstadoConversa entity — D6 PROJECT diz so `ultima_mensagem_em` (em clientes_zap, NAO em estado_conversa). Validate so verifica entities declaradas vs schema, nao o contrario — OK. Se reclamar, eu errei: precisamos confirmar comportamento.
</risks>

<output>
Apos completar, criar `.planning/phases/02-persistencia-idempotencia/02-01-SUMMARY.md` com:
- Spike OnConflictSpikeTest resultado: PASSOU (caminho feliz) ou FALHOU (registrar erro exato + decisao de fallback para Plan 03)
- 3 entities + 3 repositories + Direcao + TipoMensagem criados
- Hibernate validate confirmou schema bate
- Tests run total + BUILD SUCCESS
- Commit hash da mudanca
- 0 surpresas em A4/A7 (criadoEm + Direcao lowercase) — registrar
</output>
