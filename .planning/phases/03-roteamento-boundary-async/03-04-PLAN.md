---
phase: 03-roteamento-boundary-async
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
parallel_with: []
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ErpCallbackClient.java  # NEW
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ErpCallbackClientTest.java  # NEW
autonomous: true
requirements:
  - ROU-02  # POST {erpCallbackUrl}/api/modulos/whatsapp/comando com payload ComandoCallbackDTO
  - ROU-03  # Resilience4j circuit breaker (10/50%/60s) + retry exponencial (3 tentativas, 1s/2s/4s)
  - ROU-04  # Timeout 5s + log error sem trava do webhook (ja respondeu 200)
tags:
  - api-whatsapp
  - resilience4j
  - circuit-breaker
  - retry
  - rest-client
  - wiremock
  - aop-validation

must_haves:
  truths:
    - "ErpCallbackClient construido com RestClient.builder().baseUrl(properties.erpCallbackUrl) + requestFactory(SimpleClientHttpRequestFactory) com connect/read timeout = properties.callbackTimeout (Phase 1, default 5s)"
    - "Method despachar(ComandoCallbackDTO payload) anotado @CircuitBreaker(name='erp-callback', fallbackMethod='fallbackDespachar') @Retry(name='erp-callback')"
    - "despachar chama restClient.post().uri('/api/modulos/whatsapp/comando').contentType(MediaType.APPLICATION_JSON).body(payload).retrieve().toBodilessEntity() — sem return value"
    - "fallbackDespachar(ComandoCallbackDTO payload, Throwable t) — signature OBRIGATORIA com Throwable como ultimo parametro (Resilience4j convention)"
    - "fallbackDespachar APENAS log.error estruturado (telefone+comando+t.getMessage()) — SEM rethrow, SEM retry adicional (D-08, ROU-03)"
    - "Annotations Resilience4j RESOLVEM via Spring AOP — Wave 1 garantiu spring-boot-starter-aop no classpath (Risk A6 mitigado)"
    - "Test counter_happy_path: WireMock recebe 1 POST quando ERP retorna 200 — sem retry"
    - "Test counter_5xx_recupera: 1ª e 2ª retornam 500, 3ª retorna 200 — counter == 3 (PROVA que retry funciona — Risk A6 detector key)"
    - "Test counter_5xx_persistente: WireMock retorna 500 em todas — counter == 3 (max-attempts) + fallback chamado + log.error captured (NAO HttpServerErrorException propagado)"
    - "Test counter_4xx_no_retry: WireMock retorna 400 — counter == 1 (4xx NAO retenta per yml retry-exceptions) + fallback chamado"
    - "Test counter_timeout: WireMock com fixedDelay > callbackTimeout — retry triggers; eventualmente fallback"
    - "Test circuit_open: 4 dispatches consecutivos com 500 (3 retries cada = 12 calls > slidingWindowSize 10 com 50% threshold) — circuit abre; 5o dispatch dispara fallback IMEDIATAMENTE (CallNotPermittedException)"
    - "@BeforeEach em ErpCallbackClientTest faz circuitBreakerRegistry.find('erp-callback').ifPresent(CircuitBreaker::reset) — A3 mitigado (shared state)"
    - "@DynamicPropertySource sobrescreve app.modulos.whatsapp.erp-callback-url com WireMock URL"
    - "Build verde sem regressao em Phase 1+2 + Wave 1-3"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ErpCallbackClient.java"
      provides: "Cliente HTTP com circuit breaker + retry para callback ERP"
      contains: "@CircuitBreaker(name = \"erp-callback\""
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ErpCallbackClientTest.java"
      provides: "6+ tests WireMock validando Resilience4j AOP-driven (Risk A6 detector + A3 reset)"
      contains: "CircuitBreaker::reset"
  key_links:
    - from: "ErpCallbackClient.despachar"
      to: "Resilience4j config 'erp-callback' (Wave 1 application.yml)"
      via: "@CircuitBreaker(name=\"erp-callback\") + @Retry(name=\"erp-callback\") resolvem instances por nome"
      pattern: "name = \"erp-callback\""
    - from: "ErpCallbackClient (Wave 4)"
      to: "MensagemAsyncListener (Wave 5)"
      via: "constructor injection — listener chama erpCallbackClient.despachar(payload) como ultima acao"
      pattern: "erpCallbackClient\\.despachar"
    - from: "ErpCallbackClient.despachar"
      to: "ComandoCallbackDTO (Wave 2)"
      via: "RestClient.post().body(payload)"
      pattern: "body\\(payload\\)"
    - from: "ErpCallbackClient.fallbackDespachar"
      to: "log.error estruturado"
      via: "log.error(...telefone, comando, t.getMessage()) — SEM exception completa em arg final (PITFALL 5)"
      pattern: "log\\.error"
---

<objective>
Wave 4 (depende de Wave 3 — apenas para padrao WireMock; logicamente paralela a Wave 3): implementar `ErpCallbackClient` com `@CircuitBreaker` + `@Retry` annotations Resilience4j per D-03 + ROU-02 + ROU-03 + ROU-04. Espelha o padrao de `lib-consultas-client/ConsultasClientImpl.java` (parametros 10/50%/60s + 3x backoff 1s/2s/4s) — diferenca: aqui annotations declarativas (Spring Boot starter), la programatico.

A wave tem **2 riscos HIGH** que precisam de validacao empirica:
- **Risk A6**: sem `spring-boot-starter-aop`, annotations viram no-op SILENCIOSO. Wave 1 ja adicionou o starter; este plan tem **teste critico** que detecta esse bug — counter == 3 quando WireMock retorna 5xx, 5xx, 200. Sem AOP, counter == 1.
- **Risk A3**: circuit breaker e bean Singleton com state que persiste entre tests. `@BeforeEach reset` evita poluicao cross-test.

Purpose:
- ROU-02: payload `{telefone, comando, payload, idCliente, mediaBase64?, mediaMimeType?, mediaFilename?}` via JSON ao endpoint `/api/modulos/whatsapp/comando`.
- ROU-03: parametros exatos da config Wave 1 application.yml (10-call window, 50% failure threshold, 60s open, 3 attempts, exponencial 1s/2.0x; retry apenas em 5xx/SocketTimeoutException/IOException — 4xx NAO retenta).
- ROU-04: timeout do request (Phase 1 callbackTimeout default 5s) — em fallback NAO trava webhook (que ja respondeu 200 ao Meta), apenas log.error.
- D-08: fallback method NAO retenta — protege ERP de duplicate side effect (ERP pode ter executado parcialmente).

Output:
- `ErpCallbackClient.java` novo — service com 2 annotations + fallbackMethod
- `ErpCallbackClientTest.java` novo — 6+ tests cobrindo retry/CB/fallback/timeout/circuit-open
- `mvnw verify` BUILD SUCCESS
- 1 commit feat atomico + 1 commit docs SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-roteamento-boundary-async/03-CONTEXT.md
@.planning/phases/03-roteamento-boundary-async/03-RESEARCH.md
@.planning/phases/03-roteamento-boundary-async/03-01-SUMMARY.md
@.planning/phases/03-roteamento-boundary-async/03-03-SUMMARY.md
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ComandoCallbackDTO.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java
@lib-consultas-client/src/main/java/br/com/erpkit/consultas/client/ConsultasClientImpl.java

<interfaces>
<!-- Codigo-alvo extraido do RESEARCH §"Pattern 2: Resilience4j Annotations" + §"Pitfall 4 + 6". -->

ErpCallbackClient.java (NEW):
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.config.WhatsAppProperties;
import br.com.erpkit.whatsapp.dto.ComandoCallbackDTO;
import io.github.resilience4j.circuitbreaker.annotation.CircuitBreaker;
import io.github.resilience4j.retry.annotation.Retry;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.MediaType;
import org.springframework.http.client.SimpleClientHttpRequestFactory;
import org.springframework.stereotype.Service;
import org.springframework.web.client.RestClient;

import java.time.Duration;

/**
 * Cliente HTTP para callback ao ERP (D-03 + ROU-02 + ROU-03 + ROU-04).
 *
 * <p>Pattern espelha {@code lib-consultas-client/ConsultasClientImpl} (programatico la,
 * declarativo aqui). Parametros via {@code application.yml} bloco
 * {@code resilience4j.{circuitbreaker,retry}.instances.erp-callback}:
 * <ul>
 *   <li>CircuitBreaker: 10-call window, 50% threshold, 60s open, 3 in half-open.</li>
 *   <li>Retry: 3 tentativas, backoff exponencial 1s/2.0x = 1s/2s/4s.</li>
 *   <li>Retry-exceptions: HttpServerErrorException + SocketTimeoutException + IOException.
 *       4xx categoricos NAO retentam (D-08).</li>
 * </ul>
 *
 * <p><b>Fallback (D-08, ROU-03):</b> quando retries esgotam ou circuit abre, log.error
 * estruturado e fim — SEM rethrow, SEM retry adicional. ERP pode ter executado
 * parcialmente (handler ja marcou orcamento como "visualizado") — retry duplicaria
 * side effect.
 */
@Service
public class ErpCallbackClient {

    private static final Logger log = LoggerFactory.getLogger(ErpCallbackClient.class);

    private final RestClient restClient;

    public ErpCallbackClient(WhatsAppProperties properties) {
        SimpleClientHttpRequestFactory factory = new SimpleClientHttpRequestFactory();
        Duration timeout = properties.getCallbackTimeout();
        factory.setConnectTimeout((int) timeout.toMillis());
        factory.setReadTimeout((int) timeout.toMillis());
        this.restClient = RestClient.builder()
                .baseUrl(properties.getErpCallbackUrl())
                .requestFactory(factory)
                .build();
    }

    @CircuitBreaker(name = "erp-callback", fallbackMethod = "fallbackDespachar")
    @Retry(name = "erp-callback")
    public void despachar(ComandoCallbackDTO payload) {
        log.debug("Dispatch ERP: telefone={} comando={}", payload.telefone(), payload.comando());
        restClient.post()
                .uri("/api/modulos/whatsapp/comando")
                .contentType(MediaType.APPLICATION_JSON)
                .body(payload)
                .retrieve()
                .toBodilessEntity();
        log.info("ERP callback ok: telefone={} comando={}", payload.telefone(), payload.comando());
    }

    /**
     * Fallback chamado quando Resilience4j esgota retries OU circuit abre.
     * <ul>
     *   <li>Signature OBRIGATORIA: mesmos params da method original + Throwable como ULTIMO parametro.</li>
     *   <li>SEM retry adicional, SEM rethrow — apenas log estruturado (D-08, ROU-03).</li>
     *   <li>log.error apenas com {@code t.getMessage()}, NAO {@code t} inteiro
     *       (PITFALLS C-09: HttpClientErrorException.toString pode incluir Bearer header).</li>
     * </ul>
     */
    @SuppressWarnings("unused") // referenciado por fallbackMethod = "fallbackDespachar"
    private void fallbackDespachar(ComandoCallbackDTO payload, Throwable t) {
        log.error("ERP callback falhou apos retry+CB: telefone={} comando={}: {}",
                  payload.telefone(), payload.comando(), t.getMessage());
        // ack-first: ERP pode ter executado parcialmente; nao retentar.
    }
}
```

ErpCallbackClientTest.java (NEW) — esqueleto:
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.WhatsAppApplication;
import br.com.erpkit.whatsapp.dto.ComandoCallbackDTO;
import com.github.tomakehurst.wiremock.WireMockServer;
import com.github.tomakehurst.wiremock.core.WireMockConfiguration;
import io.github.resilience4j.circuitbreaker.CircuitBreaker;
import io.github.resilience4j.circuitbreaker.CircuitBreakerRegistry;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;

import java.util.UUID;

import static com.github.tomakehurst.wiremock.client.WireMock.*;
import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(classes = WhatsAppApplication.class)
@ActiveProfiles("test")
class ErpCallbackClientTest {

    private static WireMockServer wireMock;

    @BeforeAll
    static void startWireMock() {
        wireMock = new WireMockServer(WireMockConfiguration.options().dynamicPort());
        wireMock.start();
    }

    @AfterAll
    static void stopWireMock() {
        wireMock.stop();
    }

    @DynamicPropertySource
    static void overrideErpUrl(DynamicPropertyRegistry registry) {
        registry.add("app.modulos.whatsapp.erp-callback-url", () -> wireMock.baseUrl());
    }

    @Autowired ErpCallbackClient client;
    @Autowired CircuitBreakerRegistry cbRegistry;

    private ComandoCallbackDTO payload() {
        // unique payload por test para evitar dedup acidental por scenarioState do WireMock
        return new ComandoCallbackDTO("554784178525", "orcamento", "orcamento " + UUID.randomUUID(),
            42L, null, null, null);
    }

    @BeforeEach
    void resetEverything() {
        wireMock.resetAll();
        // Risk A3: reset circuit breaker entre tests para evitar state pollution
        cbRegistry.find("erp-callback").ifPresent(CircuitBreaker::reset);
    }

    @Test
    @DisplayName("Happy path: WireMock 200 — counter == 1, sem retry, sem fallback")
    void happy_path_counter_1() {
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .willReturn(aResponse().withStatus(200)));

        client.despachar(payload());

        wireMock.verify(1, postRequestedFor(urlEqualTo("/api/modulos/whatsapp/comando")));
    }

    @Test
    @DisplayName("5xx recupera: 500, 500, 200 — counter == 3 (PROVA Risk A6: AOP funcionando)")
    void cinquecentos_recupera_counter_3() {
        // Scenario state: 1ª request -> 500, 2ª -> 500, 3ª -> 200
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .inScenario("retry-recover").whenScenarioStateIs("Started")
            .willReturn(aResponse().withStatus(500))
            .willSetStateTo("retry-1"));
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .inScenario("retry-recover").whenScenarioStateIs("retry-1")
            .willReturn(aResponse().withStatus(500))
            .willSetStateTo("retry-2"));
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .inScenario("retry-recover").whenScenarioStateIs("retry-2")
            .willReturn(aResponse().withStatus(200)));

        client.despachar(payload());

        // CRITICO: counter == 3 PROVA que @Retry esta ativo via AOP. Sem
        // spring-boot-starter-aop (Risk A6), annotation seria no-op silencioso e
        // counter == 1 (sem retry).
        wireMock.verify(3, postRequestedFor(urlEqualTo("/api/modulos/whatsapp/comando")));
    }

    @Test
    @DisplayName("5xx persistente: counter == 3 (max-attempts) + fallback NAO propaga excecao")
    void cinquecentos_persistente_fallback_log() {
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .willReturn(aResponse().withStatus(500)));

        // Fallback NAO propaga — call retorna void normalmente
        client.despachar(payload());

        // counter == 3 (max-attempts da config)
        wireMock.verify(3, postRequestedFor(urlEqualTo("/api/modulos/whatsapp/comando")));
    }

    @Test
    @DisplayName("4xx (400): counter == 1 (NAO retenta per yml retry-exceptions) + fallback chamado")
    void quatrocentos_no_retry_counter_1() {
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .willReturn(aResponse().withStatus(400)));

        client.despachar(payload()); // fallback engole — sem rethrow

        wireMock.verify(1, postRequestedFor(urlEqualTo("/api/modulos/whatsapp/comando")));
    }

    @Test
    @DisplayName("Timeout: delay > callbackTimeout — retry, eventualmente fallback")
    void timeout_retry_e_fallback() {
        // application-test.yml read-timeout = 500ms — delay 1500ms forca timeout
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .willReturn(aResponse().withStatus(200).withFixedDelay(1500)));

        client.despachar(payload());

        // Counter pode ser 3 (todos timeouts retentaram) ou >1 dependendo do scheduling.
        // Garante que houve retry (>1) — exact == 3 e ideal mas pode ser flaky.
        int count = wireMock.findAll(postRequestedFor(urlEqualTo("/api/modulos/whatsapp/comando"))).size();
        assertThat(count).as("Timeout deve ter retentado ao menos 1 vez").isGreaterThan(1);
    }

    @Test
    @DisplayName("Circuit aberto apos 4 dispatches em 5xx (3 retries cada = 12 calls > 50% de 10) — fallback IMEDIATO")
    void circuit_open_apos_falhas_repetidas() {
        wireMock.stubFor(post(urlEqualTo("/api/modulos/whatsapp/comando"))
            .willReturn(aResponse().withStatus(500)));

        // 4 dispatches consecutivos esgotam sliding window e abrem circuit
        for (int i = 0; i < 4; i++) {
            client.despachar(payload());
        }

        // Verificar estado do circuit
        var cb = cbRegistry.find("erp-callback").orElseThrow();
        assertThat(cb.getState())
            .as("Apos 4 dispatches falhos (12 calls com retry), circuit deveria estar OPEN")
            .isIn(CircuitBreaker.State.OPEN, CircuitBreaker.State.HALF_OPEN);

        // 5o dispatch: fallback IMEDIATO (CallNotPermittedException) — counter NAO incrementa
        long countBefore = wireMock.findAll(postRequestedFor(urlEqualTo("/api/modulos/whatsapp/comando"))).size();
        client.despachar(payload());
        long countAfter = wireMock.findAll(postRequestedFor(urlEqualTo("/api/modulos/whatsapp/comando"))).size();
        assertThat(countAfter)
            .as("Circuit aberto: dispatch nao chega ao WireMock (CallNotPermittedException -> fallback)")
            .isEqualTo(countBefore);
    }
}
```

Imports criticos: `io.github.resilience4j.circuitbreaker.annotation.CircuitBreaker`, `io.github.resilience4j.retry.annotation.Retry`, `io.github.resilience4j.circuitbreaker.CircuitBreakerRegistry`, `io.github.resilience4j.circuitbreaker.CircuitBreaker` (a classe, nao annotation, no test).

Nota sobre timeout config: o `application-test.yml` da Wave 1 setou `spring.http.client.read-timeout: 500ms` — globalmente. Mas o `ErpCallbackClient` define seu proprio `requestFactory` com `properties.callbackTimeout` (default 5s). Para o test `timeout_retry_e_fallback` funcionar, o `callbackTimeout` em test profile precisa ser <1500ms. Confirmar `application-test.yml` linha 73: `callbackTimeout: 5s` — TROCAR para `callbackTimeout: 500ms` em Wave 1 OU no test sobrescrever via `@DynamicPropertySource`. Recomendacao: aproveitar Task 2 deste plan para AJUSTAR `application-test.yml` callbackTimeout para `500ms` (sem afetar tests de Phase 1+2 que nao chamam callback).

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar ErpCallbackClient.java + ajustar callbackTimeout em application-test.yml</name>
  <files>
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ErpCallbackClient.java,
    api-whatsapp/src/test/resources/application-test.yml
  </files>
  <action>
    1. Criar `ErpCallbackClient.java` EXATAMENTE como o snippet em <interfaces>:
       - `@Service` na classe.
       - Constructor recebe `WhatsAppProperties`. Constroi `SimpleClientHttpRequestFactory` com connect/read timeout = `properties.getCallbackTimeout().toMillis()` (cast int).
       - `RestClient.builder().baseUrl(properties.getErpCallbackUrl()).requestFactory(factory).build()`.
       - Method `despachar(ComandoCallbackDTO payload)` anotado `@CircuitBreaker(name = "erp-callback", fallbackMethod = "fallbackDespachar")` + `@Retry(name = "erp-callback")`.
       - Body: `restClient.post().uri("/api/modulos/whatsapp/comando").contentType(MediaType.APPLICATION_JSON).body(payload).retrieve().toBodilessEntity();`.
       - log.debug antes do POST + log.info apos sucesso (ambos com telefone+comando — sem mediaBase64 nos logs).
       - Method privado `fallbackDespachar(ComandoCallbackDTO payload, Throwable t)` — assinatura OBRIGATORIA com Throwable como ultimo arg. Apenas `log.error("ERP callback falhou apos retry+CB: telefone={} comando={}: {}", payload.telefone(), payload.comando(), t.getMessage());` — SEM rethrow, SEM retry, NAO logar `t` inteiro (PITFALL 5).

    2. Editar `application-test.yml` linha 73 — TROCAR `callbackTimeout: 5s` por `callbackTimeout: 500ms`. Justificativa: necessario para o test `timeout_retry_e_fallback` (delay 1500ms > timeout 500ms). Phase 1+2 tests nao chamam callback ERP — sem regressao esperada.

    Convencoes: imports na ordem (io.github... → java... → org.springframework... → br.com.erpkit). 4-space indent. PT-BR no Javadoc.
  </action>
  <verify>
    <automated>./mvnw compile -pl api-whatsapp -q 2>&amp;1 | tail -10</automated>
    Esperado: BUILD SUCCESS sem erros de import. As annotations `@CircuitBreaker` + `@Retry` existem no classpath via `resilience4j-spring-boot3` (Wave 1).
  </verify>
  <done>
    ErpCallbackClient.java compila. application-test.yml tem callbackTimeout=500ms. Phase 1+2 tests rodam normais (callback nao acionado fora deste plan).
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar ErpCallbackClientTest.java com 6 tests cobrindo retry/fallback/timeout/circuit-open + reset @BeforeEach</name>
  <files>
    api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ErpCallbackClientTest.java
  </files>
  <action>
    Criar `ErpCallbackClientTest.java` EXATAMENTE como o snippet em <interfaces>:

    Setup:
    - `@SpringBootTest(classes = WhatsAppApplication.class) @ActiveProfiles("test")`.
    - `WireMockServer wireMock` static, dynamicPort, start/stop @BeforeAll/@AfterAll.
    - `@DynamicPropertySource` sobrescreve `app.modulos.whatsapp.erp-callback-url` com `wireMock.baseUrl()`.
    - `@Autowired ErpCallbackClient client; @Autowired CircuitBreakerRegistry cbRegistry;`.
    - `@BeforeEach`: `wireMock.resetAll(); cbRegistry.find("erp-callback").ifPresent(CircuitBreaker::reset);` — Risk A3 mitigado.
    - Helper `payload()` retorna `ComandoCallbackDTO` com payload UUID-randomico (evita dedup acidental).

    6 tests minimo:
    1. `happy_path_counter_1` — WireMock 200; assert `verify(1, postRequestedFor(...))`.
    2. **`cinquecentos_recupera_counter_3`** — Scenario state WireMock: 500 → 500 → 200; assert `verify(3, ...)`. Este test e o **DETECTOR Risk A6**: sem AOP, counter seria 1.
    3. `cinquecentos_persistente_fallback_log` — WireMock 500 sempre; chama `client.despachar(payload())` que NAO propaga (fallback engole); assert `verify(3, ...)`.
    4. `quatrocentos_no_retry_counter_1` — WireMock 400; assert `verify(1, ...)` — 4xx NAO retenta per yml retry-exceptions.
    5. `timeout_retry_e_fallback` — WireMock 200 com `withFixedDelay(1500)` (callbackTimeout=500ms); chamada nao propaga; assert counter > 1 (retry triggered).
    6. `circuit_open_apos_falhas_repetidas` — WireMock 500 sempre; 4 dispatches consecutivos; assert `cb.getState() in (OPEN, HALF_OPEN)`; 5o dispatch NAO incrementa counter no WireMock (CallNotPermittedException → fallback imediato).

    Imports: `com.github.tomakehurst.wiremock.WireMockServer`, static `WireMock.*`, `io.github.resilience4j.circuitbreaker.{CircuitBreaker, CircuitBreakerRegistry}`.
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=ErpCallbackClientTest -q 2>&amp;1 | tail -15</automated>
    Esperado: `Tests run: 6, Failures: 0, Errors: 0`. Especialmente `cinquecentos_recupera_counter_3` deve passar — provando que AOP esta funcionando.
  </verify>
  <done>
    6 tests verdes. Counter assertions confirmam: 200 → 1, 5xx → 3, 4xx → 1, timeout → >1, circuit aberto → fallback imediato sem nova request. @BeforeEach reset garante isolamento.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verde do reator + commit feat + commit docs SUMMARY</name>
  <files>
    .planning/phases/03-roteamento-boundary-async/03-04-SUMMARY.md
  </files>
  <action>
    1. Rodar `./mvnw verify -pl api-whatsapp -am` — esperar BUILD SUCCESS. Total deve ser 126+ + 6 = 132+ tests verdes.

    2. Criar `.planning/phases/03-roteamento-boundary-async/03-04-SUMMARY.md` documentando: ErpCallbackClient implementado, AOP confirmado via counter == 3 em retry, circuit-open verificado empiricamente. Documentar Risk A6 e A3 mitigados.

    3. Commits:
       ```bash
       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): ErpCallbackClient com @CircuitBreaker + @Retry Resilience4j (Phase 3 Wave 4)" --files \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ErpCallbackClient.java \
         api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ErpCallbackClientTest.java \
         api-whatsapp/src/test/resources/application-test.yml

       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(03-04): SUMMARY plan 04 + atualizar STATE/ROADMAP" --files \
         .planning/phases/03-roteamento-boundary-async/03-04-SUMMARY.md \
         .planning/STATE.md \
         .planning/ROADMAP.md
       ```
  </action>
  <verify>
    <automated>./mvnw verify -pl api-whatsapp -am -q 2>&amp;1 | tail -10</automated>
    Esperado: BUILD SUCCESS + 132+ tests verdes.
  </verify>
  <done>
    Build verde, 6 tests novos validando retry/CB/fallback/timeout/circuit-open, 2 commits.
  </done>
</task>

</tasks>

<verification>
- ROU-02 satisfeito: POST {erpCallbackUrl}/api/modulos/whatsapp/comando com body ComandoCallbackDTO via JSON.
- ROU-03 satisfeito: parametros 10/50%/60s + 3x backoff exponencial — counter assertions confirmam empiricamente.
- ROU-04 satisfeito: timeout via SimpleClientHttpRequestFactory; fallback NAO propaga — webhook (que ja respondeu 200) nao trava.
- D-08 satisfeito: fallbackDespachar SEM rethrow SEM retry adicional — apenas log.error.
- Risk A6 mitigado e VALIDADO: test `cinquecentos_recupera_counter_3` passa (counter == 3) — prova que AOP esta resolvendo annotations.
- Risk A3 mitigado: @BeforeEach reset garante isolamento.
- Sem regressao em Phase 1+2 + Waves 1-3.
</verification>

<success_criteria>
- 6 tests cobrem TODOS os cenarios de Resilience4j: happy / 5xx-recupera / 5xx-persistente / 4xx-no-retry / timeout / circuit-open.
- Counter == 3 em 5xx-recupera prova AOP ativo (Risk A6).
- Counter == 1 em 4xx prova retry-exceptions config respeitada (D-08).
- Circuit state OPEN apos 4 dispatches falhos prova sliding window funcionando.
- mvnw verify BUILD SUCCESS, 132+ tests verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-roteamento-boundary-async/03-04-SUMMARY.md`
</output>
