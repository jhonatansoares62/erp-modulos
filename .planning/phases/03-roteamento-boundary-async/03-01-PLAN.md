---
phase: 03-roteamento-boundary-async
plan: 01
type: execute
wave: 1
depends_on: []
parallel_with: []
files_modified:
  - pom.xml  # MODIFY: adicionar resilience4j-spring-boot3 ao dependencyManagement
  - api-whatsapp/pom.xml  # MODIFY: adicionar resilience4j-spring-boot3 + spring-boot-starter-aop + wiremock-standalone (test)
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/AsyncConfig.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java  # MODIFY: adicionar metaApiBaseUrl
  - api-whatsapp/src/main/resources/application.yml  # MODIFY: bloco resilience4j + spring.http.client + metaApiBaseUrl default
  - api-whatsapp/src/test/resources/application-test.yml  # MODIFY: timeouts curtos + metaApiBaseUrl override
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/config/AsyncConfigSmokeTest.java  # NEW
autonomous: true
requirements:
  - ROU-03  # Resilience4j circuit breaker + retry config (10/50%/60s + 3 tentativas, 1s/2s/4s)
  - ROU-04  # Callback timeout 5s configuravel — defesa em profundidade via spring.http.client + WhatsAppProperties.callbackTimeout (Phase 1)
tags:
  - api-whatsapp
  - infra
  - resilience4j
  - aop
  - async-config
  - wiremock-setup

must_haves:
  truths:
    - "Parent pom.xml dependencyManagement declara resilience4j-spring-boot3 versao 2.2.0 (via ${resilience4j.version})"
    - "api-whatsapp/pom.xml inclui resilience4j-spring-boot3 (compile) + spring-boot-starter-aop (compile) + wiremock-standalone 3.10.0 (test)"
    - "spring-boot-starter-aop CRITICO: sem ele @CircuitBreaker/@Retry viram no-op silencioso (Risk A6) — Wave 4 valida via counter assertion"
    - "AsyncConfig anotado @Configuration @EnableAsync com bean whatsappTaskExecutor (ThreadPoolTaskExecutor: corePool=2, maxPool=10, queueCapacity=100, threadNamePrefix=whatsapp-async-, RejectedExecutionHandler=CallerRunsPolicy)"
    - "application.yml expoe bloco resilience4j.circuitbreaker.instances.erp-callback (slidingWindowSize=10, failureRateThreshold=50, waitDurationInOpenState=60s, permittedNumberOfCallsInHalfOpenState=3) per D-03"
    - "application.yml expoe bloco resilience4j.retry.instances.erp-callback (maxAttempts=3, waitDuration=1s, enableExponentialBackoff=true, exponentialBackoffMultiplier=2.0, retryExceptions=HttpServerErrorException/SocketTimeoutException/IOException) per D-03"
    - "application.yml NUNCA inclui HttpClientErrorException em retry-exceptions — 4xx categoricos NAO retentam (D-08, ROU-03)"
    - "application.yml expoe bloco spring.http.client (connect-timeout=5s, read-timeout=10s) — defesa em profundidade para MetaMediaClient (sem Resilience4j proprio)"
    - "WhatsAppProperties ganha campo metaApiBaseUrl (String, default https://graph.facebook.com/v22.0, sem @NotBlank) com getter/setter — permite override em test via @DynamicPropertySource"
    - "application-test.yml override metaApiBaseUrl + spring.http.client com valores curtos (connect-timeout=200ms, read-timeout=500ms) para tests rapidos"
    - "AsyncConfigSmokeTest @SpringBootTest verifica: bean whatsappTaskExecutor presente + e instancia de ThreadPoolTaskExecutor + corePool/maxPool/queue/threadNamePrefix corretos"
    - "mvnw verify -pl api-whatsapp -am BUILD SUCCESS sem regressao em 112 tests da Phase 1+2"
  artifacts:
    - path: "pom.xml"
      provides: "Versao gerenciada de resilience4j-spring-boot3"
      contains: "resilience4j-spring-boot3"
    - path: "api-whatsapp/pom.xml"
      provides: "Dependencias Resilience4j + AOP + WireMock test"
      contains: "spring-boot-starter-aop"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/AsyncConfig.java"
      provides: "Bean whatsappTaskExecutor + @EnableAsync"
      contains: "whatsappTaskExecutor"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java"
      provides: "Campo metaApiBaseUrl para override em tests Wave 3"
      contains: "metaApiBaseUrl"
    - path: "api-whatsapp/src/main/resources/application.yml"
      provides: "Config Resilience4j circuit breaker + retry + spring.http.client"
      contains: "erp-callback"
    - path: "api-whatsapp/src/test/resources/application-test.yml"
      provides: "Override timeouts curtos + metaApiBaseUrl placeholder"
      contains: "metaApiBaseUrl"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/config/AsyncConfigSmokeTest.java"
      provides: "Smoke test verificando bean whatsappTaskExecutor parametrizado"
      contains: "whatsappTaskExecutor"
  key_links:
    - from: "api-whatsapp/pom.xml"
      to: "spring-boot-starter-aop runtime"
      via: "compile dependency adicionada"
      pattern: "spring-boot-starter-aop"
    - from: "AsyncConfig.whatsappTaskExecutor"
      to: "MensagemAsyncListener (Wave 5)"
      via: "@Async(\"whatsappTaskExecutor\") referencia bean por nome"
      pattern: "whatsappTaskExecutor"
    - from: "application.yml resilience4j.*.instances.erp-callback"
      to: "ErpCallbackClient @CircuitBreaker(name=\"erp-callback\") @Retry(name=\"erp-callback\") (Wave 4)"
      via: "Resilience4j Spring Boot starter resolve config por instance name"
      pattern: "erp-callback"
    - from: "WhatsAppProperties.metaApiBaseUrl"
      to: "MetaMediaClient (Wave 3)"
      via: "constructor injection — RestClient.builder().baseUrl(properties.getMetaApiBaseUrl())"
      pattern: "getMetaApiBaseUrl"
---

<objective>
Wave 1 (zero dependencias): preparar a infraestrutura completa para todas as outras waves da Phase 3 — Resilience4j + AOP no classpath, ThreadPoolTaskExecutor configurado, application.yml com blocos resilience4j + spring.http.client, WhatsAppProperties com campo `metaApiBaseUrl` (override em tests).

Sem mudanca funcional ainda — Phase 1+2 continuam intactos. O smoke test prova que o bean `whatsappTaskExecutor` arrancou com a configuracao de pool correta. Fundacao critica: sem `spring-boot-starter-aop`, as annotations Resilience4j da Wave 4 viram no-op silenciosos (Risk A6 do RESEARCH §"Pitfall 2"). Sem o bloco yaml `resilience4j.*.instances.erp-callback`, a annotation `@CircuitBreaker(name="erp-callback")` nao resolve config.

Purpose: D-02 (pool dedicado degrada graciosamente em pico via CallerRunsPolicy) + D-03 (Resilience4j config alinha com lib-consultas-client: 10/50%/60s + 3x backoff exponencial 1s/2s/4s) + ROU-04 (timeout 5s do callback ja existe em WhatsAppProperties.callbackTimeout desde Phase 1, somar `spring.http.client` global como defesa em profundidade para MetaMediaClient).

Output:
- `pom.xml` (parent) modificado — +1 entry no dependencyManagement
- `api-whatsapp/pom.xml` modificado — +3 dependencias
- `WhatsAppProperties.java` modificado — +1 campo `metaApiBaseUrl` (default valido, sem @NotBlank)
- `AsyncConfig.java` novo — bean dedicado
- `application.yml` modificado — +blocos resilience4j + spring.http.client
- `application-test.yml` modificado — overrides para tests rapidos
- `AsyncConfigSmokeTest.java` novo — verificacao do bean
- `mvnw verify -pl api-whatsapp -am` BUILD SUCCESS
- 1 commit feat atomico + 1 commit docs SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-roteamento-boundary-async/03-CONTEXT.md
@.planning/phases/03-roteamento-boundary-async/03-RESEARCH.md
@.planning/phases/02-persistencia-idempotencia/02-VERIFICATION.md
@pom.xml
@api-whatsapp/pom.xml
@api-whatsapp/src/main/resources/application.yml
@api-whatsapp/src/test/resources/application-test.yml
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java
@lib-consultas-client/pom.xml

<interfaces>
<!-- Codigo-alvo (extraido do RESEARCH §"Code Examples 1" + §"Code Example 6" + §"Code Example 8"). -->
<!-- Implementar EXATAMENTE estes shapes. -->

AsyncConfig.java (NEW):
```java
package br.com.erpkit.whatsapp.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.annotation.EnableAsync;
import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;

import java.util.concurrent.ThreadPoolExecutor;

/**
 * Pool dedicado para o {@code MensagemAsyncListener} (D-02 do CONTEXT).
 *
 * <p>Pool dedicado em vez de SimpleAsyncTaskExecutor: SimpleAsync cria thread por task —
 * em pico de mensagens, OOM. Pool fixo com queueCapacity 100 + CallerRunsPolicy degrada
 * graciosamente: sob estresse extremo, listener roda inline na thread chamadora.
 *
 * <p><b>Test profile (Wave 6):</b> sobrescreve {@code whatsappTaskExecutor} com
 * SyncTaskExecutor via {@code @TestConfiguration} para tests E2E manterem assertions
 * DB sincronas sem Awaitility.
 */
@Configuration
@EnableAsync
public class AsyncConfig {

    @Bean(name = "whatsappTaskExecutor")
    public ThreadPoolTaskExecutor whatsappTaskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(2);
        executor.setMaxPoolSize(10);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("whatsapp-async-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.initialize();
        return executor;
    }
}
```

WhatsAppProperties.java — adicionar campo (entre callbackTimeout e toString):
```java
/** Base URL da Graph API do Meta (override em tests). Default v22.0. */
private String metaApiBaseUrl = "https://graph.facebook.com/v22.0";

public String getMetaApiBaseUrl() { return metaApiBaseUrl; }
public void setMetaApiBaseUrl(String metaApiBaseUrl) { this.metaApiBaseUrl = metaApiBaseUrl; }
```

application.yml — ADICIONAR ao final (apos bloco management):
```yaml
# Resilience4j: circuit breaker + retry para ErpCallbackClient (D-03 + ROU-03 + ROU-04).
#
# Aspect order (Resilience4j Spring Boot starter): Retry POR FORA de CircuitBreaker.
# Significa: 1 dispatch falho = 3 retries (com backoff 1s/2s/4s) = 3 calls counted
# pelo CB sliding-window. 4 dispatches falhos consecutivos = 12 calls = circuit aberto.
#
# retry-exceptions: APENAS transient errors. NAO 4xx categoricos (400/401/403/404 etc.)
# — esses indicam bug de configuracao no ERP, nao falha temporaria; retry duplicaria
# side effect (PITFALLS C-05 / ROU-03 D-08).
resilience4j:
  circuitbreaker:
    instances:
      erp-callback:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 60s
        permitted-number-of-calls-in-half-open-state: 3
        register-health-indicator: false  # Phase 6 pode habilitar via actuator
  retry:
    instances:
      erp-callback:
        max-attempts: 3
        wait-duration: 1s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2.0
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - java.net.SocketTimeoutException
          - java.io.IOException
        # 4xx categoricos NAO em retry-exceptions — Resilience4j por default NAO retenta
        # excecoes nao listadas. HttpClientErrorException (4xx) NAO retenta.

# Spring HTTP client global timeout — defesa em profundidade para MetaMediaClient
# (sem Resilience4j proprio). RestClient pode ainda override per-instance.
spring:
  http:
    client:
      connect-timeout: 5s
      read-timeout: 10s

# Default override-able em tests via @DynamicPropertySource (Wave 3 MetaMediaClientTest).
app:
  modulos:
    whatsapp:
      metaApiBaseUrl: ${WHATSAPP_META_API_BASE_URL:https://graph.facebook.com/v22.0}
```

application-test.yml — ADICIONAR no bloco app.modulos.whatsapp e antes de modulo:
```yaml
      # Override para tests — placeholder substituido por WireMock URL via @DynamicPropertySource em Wave 3.
      metaApiBaseUrl: http://localhost:0/test

# Timeouts globais curtos para tests rapidos
spring:
  http:
    client:
      connect-timeout: 200ms
      read-timeout: 500ms

# Resilience4j com janelas curtas para tests (em Wave 4 alguns tests podem precisar override per-class).
resilience4j:
  circuitbreaker:
    instances:
      erp-callback:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 1s
        permitted-number-of-calls-in-half-open-state: 3
  retry:
    instances:
      erp-callback:
        max-attempts: 3
        wait-duration: 50ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2.0
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - java.net.SocketTimeoutException
          - java.io.IOException
```

pom.xml (parent) — ADICIONAR em <dependencyManagement>/<dependencies>:
```xml
<dependency>
    <groupId>io.github.resilience4j</groupId>
    <artifactId>resilience4j-spring-boot3</artifactId>
    <version>${resilience4j.version}</version>
</dependency>
```

api-whatsapp/pom.xml — ADICIONAR em <dependencies>:
```xml
<!-- Resilience4j Spring Boot 3 starter (D-03) -->
<dependency>
    <groupId>io.github.resilience4j</groupId>
    <artifactId>resilience4j-spring-boot3</artifactId>
</dependency>

<!-- AOP runtime para @CircuitBreaker + @Retry annotations (Risk A6 do RESEARCH) -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId>
</dependency>

<!-- WireMock standalone para integration tests Waves 3+4+6 -->
<dependency>
    <groupId>org.wiremock</groupId>
    <artifactId>wiremock-standalone</artifactId>
    <version>3.10.0</version>
    <scope>test</scope>
</dependency>
```

AsyncConfigSmokeTest.java (NEW):
```java
package br.com.erpkit.whatsapp.config;

import br.com.erpkit.whatsapp.WhatsAppApplication;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;
import org.springframework.test.context.ActiveProfiles;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(classes = WhatsAppApplication.class)
@ActiveProfiles("test")
class AsyncConfigSmokeTest {

    @Autowired
    @Qualifier("whatsappTaskExecutor")
    private ThreadPoolTaskExecutor executor;

    @Test
    @DisplayName("Bean whatsappTaskExecutor parametrizado com corePool=2 maxPool=10 queue=100")
    void bean_thread_pool_configurado() {
        assertThat(executor).isNotNull();
        assertThat(executor.getCorePoolSize()).isEqualTo(2);
        assertThat(executor.getMaxPoolSize()).isEqualTo(10);
        assertThat(executor.getQueueCapacity()).isEqualTo(100);
        assertThat(executor.getThreadNamePrefix()).isEqualTo("whatsapp-async-");
    }
}
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar parent pom.xml + api-whatsapp/pom.xml com dependencias Resilience4j + AOP + WireMock</name>
  <files>
    pom.xml,
    api-whatsapp/pom.xml
  </files>
  <action>
    Editar `pom.xml` (parent) per D-03 + RESEARCH §"Code Example 8":
    1. No bloco `<dependencyManagement>/<dependencies>` (entre os 2 entries Resilience4j existentes resilience4j-circuitbreaker e resilience4j-retry), adicionar:
       ```xml
       <dependency>
           <groupId>io.github.resilience4j</groupId>
           <artifactId>resilience4j-spring-boot3</artifactId>
           <version>${resilience4j.version}</version>
       </dependency>
       ```
       A propriedade `${resilience4j.version}` ja existe (linha 33 = 2.2.0). Sem precisar declarar nova property.

    Editar `api-whatsapp/pom.xml` per D-03 + RESEARCH:
    1. No bloco `<dependencies>`, ANTES da seccao `<!-- Test -->`, adicionar:
       ```xml
       <!-- Resilience4j Spring Boot 3 starter (D-03 + ROU-03) -->
       <dependency>
           <groupId>io.github.resilience4j</groupId>
           <artifactId>resilience4j-spring-boot3</artifactId>
       </dependency>

       <!-- AOP runtime para @CircuitBreaker + @Retry annotations (Risk A6) -->
       <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-aop</artifactId>
       </dependency>
       ```
    2. No bloco `<!-- Test -->`, ao final, adicionar:
       ```xml
       <!-- WireMock standalone para integration tests Waves 3+4+6 -->
       <dependency>
           <groupId>org.wiremock</groupId>
           <artifactId>wiremock-standalone</artifactId>
           <version>3.10.0</version>
           <scope>test</scope>
       </dependency>
       ```
    Versao 3.10.0 escolhida per RESEARCH §"WireMock" (Jetty 12 standalone shadow evita conflito com Spring Boot 3.5).

    NAO mexer em outras dependencias. NAO mexer em outras `<properties>`. Sem alterar nem `<modules>` nem outros artefatos.

    `spring-boot-starter-aop` e o item CRITICO. Sem ele, Wave 4 vai aparentemente rodar mas as annotations Resilience4j NUNCA executam — Wave 4 tem teste explicito (counter == 3 em retry de 5xx) que detecta esse bug.
  </action>
  <verify>
    <automated>./mvnw -pl api-whatsapp -am dependency:resolve -q 2>&amp;1 | grep -E "resilience4j-spring-boot3|spring-boot-starter-aop|wiremock-standalone"</automated>
    Esperado: 3 linhas distintas mostrando os artefatos resolvidos. Confirmacao adicional: ./mvnw -pl api-whatsapp -am compile -q (BUILD SUCCESS) — sem erros de classpath.
  </verify>
  <done>
    Parent pom.xml dependencyManagement tem +1 entry resilience4j-spring-boot3. api-whatsapp/pom.xml tem +3 dependencias (2 compile + 1 test). mvnw compile verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar AsyncConfig.java + adicionar metaApiBaseUrl em WhatsAppProperties + atualizar application.yml + application-test.yml + smoke test</name>
  <files>
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/AsyncConfig.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java,
    api-whatsapp/src/main/resources/application.yml,
    api-whatsapp/src/test/resources/application-test.yml,
    api-whatsapp/src/test/java/br/com/erpkit/whatsapp/config/AsyncConfigSmokeTest.java
  </files>
  <action>
    1. Criar `AsyncConfig.java` em `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/` EXATAMENTE como o snippet em <interfaces>. `@Configuration` + `@EnableAsync` na classe. Bean `whatsappTaskExecutor` retornando `ThreadPoolTaskExecutor` configurado per D-02 (corePool=2, maxPool=10, queue=100, threadNamePrefix="whatsapp-async-", CallerRunsPolicy). NAO anotar `@EnableAsync` em `WhatsAppApplication.java` — manter centralizado em AsyncConfig per RESEARCH §"Example 2".

    2. Editar `WhatsAppProperties.java`: adicionar entre o campo `callbackTimeout` (linha 40) e o `getCallbackTimeout()` (linha 82) ANTES do `toString()` o campo + getter/setter `metaApiBaseUrl` per snippet em <interfaces>. Default `"https://graph.facebook.com/v22.0"`. SEM `@NotBlank` (default valido). Atualizar `toString()` para incluir `metaApiBaseUrl=` (nao mascarar, nao e secret).

    3. Editar `application.yml`: ADICIONAR ao FINAL do arquivo (apos bloco `management:`) o snippet em <interfaces>: blocos `resilience4j.circuitbreaker.instances.erp-callback`, `resilience4j.retry.instances.erp-callback`, `spring.http.client.{connect-timeout,read-timeout}`, e dentro do bloco `app.modulos.whatsapp` ja existente acrescentar a chave `metaApiBaseUrl: ${WHATSAPP_META_API_BASE_URL:https://graph.facebook.com/v22.0}`. Atencao: o bloco `app.modulos.whatsapp` ja existe na linha 79 do arquivo atual — INSERIR a nova chave apos `callbackTimeout`, NAO duplicar a estrutura.

    4. Editar `application-test.yml`: dentro do bloco `app.modulos.whatsapp` existente (linha 67), apos `callbackTimeout: 5s`, adicionar `metaApiBaseUrl: http://localhost:0/test`. Apos o bloco `modulo:`, adicionar os blocos `spring.http.client` (timeouts curtos 200ms/500ms) + `resilience4j` (mesma config de prod mas waitDuration menor). Snippet completo em <interfaces>.

    5. Criar `AsyncConfigSmokeTest.java` em `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/config/` EXATAMENTE como o snippet em <interfaces>. `@SpringBootTest(classes = WhatsAppApplication.class) @ActiveProfiles("test")`. Injeta `@Qualifier("whatsappTaskExecutor") ThreadPoolTaskExecutor`. Asserta corePool=2, maxPool=10, queueCapacity=100, threadNamePrefix="whatsapp-async-".

    Importante: Edicoes em yml devem preservar comentarios existentes — NAO sobrescrever blocos antigos, apenas APPEND.
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=AsyncConfigSmokeTest,WhatsAppPropertiesHappyPathTest,WhatsAppPropertiesValidationTest -q</automated>
    Esperado: AsyncConfigSmokeTest 1 test verde + WhatsAppPropertiesHappyPathTest 1 test verde + WhatsAppPropertiesValidationTest 6 tests verdes (sem regressao).
  </verify>
  <done>
    AsyncConfig.java + WhatsAppProperties.metaApiBaseUrl + application.yml com blocos resilience4j/spring.http.client + application-test.yml com overrides + AsyncConfigSmokeTest 1/1 verde + WhatsAppProperties tests 7/7 verdes (sem regressao no toString test).
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verde do reator + commit feat + commit docs SUMMARY</name>
  <files>
    .planning/phases/03-roteamento-boundary-async/03-01-SUMMARY.md
  </files>
  <action>
    1. Rodar `./mvnw verify -pl api-whatsapp -am` — esperar `BUILD SUCCESS`. Sem regressao em 112 tests da Phase 1+2 + 1 novo test smoke (113 total). Em caso de falha, NAO seguir; investigar e corrigir antes de commitar.

    2. Criar `.planning/phases/03-roteamento-boundary-async/03-01-SUMMARY.md` seguindo o padrao de `.planning/phases/02-persistencia-idempotencia/02-04-SUMMARY.md` (frontmatter com phase/plan/status/files_changed/build/tests; corpo com What/Why/Decisions/Risks/References). Documentar: deps adicionadas, AsyncConfig criado, blocos yaml, smoke test verde, build do reator verde.

    3. Commit feat (atomico) com `gsd-tools.cjs commit`:
       ```bash
       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): infra Resilience4j + AOP + AsyncConfig (Phase 3 Wave 1)" --files \
         pom.xml \
         api-whatsapp/pom.xml \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/AsyncConfig.java \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java \
         api-whatsapp/src/main/resources/application.yml \
         api-whatsapp/src/test/resources/application-test.yml \
         api-whatsapp/src/test/java/br/com/erpkit/whatsapp/config/AsyncConfigSmokeTest.java
       ```

    4. Commit docs (atomico):
       ```bash
       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(03-01): SUMMARY plan 01 + atualizar STATE/ROADMAP" --files \
         .planning/phases/03-roteamento-boundary-async/03-01-SUMMARY.md \
         .planning/STATE.md \
         .planning/ROADMAP.md
       ```

    Atualizar STATE.md adicionando registro de Plano 03-01 completo. Atualizar ROADMAP.md Phase 3 marcando `- [x] 03-01-PLAN.md ...` (apenas o item especifico, NAO marcar Phase 3 como completa).
  </action>
  <verify>
    <automated>./mvnw verify -pl api-whatsapp -am -q 2>&amp;1 | tail -20</automated>
    Esperado: `BUILD SUCCESS` + `Tests run: 113, Failures: 0, Errors: 0` (113 = 112 Phase 1+2 + 1 smoke).
  </verify>
  <done>
    BUILD SUCCESS, SUMMARY criado, 2 commits atomicos via gsd-tools.cjs, STATE.md + ROADMAP.md atualizados.
  </done>
</task>

</tasks>

<verification>
- `./mvnw verify -pl api-whatsapp -am` → BUILD SUCCESS, 113 tests verdes (zero regressao).
- Bean `whatsappTaskExecutor` carrega com parametros corretos (smoke test confirma).
- Properties `app.modulos.whatsapp.metaApiBaseUrl` parseia com default valido.
- Bloco `resilience4j.circuitbreaker.instances.erp-callback` parseia (validacao implicita: app sobe sem erro de unmarshalling).
- 2 commits no log: 1 feat + 1 docs SUMMARY.
</verification>

<success_criteria>
- ROU-03 satisfeito: parametros 10/50%/60s + 3x/1s/2.0x configurados em application.yml com retry-exceptions limitada a transient (exclusao explicita de 4xx).
- ROU-04 satisfeito: timeout 5s do callback ja em WhatsAppProperties.callbackTimeout (Phase 1) + spring.http.client global (5s/10s) como defesa em profundidade.
- Risk A6 mitigado: spring-boot-starter-aop no classpath — pre-condicao para Wave 4 funcionar.
- Risk A4 endereçado: metaApiBaseUrl override em test profile permite WireMock em Wave 3 sem hack.
- Phase 1+2 sem regressao (112 tests originais continuam verdes).
</success_criteria>

<output>
After completion, create `.planning/phases/03-roteamento-boundary-async/03-01-SUMMARY.md`
</output>
