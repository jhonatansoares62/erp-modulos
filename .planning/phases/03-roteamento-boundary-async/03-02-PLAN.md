---
phase: 03-roteamento-boundary-async
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
parallel_with: []
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/event/MensagemPersistidaEvent.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ComandoExtractor.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ComandoCallbackDTO.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MetaMediaResultado.java  # NEW
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaMetadataDTO.java  # NEW
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ComandoExtractorTest.java  # NEW
autonomous: true
requirements:
  - ROU-02  # Payload do callback com {telefone, comando, payload, idCliente} (+ media optional) — ComandoCallbackDTO
tags:
  - api-whatsapp
  - dto
  - record
  - event
  - comando-extractor
  - logica-pura

must_haves:
  truths:
    - "MensagemPersistidaEvent record imutavel com 6 fields: wamid, telefone, tipo, conteudo, mediaId, idClienteErp (idClienteErp sempre null quando publicado — listener busca via service)"
    - "ComandoExtractor.extrair(String tipo, String conteudo) e logica pura sem I/O — sem @Autowired no test (JUnit puro)"
    - "ComandoExtractor: text → primeira palavra do conteudo (lowercase Locale.ROOT, trim, split('\\s+')[0])"
    - "ComandoExtractor: interactive_button/interactive_list → id parseado de 'id|title' (substring antes do '|', lowercase) — formato vem do parser Phase 2"
    - "ComandoExtractor: document/image/audio/video → tipo literal (string igual ao constant)"
    - "ComandoExtractor: tipo desconhecido OU null OU conteudo invalido → null (skip dispatch)"
    - "ComandoCallbackDTO record com 7 fields: telefone, comando, payload, idCliente, mediaBase64, mediaMimeType, mediaFilename — Jackson auto-serializa para JSON"
    - "MetaMediaResultado record com 3 fields: byte[] bytes, String mimeType, String filename — uso interno (input do listener para construir DTO callback)"
    - "MediaMetadataDTO regular Jackson DTO com getters/setters (NAO record) — campos: url, mime_type (via @JsonProperty mimeType), filename, sha256, file_size (via @JsonProperty fileSize), id, messaging_product (via @JsonProperty messagingProduct); @JsonIgnoreProperties(ignoreUnknown=true)"
    - "ComandoExtractorTest cobre 8 cases minimo: text simples, text com acentos (Orcamento), text vazio/null, interactive_button com id+title, interactive_button sem '|', interactive_list, document literal, tipo null/desconhecido"
    - "Build verde do reator inteiro sem regressao"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/event/MensagemPersistidaEvent.java"
      provides: "Event imutavel disparado apos persistencia em mensagens_log (Wave 5 publica via ApplicationEventPublisher)"
      contains: "record MensagemPersistidaEvent"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ComandoExtractor.java"
      provides: "Extrai keyword/comando de uma mensagem entrante (text/interactive/media)"
      contains: "extrair(String tipo, String conteudo)"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ComandoCallbackDTO.java"
      provides: "Payload do POST /api/modulos/whatsapp/comando ao ERP"
      contains: "record ComandoCallbackDTO"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MetaMediaResultado.java"
      provides: "Tupla interna bytes+mime+filename retornada por MetaMediaClient.baixar"
      contains: "record MetaMediaResultado"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaMetadataDTO.java"
      provides: "Jackson DTO do step 1 do download Meta Graph API (URL temporaria + metadata)"
      contains: "@JsonIgnoreProperties"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ComandoExtractorTest.java"
      provides: "8+ tests cobrindo todos os branches do switch de ComandoExtractor"
      contains: "primeiraPalavra"
  key_links:
    - from: "ComandoExtractor.extrair"
      to: "TipoMensagem constants (Phase 2)"
      via: "switch case TipoMensagem.TEXT/INTERACTIVE_BUTTON/DOCUMENT/IMAGE/AUDIO/VIDEO"
      pattern: "TipoMensagem\\."
    - from: "MensagemPersistidaEvent (Wave 2)"
      to: "MensagemAsyncListener (Wave 5)"
      via: "ApplicationEventPublisher.publishEvent + @TransactionalEventListener(AFTER_COMMIT)"
      pattern: "MensagemPersistidaEvent"
    - from: "ComandoCallbackDTO (Wave 2)"
      to: "ErpCallbackClient.despachar (Wave 4)"
      via: "RestClient.post().body(payload)"
      pattern: "ComandoCallbackDTO"
    - from: "MediaMetadataDTO (Wave 2)"
      to: "MetaMediaClient.baixar step 1 (Wave 3)"
      via: "RestClient.get().retrieve().body(MediaMetadataDTO.class)"
      pattern: "MediaMetadataDTO"
---

<objective>
Wave 2 (depende de Wave 1 apenas para garantir build/types Resilience4j no classpath; nao usa Resilience4j ainda): criar tipos de dados puros + logica pura SEM I/O. Tudo testavel sem Spring context. Esses 5 artefatos sao consumidos pelas Waves 3, 4 e 5 — entregar antes destas evita o anti-pattern "scavenger hunt" do executor.

Purpose:
- D-05 (comando extraction simples por tipo, sem NLP): `ComandoExtractor` cobre os 4 cenarios mapeados pelo parser Phase 2 (text/interactive/media/desconhecido).
- D-06 (callback via base64, nao filesystem): `ComandoCallbackDTO` carrega `mediaBase64` + mime + filename como campos opcionais — self-contained.
- ROU-02 (payload do callback): record com `{telefone, comando, payload, idCliente}` + 3 campos extras de media.
- D-04 (download 2-step Meta): `MediaMetadataDTO` (Jackson) parseia o step 1 da Graph API; `MetaMediaResultado` (record) e o output ja consolidado.

Output:
- `MensagemPersistidaEvent.java` novo — record 6 fields
- `ComandoExtractor.java` novo — service @Service com switch sobre TipoMensagem
- `ComandoCallbackDTO.java` novo — record 7 fields (Jackson auto)
- `MetaMediaResultado.java` novo — record 3 fields (uso interno)
- `MediaMetadataDTO.java` novo — Jackson POJO com getters/setters + @JsonProperty para snake_case
- `ComandoExtractorTest.java` novo — 8+ tests JUnit puro
- `mvnw verify -pl api-whatsapp` BUILD SUCCESS
- 1 commit feat atomico + 1 commit docs SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-roteamento-boundary-async/03-CONTEXT.md
@.planning/phases/03-roteamento-boundary-async/03-RESEARCH.md
@.planning/phases/03-roteamento-boundary-async/03-01-SUMMARY.md
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MensagemEntranteDTO.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WebhookPayloadParser.java
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Codigo-alvo extraido do RESEARCH §"Code Examples 3, 4, 5" + §"Pattern 3 MediaMetadataDTO". -->
<!-- Implementar EXATAMENTE estes shapes. Sem Lombok per CLAUDE.md. -->

MensagemPersistidaEvent.java (NEW):
```java
package br.com.erpkit.whatsapp.event;

/**
 * Event imutavel disparado APOS persistencia bem-sucedida em mensagens_log.
 *
 * <p>Listener async ({@code MensagemAsyncListener}) consome em pool dedicado.
 * AFTER_COMMIT garante que evento NAO dispara se INSERT rolled back.
 *
 * @param wamid       ID unico do Meta (correlacao)
 * @param telefone    JA NORMALIZADO via TelefoneBR (parser Phase 2 fez antes da persistencia)
 * @param tipo        TipoMensagem constants (text, interactive_button, document, etc.)
 * @param conteudo    payload extraido (texto cru, "id|title", filename) — pode ser null
 * @param mediaId     id Meta para media (document/image/audio/video) — null se sem media
 * @param idClienteErp campo reservado, sempre null neste evento — listener busca via ClienteZapService
 */
public record MensagemPersistidaEvent(
    String wamid,
    String telefone,
    String tipo,
    String conteudo,
    String mediaId,
    Long idClienteErp
) { }
```

ComandoExtractor.java (NEW):
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.util.TipoMensagem;
import org.springframework.stereotype.Service;

import java.util.Locale;

/**
 * Extrai keyword/comando de uma mensagem entrante (D-05 do CONTEXT).
 *
 * <ul>
 *   <li><b>text:</b> primeira palavra do conteudo, lowercase. Ex: "Orcamento 1234" -> "orcamento"</li>
 *   <li><b>interactive_button / interactive_list:</b> id (parser Phase 2 formata "id|title").
 *       Ex: "aprovar_42|Aprovar" -> "aprovar_42"</li>
 *   <li><b>document / image / audio / video:</b> tipo literal — "document", "image", etc.</li>
 *   <li><b>desconhecido / null / conteudo invalido:</b> retorna null (skip dispatch ERP)</li>
 * </ul>
 *
 * <p>Logica pura sem I/O — testavel sem Spring context.
 */
@Service
public class ComandoExtractor {

    public String extrair(String tipo, String conteudo) {
        if (tipo == null) {
            return null;
        }
        return switch (tipo) {
            case TipoMensagem.TEXT -> primeiraPalavra(conteudo);
            case TipoMensagem.INTERACTIVE_BUTTON, TipoMensagem.INTERACTIVE_LIST -> idDeInteractive(conteudo);
            case TipoMensagem.DOCUMENT, TipoMensagem.IMAGE, TipoMensagem.AUDIO, TipoMensagem.VIDEO -> tipo;
            default -> null;  // desconhecido / outros: skip dispatch
        };
    }

    private String primeiraPalavra(String conteudo) {
        if (conteudo == null || conteudo.isBlank()) {
            return null;
        }
        return conteudo.trim().split("\\s+")[0].toLowerCase(Locale.ROOT);
    }

    private String idDeInteractive(String conteudo) {
        // parser Phase 2 formata como "id|title"
        if (conteudo == null) {
            return null;
        }
        int sep = conteudo.indexOf('|');
        return sep > 0 ? conteudo.substring(0, sep).toLowerCase(Locale.ROOT) : null;
    }
}
```

ComandoCallbackDTO.java (NEW):
```java
package br.com.erpkit.whatsapp.dto;

/**
 * Payload do callback POST /api/modulos/whatsapp/comando ao ERP (D-06 + ROU-02).
 *
 * <p>Record imutavel. Jackson auto-serializa para JSON via convertor padrao do Spring Boot.
 *
 * @param telefone        normalizado via TelefoneBR (digitos)
 * @param comando         keyword extraida (ex: "orcamento", "aprovar_42", "document")
 * @param payload         conteudo cru da mensagem (texto, "id|title", filename) — pode ser null
 * @param idCliente       id_cliente_erp resolvido — null se cliente nao mapeado
 * @param mediaBase64     bytes da media em base64 — null se sem media ou expirado
 * @param mediaMimeType   MIME do arquivo — null se sem media
 * @param mediaFilename   nome original — null se sem media
 */
public record ComandoCallbackDTO(
    String telefone,
    String comando,
    String payload,
    Long idCliente,
    String mediaBase64,
    String mediaMimeType,
    String mediaFilename
) { }
```

MetaMediaResultado.java (NEW):
```java
package br.com.erpkit.whatsapp.dto;

/**
 * Resultado interno do download de media Meta — output de {@link br.com.erpkit.whatsapp.service.MetaMediaClient#baixar(String)}.
 *
 * <p>Listener async transforma em base64 antes de incluir no {@link ComandoCallbackDTO}.
 *
 * @param bytes      payload binario completo (PDF/imagem/audio)
 * @param mimeType   MIME declarado pelo Meta no metadata
 * @param filename   nome original (Meta retorna no metadata para document; pode ser null para image/audio)
 */
public record MetaMediaResultado(byte[] bytes, String mimeType, String filename) { }
```

MediaMetadataDTO.java (NEW):
```java
package br.com.erpkit.whatsapp.dto;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;

/**
 * Jackson DTO do step 1 do download de media (GET /v22.0/{media_id}).
 * Resposta do Graph API tem campos snake_case que mapeiamos para camelCase.
 *
 * <p>NAO usar record — Spring Boot 3 + Jackson 2.18 funciona bem com record para
 * deserializacao mas a convencao do monorepo (api-email/api-storage/Phase 2 Webhook
 * envelope DTOs) e POJO com getters/setters explicitos.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public class MediaMetadataDTO {

    private String url;

    @JsonProperty("mime_type")
    private String mimeType;

    private String filename;

    private String sha256;

    @JsonProperty("file_size")
    private Long fileSize;

    private String id;

    @JsonProperty("messaging_product")
    private String messagingProduct;

    public String getUrl() { return url; }
    public void setUrl(String url) { this.url = url; }

    public String getMimeType() { return mimeType; }
    public void setMimeType(String mimeType) { this.mimeType = mimeType; }

    public String getFilename() { return filename; }
    public void setFilename(String filename) { this.filename = filename; }

    public String getSha256() { return sha256; }
    public void setSha256(String sha256) { this.sha256 = sha256; }

    public Long getFileSize() { return fileSize; }
    public void setFileSize(Long fileSize) { this.fileSize = fileSize; }

    public String getId() { return id; }
    public void setId(String id) { this.id = id; }

    public String getMessagingProduct() { return messagingProduct; }
    public void setMessagingProduct(String messagingProduct) { this.messagingProduct = messagingProduct; }
}
```

ComandoExtractorTest.java (NEW) — 8+ tests JUnit puro:
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.util.TipoMensagem;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class ComandoExtractorTest {

    private final ComandoExtractor extractor = new ComandoExtractor();

    @Test
    @DisplayName("text: primeira palavra lowercase")
    void text_primeira_palavra() {
        assertThat(extractor.extrair(TipoMensagem.TEXT, "Orcamento 1234"))
            .isEqualTo("orcamento");
    }

    @Test
    @DisplayName("text: preserva acentos no lowercase")
    void text_acentos() {
        assertThat(extractor.extrair(TipoMensagem.TEXT, "Orcamento de geladeira"))
            .isEqualTo("orcamento");
    }

    @Test
    @DisplayName("text: vazio/null retorna null")
    void text_vazio_null() {
        assertThat(extractor.extrair(TipoMensagem.TEXT, "")).isNull();
        assertThat(extractor.extrair(TipoMensagem.TEXT, "   ")).isNull();
        assertThat(extractor.extrair(TipoMensagem.TEXT, null)).isNull();
    }

    @Test
    @DisplayName("interactive_button: id parseado do formato 'id|title'")
    void interactive_button_com_separador() {
        assertThat(extractor.extrair(TipoMensagem.INTERACTIVE_BUTTON, "aprovar_42|Aprovar"))
            .isEqualTo("aprovar_42");
    }

    @Test
    @DisplayName("interactive_list: id parseado do formato 'id|title'")
    void interactive_list_com_separador() {
        assertThat(extractor.extrair(TipoMensagem.INTERACTIVE_LIST, "menu_inicial|Menu Inicial"))
            .isEqualTo("menu_inicial");
    }

    @Test
    @DisplayName("interactive_button: sem '|' retorna null")
    void interactive_sem_separador() {
        assertThat(extractor.extrair(TipoMensagem.INTERACTIVE_BUTTON, "sem-pipe")).isNull();
        assertThat(extractor.extrair(TipoMensagem.INTERACTIVE_BUTTON, null)).isNull();
    }

    @Test
    @DisplayName("document/image/audio/video retornam o tipo literal")
    void media_tipos_retornam_literal() {
        assertThat(extractor.extrair(TipoMensagem.DOCUMENT, "fatura.pdf"))
            .isEqualTo(TipoMensagem.DOCUMENT);
        assertThat(extractor.extrair(TipoMensagem.IMAGE, "qualquer"))
            .isEqualTo(TipoMensagem.IMAGE);
        assertThat(extractor.extrair(TipoMensagem.AUDIO, "qualquer"))
            .isEqualTo(TipoMensagem.AUDIO);
        assertThat(extractor.extrair(TipoMensagem.VIDEO, "qualquer"))
            .isEqualTo(TipoMensagem.VIDEO);
    }

    @Test
    @DisplayName("tipo desconhecido OU null retorna null (skip dispatch)")
    void desconhecido_e_null() {
        assertThat(extractor.extrair(TipoMensagem.DESCONHECIDO, "qualquer")).isNull();
        assertThat(extractor.extrair(null, "qualquer")).isNull();
        assertThat(extractor.extrair("tipo_inexistente", "qualquer")).isNull();
    }
}
```

NOTA: Verificar se `TipoMensagem` ja tem `VIDEO` constant. Phase 2 SUMMARY (concerns item 3) mencionou que `TipoMensagem` tem 7 constants, falta `STATUS`. Confirmar via `grep "VIDEO" api-whatsapp/src/main/java/.../util/TipoMensagem.java`. Se VIDEO nao existir, ajustar para usar apenas DOCUMENT/IMAGE/AUDIO no switch + remover assertion VIDEO do test (a Phase 2 deliberadamente nao tem VIDEO no parser).

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar TipoMensagem constants + criar 5 artefatos novos (record event/DTOs + service ComandoExtractor + Jackson DTO)</name>
  <files>
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/event/MensagemPersistidaEvent.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ComandoCallbackDTO.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MetaMediaResultado.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaMetadataDTO.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ComandoExtractor.java
  </files>
  <action>
    1. PRIMEIRO: ler `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java` para confirmar quais constants existem (Phase 2 SUMMARY menciona 7 constants — TEXT, INTERACTIVE_BUTTON, INTERACTIVE_LIST, DOCUMENT, IMAGE, AUDIO, DESCONHECIDO). Se `VIDEO` NAO existir:
       - Remover `case TipoMensagem.VIDEO` do switch em `ComandoExtractor.extrair`.
       - Remover assertion VIDEO do test correspondente.
       Se `VIDEO` existir: manter o snippet integral.

    2. Criar `event/MensagemPersistidaEvent.java` EXATAMENTE como em <interfaces>. Record com 6 fields. Javadoc completo.

    3. Criar `dto/ComandoCallbackDTO.java` EXATAMENTE como em <interfaces>. Record com 7 fields.

    4. Criar `dto/MetaMediaResultado.java` EXATAMENTE como em <interfaces>. Record com 3 fields.

    5. Criar `dto/MediaMetadataDTO.java` EXATAMENTE como em <interfaces>. POJO Jackson com `@JsonIgnoreProperties(ignoreUnknown=true)`, getters/setters explicitos, `@JsonProperty` em mime_type/file_size/messaging_product.

    6. Criar `service/ComandoExtractor.java` EXATAMENTE como em <interfaces>. `@Service`. Switch com `Locale.ROOT` no `toLowerCase`. Helpers privados `primeiraPalavra` + `idDeInteractive`.

    Convencoes: imports na ordem do `.planning/codebase/CONVENTIONS.md` (jakarta/java → org.springframework/com.fasterxml → br.com.erpkit). 4-space indent. UTF-8. Sem Lombok. PT-BR nos identificadores e Javadoc.
  </action>
  <verify>
    <automated>./mvnw compile -pl api-whatsapp -q 2>&amp;1 | tail -10</automated>
    Esperado: BUILD SUCCESS sem erros. Verificacao adicional: `grep -l "VIDEO" api-whatsapp/src/main/java/br/com/erpkit/whatsapp/util/TipoMensagem.java` (sucesso = arquivo existe; saida vazia = VIDEO ausente, codigo deve ter sido ajustado).
  </verify>
  <done>
    5 arquivos compilam sem erros. ComandoExtractor.extrair switch alinhado com constants reais de TipoMensagem.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar ComandoExtractorTest com 8+ tests JUnit puro</name>
  <files>
    api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ComandoExtractorTest.java
  </files>
  <action>
    Criar `ComandoExtractorTest.java` EXATAMENTE como o snippet em <interfaces>, ajustando se `TipoMensagem.VIDEO` nao existir (remover assertion VIDEO).

    Cobertura minima de 8 tests:
    1. `text_primeira_palavra` — text + "Orcamento 1234" → "orcamento"
    2. `text_acentos` — text + "Orcamento de geladeira" → "orcamento"
    3. `text_vazio_null` — text + ""/"   "/null → 3 asserts null
    4. `interactive_button_com_separador` — INTERACTIVE_BUTTON + "aprovar_42|Aprovar" → "aprovar_42"
    5. `interactive_list_com_separador` — INTERACTIVE_LIST + "menu_inicial|Menu Inicial" → "menu_inicial"
    6. `interactive_sem_separador` — INTERACTIVE_BUTTON + "sem-pipe"/null → 2 asserts null
    7. `media_tipos_retornam_literal` — DOCUMENT/IMAGE/AUDIO (e VIDEO se existir) → tipo literal
    8. `desconhecido_e_null` — TipoMensagem.DESCONHECIDO + null + "tipo_inexistente" → 3 asserts null

    Test class SEM `@SpringBootTest` (logica pura). `private final ComandoExtractor extractor = new ComandoExtractor();` — instanciacao direta.
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=ComandoExtractorTest -q 2>&amp;1 | tail -10</automated>
    Esperado: `Tests run: 8, Failures: 0, Errors: 0` (ou 8+ se VIDEO existir e adicionar mais cases).
  </verify>
  <done>
    ComandoExtractorTest 8+ tests verdes, todos os branches do switch cobertos.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verde do reator + commit feat + commit docs SUMMARY</name>
  <files>
    .planning/phases/03-roteamento-boundary-async/03-02-SUMMARY.md
  </files>
  <action>
    1. Rodar `./mvnw verify -pl api-whatsapp -am` — esperar BUILD SUCCESS. Sem regressao em 113 tests da Wave 1 + 8+ novos = 121+ total.

    2. Criar `.planning/phases/03-roteamento-boundary-async/03-02-SUMMARY.md` documentando: 5 artefatos criados (1 event record + 3 DTOs records + 1 Jackson DTO + 1 service), 8+ tests JUnit puro verdes, build do reator verde.

    3. Commits via gsd-tools.cjs:
       ```bash
       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): MensagemPersistidaEvent + ComandoExtractor + DTOs (Phase 3 Wave 2)" --files \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/event/MensagemPersistidaEvent.java \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/ComandoExtractor.java \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ComandoCallbackDTO.java \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MetaMediaResultado.java \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaMetadataDTO.java \
         api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ComandoExtractorTest.java

       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(03-02): SUMMARY plan 02 + atualizar STATE/ROADMAP" --files \
         .planning/phases/03-roteamento-boundary-async/03-02-SUMMARY.md \
         .planning/STATE.md \
         .planning/ROADMAP.md
       ```

    Atualizar STATE.md com Plano 03-02 completo. Atualizar ROADMAP.md Phase 3 marcando `- [x] 03-02-PLAN.md ...`.
  </action>
  <verify>
    <automated>./mvnw verify -pl api-whatsapp -am -q 2>&amp;1 | tail -10</automated>
    Esperado: BUILD SUCCESS + 121+ tests verdes.
  </verify>
  <done>
    Build verde, SUMMARY criado, 2 commits atomicos.
  </done>
</task>

</tasks>

<verification>
- 5 arquivos novos compilam — `mvnw compile` verde.
- ComandoExtractorTest 8+ tests cobrindo cada branch do switch.
- `mvnw verify -pl api-whatsapp -am` BUILD SUCCESS, sem regressao em Wave 1 ou Phase 1+2 (121+ tests verdes).
- 2 commits no log: 1 feat + 1 docs SUMMARY.
</verification>

<success_criteria>
- D-05 satisfeito: ComandoExtractor cobre os 4 cenarios (text/interactive/media/desconhecido) sem NLP.
- D-06 + ROU-02 satisfeitos: ComandoCallbackDTO record com {telefone, comando, payload, idCliente} + 3 fields opcionais de media (base64/mime/filename).
- D-04 (parcial — pre-tipo): MediaMetadataDTO + MetaMediaResultado prontos para Wave 3 consumir.
- ROU-01 (parcial): MensagemPersistidaEvent record pronto para Wave 5 publicar via ApplicationEventPublisher.
- 0 dependencia de I/O — todas Wave 5 unit tests podem mockar essas estruturas.
</success_criteria>

<output>
After completion, create `.planning/phases/03-roteamento-boundary-async/03-02-SUMMARY.md`
</output>
