---
phase: 03-roteamento-boundary-async
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
parallel_with: []
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MetaMediaClient.java  # NEW
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MetaMediaClientTest.java  # NEW
autonomous: true
requirements:
  - ROU-05  # Download de media entrante e a PRIMEIRA acao apos ack — URL Meta expira em 5min; bytes guardados em memoria pra entregar pro ERP no callback
tags:
  - api-whatsapp
  - meta-graph-api
  - rest-client
  - 2-step-download
  - bearer-header
  - wiremock

must_haves:
  truths:
    - "MetaMediaClient.baixar(String mediaId) retorna Optional<MetaMediaResultado>"
    - "Step 1: GET {metaApiBaseUrl}/{mediaId} com header Authorization: Bearer {accessToken} — desserializa JSON em MediaMetadataDTO"
    - "Step 2: GET {metadata.url} com mesmo Authorization Bearer header (URL e absoluta tipo lookaside.fbsbx.com — RestClient segue) — body como byte[].class via ByteArrayHttpMessageConverter"
    - "Bearer accessToken NUNCA em query param (PITFALLS C-14) — sempre via header HttpHeaders.AUTHORIZATION"
    - "404 step 1 (HttpClientErrorException.NotFound): Optional.empty() + log.warn — URL Meta expirada (5min PITFALLS C-08)"
    - "404 step 2 (HttpClientErrorException.NotFound): Optional.empty() + log.warn — URL expirada antes do step 2"
    - "metadata null OU metadata.url null OU bytes null/vazio: Optional.empty() + log.warn — defesa em profundidade"
    - "RestClient construido em constructor via WhatsAppProperties.metaApiBaseUrl (configurable per Wave 1)"
    - "SEM Resilience4j — primeira acao apos ack precisa ser rapida; queue delay e o risco principal, nao indisponibilidade Meta"
    - "MetaMediaClientTest @SpringBootTest com WireMock 3.10.0 standalone — porta dinamica via WireMockServer.dynamicPort"
    - "MetaMediaClientTest @DynamicPropertySource sobrescreve app.modulos.whatsapp.metaApiBaseUrl com URL do WireMock"
    - "Test happy_path: WireMock retorna 200+JSON em /v22.0/{id} + 200+bytes na URL absoluta — MetaMediaResultado completo retornado"
    - "Test 404_step_1: WireMock retorna 404 em /v22.0/{id} — Optional.empty()"
    - "Test 404_step_2: WireMock retorna 200 em step 1 mas 404 na URL absoluta — Optional.empty()"
    - "Test 5xx_step_1: WireMock retorna 500 em /v22.0/{id} — sem retry (sem Resilience4j); excecao escapa OU Optional.empty (decisao: deixar HttpServerErrorException escapar; listener Wave 5 captura via try/catch generico)"
    - "Test bearer_header_presente: assert WireMock.verify(getRequestedFor.withHeader('Authorization', equalTo('Bearer test-access-token'))) em ambas as requests"
    - "Test bearer_nunca_em_query: assert WireMock NAO recebeu request com '?access_token=' na query (defesa contra regressao C-14)"
    - "Build verde sem regressao em Phase 1+2 + Wave 1+2"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MetaMediaClient.java"
      provides: "Cliente HTTP 2-step para Graph API media download (entrada apenas)"
      contains: "Optional<MetaMediaResultado> baixar(String mediaId)"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MetaMediaClientTest.java"
      provides: "5+ tests com WireMock cobrindo happy path + 404 step 1/2 + 5xx + bearer header"
      contains: "WireMockServer"
  key_links:
    - from: "MetaMediaClient.baixar"
      to: "WhatsAppProperties.getMetaApiBaseUrl + getAccessToken"
      via: "constructor injection — RestClient.builder().baseUrl(props.getMetaApiBaseUrl())"
      pattern: "getMetaApiBaseUrl"
    - from: "MetaMediaClient (Wave 3)"
      to: "MensagemAsyncListener (Wave 5)"
      via: "constructor injection — listener chama metaMediaClient.baixar(mediaId) como PRIMEIRA acao apos ack (D-04)"
      pattern: "metaMediaClient\\.baixar"
    - from: "MetaMediaClient.baixar step 1"
      to: "MediaMetadataDTO (Wave 2)"
      via: "RestClient.retrieve().body(MediaMetadataDTO.class)"
      pattern: "MediaMetadataDTO\\.class"
    - from: "MetaMediaClient.baixar"
      to: "MetaMediaResultado record (Wave 2)"
      via: "Optional.of(new MetaMediaResultado(bytes, mimeType, filename))"
      pattern: "new MetaMediaResultado"
---

<objective>
Wave 3 (depende de Wave 2 — DTOs + Properties.metaApiBaseUrl): implementar `MetaMediaClient` com 2-step Graph API media download per D-04 + ROU-05. Sem Resilience4j (primeira acao precisa ser rapida; PITFALLS C-08 — URL expira em 5min). 404 graceful via Optional.empty + log.warn. Bearer accessToken sempre em Authorization header (PITFALLS C-14).

Tests com WireMock 3.10.0 standalone (porta dinamica) cobrem happy path + os 2 cenarios de 404 (step 1 ou step 2) + 5xx + assertions de header Bearer + assertion explicita NAO ha query param `?access_token=` (regressao C-14).

Purpose:
- ROU-05: media download e a primeira acao do listener (Wave 5) — Wave 3 entrega o cliente HTTP isolado, sem AOP/Resilience4j.
- D-04: 2-step (`GET /{id}` → metadata → `GET url` → bytes), ambas autenticadas via header.
- Risk A4 mitigado: WireMock setup pattern estabelecido aqui sera reusado nas Waves 4 e 6.
- Risk A7 validado: `byte[].class` no `RestClient.body()` funciona via ByteArrayHttpMessageConverter (Spring default).

Output:
- `MetaMediaClient.java` novo — service @Service, RestClient construido via Properties
- `MetaMediaClientTest.java` novo — 5+ tests WireMock
- `mvnw verify -pl api-whatsapp` BUILD SUCCESS
- 1 commit feat atomico + 1 commit docs SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-roteamento-boundary-async/03-CONTEXT.md
@.planning/phases/03-roteamento-boundary-async/03-RESEARCH.md
@.planning/phases/03-roteamento-boundary-async/03-01-SUMMARY.md
@.planning/phases/03-roteamento-boundary-async/03-02-SUMMARY.md
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MediaMetadataDTO.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/MetaMediaResultado.java
@api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java

<interfaces>
<!-- Codigo-alvo extraido do RESEARCH §"Pattern 3: 2-Step Media Download" + §"Open Question 3" InOrder verification. -->

MetaMediaClient.java (NEW):
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.config.WhatsAppProperties;
import br.com.erpkit.whatsapp.dto.MediaMetadataDTO;
import br.com.erpkit.whatsapp.dto.MetaMediaResultado;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpHeaders;
import org.springframework.stereotype.Service;
import org.springframework.web.client.HttpClientErrorException;
import org.springframework.web.client.RestClient;

import java.util.Optional;

/**
 * Cliente HTTP para download de media de ENTRADA do Meta (D-04 + ROU-05).
 *
 * <p>Sequencia 2-step:
 * <ol>
 *   <li>GET {metaApiBaseUrl}/{mediaId} -> {@link MediaMetadataDTO} (URL temporaria + metadata)</li>
 *   <li>GET {metadata.url} -> byte[] (URL absoluta tipo lookaside.fbsbx.com — RestClient segue)</li>
 * </ol>
 *
 * <p><b>Bearer obrigatorio em ambas requests (PITFALLS C-14):</b> token NUNCA em query param.
 *
 * <p><b>404 graceful (PITFALLS C-08):</b> URL Meta expira em 5min; se queue async atrasar
 * apos o ack 200 e a URL ja expirou no momento do download, retorna {@link Optional#empty()}
 * + log.warn — mensagem persiste sem bytes; ERP recebe callback sem mediaBase64.
 *
 * <p><b>Sem Resilience4j:</b> primeira acao apos ack precisa ser RAPIDA; circuit breaker
 * adicionaria latencia desnecessaria. Falha = mensagem persistida sem bytes; nao critico.
 */
@Service
public class MetaMediaClient {

    private static final Logger log = LoggerFactory.getLogger(MetaMediaClient.class);

    private final WhatsAppProperties properties;
    private final RestClient restClient;

    public MetaMediaClient(WhatsAppProperties properties) {
        this.properties = properties;
        this.restClient = RestClient.builder()
                .baseUrl(properties.getMetaApiBaseUrl())
                .build();
    }

    /**
     * Baixa media de entrada do Meta. Retorna {@link Optional#empty()} se URL expirada
     * ou metadata invalida; nunca propaga {@link HttpClientErrorException.NotFound}.
     *
     * @param mediaId ID retornado em messages[].image|document|audio|video.id do webhook
     * @return Optional com bytes + metadata; empty() se 404 em qualquer step
     */
    public Optional<MetaMediaResultado> baixar(String mediaId) {
        // Step 1: metadata (URL temporaria assinada + mime + filename)
        MediaMetadataDTO metadata;
        try {
            metadata = restClient.get()
                    .uri("/{id}", mediaId)
                    .header(HttpHeaders.AUTHORIZATION, "Bearer " + properties.getAccessToken())
                    .retrieve()
                    .body(MediaMetadataDTO.class);
        } catch (HttpClientErrorException.NotFound e) {
            log.warn("Meta media metadata 404 — URL expirada (5min): mediaId={}", mediaId);
            return Optional.empty();
        }
        if (metadata == null || metadata.getUrl() == null) {
            log.warn("Meta media metadata sem URL: mediaId={}", mediaId);
            return Optional.empty();
        }

        // Step 2: bytes binarios. URL e absoluta — RestClient segue. Bearer continua
        // obrigatorio no header da CDN do Meta (PITFALLS C-14: NUNCA query param).
        byte[] bytes;
        try {
            bytes = restClient.get()
                    .uri(metadata.getUrl())
                    .header(HttpHeaders.AUTHORIZATION, "Bearer " + properties.getAccessToken())
                    .retrieve()
                    .body(byte[].class);
        } catch (HttpClientErrorException.NotFound e) {
            log.warn("Meta media bytes 404 — URL expirada antes do step 2: mediaId={}", mediaId);
            return Optional.empty();
        }
        if (bytes == null || bytes.length == 0) {
            log.warn("Meta media bytes vazio: mediaId={}", mediaId);
            return Optional.empty();
        }

        return Optional.of(new MetaMediaResultado(bytes, metadata.getMimeType(), metadata.getFilename()));
    }
}
```

MetaMediaClientTest.java (NEW) — esqueleto do test:
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.WhatsAppApplication;
import br.com.erpkit.whatsapp.dto.MetaMediaResultado;
import com.github.tomakehurst.wiremock.WireMockServer;
import com.github.tomakehurst.wiremock.client.WireMock;
import com.github.tomakehurst.wiremock.core.WireMockConfiguration;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;

import java.util.Optional;

import static com.github.tomakehurst.wiremock.client.WireMock.*;
import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(classes = WhatsAppApplication.class)
@ActiveProfiles("test")
class MetaMediaClientTest {

    private static WireMockServer wireMock;

    @BeforeAll
    static void startWireMock() {
        wireMock = new WireMockServer(WireMockConfiguration.options().dynamicPort());
        wireMock.start();
    }

    @AfterAll
    static void stopWireMock() {
        wireMock.stop();
    }

    @DynamicPropertySource
    static void overrideProperties(DynamicPropertyRegistry registry) {
        registry.add("app.modulos.whatsapp.metaApiBaseUrl", () -> wireMock.baseUrl());
    }

    @Autowired MetaMediaClient client;

    @BeforeEach
    void resetWireMock() {
        wireMock.resetAll();
    }

    @Test
    @DisplayName("Happy path: step 1 retorna metadata + step 2 retorna bytes — Optional preenchido")
    void happy_path() {
        // Step 1: metadata
        wireMock.stubFor(get(urlEqualTo("/MEDIA-ID-001"))
            .willReturn(aResponse()
                .withStatus(200)
                .withHeader("Content-Type", "application/json")
                .withBody("""
                    {
                      "url": "%s/cdn/MEDIA-ID-001/data",
                      "mime_type": "application/pdf",
                      "filename": "fatura.pdf",
                      "sha256": "abc123",
                      "file_size": 12345,
                      "id": "MEDIA-ID-001",
                      "messaging_product": "whatsapp"
                    }
                    """.formatted(wireMock.baseUrl()))));
        // Step 2: bytes
        wireMock.stubFor(get(urlEqualTo("/cdn/MEDIA-ID-001/data"))
            .willReturn(aResponse()
                .withStatus(200)
                .withHeader("Content-Type", "application/pdf")
                .withBody(new byte[] {1, 2, 3, 4, 5})));

        Optional<MetaMediaResultado> resultado = client.baixar("MEDIA-ID-001");

        assertThat(resultado).isPresent();
        assertThat(resultado.get().bytes()).containsExactly(1, 2, 3, 4, 5);
        assertThat(resultado.get().mimeType()).isEqualTo("application/pdf");
        assertThat(resultado.get().filename()).isEqualTo("fatura.pdf");

        // Assert: ambas requests com Bearer header. Token "test-access-token" vem do application-test.yml.
        wireMock.verify(getRequestedFor(urlEqualTo("/MEDIA-ID-001"))
            .withHeader("Authorization", equalTo("Bearer test-access-token")));
        wireMock.verify(getRequestedFor(urlEqualTo("/cdn/MEDIA-ID-001/data"))
            .withHeader("Authorization", equalTo("Bearer test-access-token")));
    }

    @Test
    @DisplayName("404 no step 1: Optional.empty + log.warn (URL expirada antes do listener rodar)")
    void quatrocentosequatro_step_1() {
        wireMock.stubFor(get(urlEqualTo("/MEDIA-ID-EXPIRED"))
            .willReturn(aResponse().withStatus(404).withBody("{\"error\":\"expired\"}")));

        Optional<MetaMediaResultado> resultado = client.baixar("MEDIA-ID-EXPIRED");

        assertThat(resultado).isEmpty();
        wireMock.verify(0, getRequestedFor(urlMatching("/cdn/.*")));
    }

    @Test
    @DisplayName("404 no step 2: step 1 ok mas URL absoluta retorna 404 — Optional.empty")
    void quatrocentosequatro_step_2() {
        wireMock.stubFor(get(urlEqualTo("/MEDIA-ID-002"))
            .willReturn(aResponse().withStatus(200).withHeader("Content-Type", "application/json")
                .withBody("""
                    {
                      "url": "%s/cdn/MEDIA-ID-002/data",
                      "mime_type": "image/jpeg",
                      "filename": "foto.jpg"
                    }
                    """.formatted(wireMock.baseUrl()))));
        wireMock.stubFor(get(urlEqualTo("/cdn/MEDIA-ID-002/data"))
            .willReturn(aResponse().withStatus(404)));

        Optional<MetaMediaResultado> resultado = client.baixar("MEDIA-ID-002");
        assertThat(resultado).isEmpty();
    }

    @Test
    @DisplayName("5xx no step 1: sem retry (sem Resilience4j) — excecao propaga; listener Wave 5 captura")
    void cincocentos_step_1_sem_retry() {
        wireMock.stubFor(get(urlEqualTo("/MEDIA-ID-500"))
            .willReturn(aResponse().withStatus(500)));

        // Espera-se que client.baixar lance HttpServerErrorException (sem captura interna).
        // Listener (Wave 5) tem try/catch generico em volta da chamada.
        org.assertj.core.api.Assertions.assertThatThrownBy(() -> client.baixar("MEDIA-ID-500"))
            .isInstanceOf(org.springframework.web.client.HttpServerErrorException.class);

        // Counter == 1 (sem retry — sem Resilience4j neste cliente).
        wireMock.verify(1, getRequestedFor(urlEqualTo("/MEDIA-ID-500")));
    }

    @Test
    @DisplayName("Bearer NUNCA em query param (regressao C-14)")
    void bearer_nunca_em_query_param() {
        wireMock.stubFor(get(urlEqualTo("/MEDIA-ID-003"))
            .willReturn(aResponse().withStatus(200).withHeader("Content-Type", "application/json")
                .withBody("""
                    {
                      "url": "%s/cdn/MEDIA-ID-003/data",
                      "mime_type": "application/pdf",
                      "filename": "f.pdf"
                    }
                    """.formatted(wireMock.baseUrl()))));
        wireMock.stubFor(get(urlEqualTo("/cdn/MEDIA-ID-003/data"))
            .willReturn(aResponse().withStatus(200).withBody(new byte[]{1, 2, 3})));

        client.baixar("MEDIA-ID-003");

        // Assert: NENHUMA das requests recebidas teve "?access_token=" na query string.
        // WireMock matchers nao tem "url-with-query-not-containing", entao validamos via
        // get all requests + filter manual.
        wireMock.getAllServeEvents().forEach(event -> {
            String url = event.getRequest().getUrl();
            assertThat(url)
                .as("PITFALLS C-14: token NUNCA em query param")
                .doesNotContain("access_token=");
        });
    }
}
```

WireMock import (nas dependencias de Wave 1): `com.github.tomakehurst.wiremock.WireMockServer` (do package shaded `wiremock-standalone`).

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar MetaMediaClient.java + verificar build compile sem regressao</name>
  <files>
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MetaMediaClient.java
  </files>
  <action>
    Criar `MetaMediaClient.java` EXATAMENTE como o snippet em <interfaces>:

    1. Constructor recebe `WhatsAppProperties` (injection padrao Spring).
    2. `RestClient.builder().baseUrl(properties.getMetaApiBaseUrl()).build()` no constructor.
    3. Method `baixar(String mediaId)` retorna `Optional<MetaMediaResultado>`.
    4. Step 1: `restClient.get().uri("/{id}", mediaId).header(HttpHeaders.AUTHORIZATION, "Bearer " + properties.getAccessToken()).retrieve().body(MediaMetadataDTO.class)`. Catch `HttpClientErrorException.NotFound` → log.warn + Optional.empty.
    5. Validacao defensiva: `metadata == null || metadata.getUrl() == null` → Optional.empty + log.warn.
    6. Step 2: `restClient.get().uri(metadata.getUrl()).header(HttpHeaders.AUTHORIZATION, "Bearer " + properties.getAccessToken()).retrieve().body(byte[].class)`. Catch `HttpClientErrorException.NotFound` → Optional.empty.
    7. `bytes == null || bytes.length == 0` → Optional.empty.
    8. Retorna `Optional.of(new MetaMediaResultado(bytes, metadata.getMimeType(), metadata.getFilename()))`.

    NAO catch `HttpServerErrorException` (5xx) — listener Wave 5 captura via try/catch generico. NAO usar Resilience4j annotations. NAO usar interceptor para Bearer (sempre explicito no header per request — clareza > magic).

    Convencoes: imports na ordem (jakarta/java → org.springframework/com.fasterxml/org.slf4j → br.com.erpkit). 4-space indent. PT-BR no Javadoc.
  </action>
  <verify>
    <automated>./mvnw compile -pl api-whatsapp -q 2>&amp;1 | tail -10</automated>
    Esperado: BUILD SUCCESS sem erros de classpath/import.
  </verify>
  <done>
    MetaMediaClient.java compila. Bean Spring auto-detectavel via @Service.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar MetaMediaClientTest.java com 5 tests WireMock (happy + 404 step 1 + 404 step 2 + 5xx + bearer nunca em query)</name>
  <files>
    api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MetaMediaClientTest.java
  </files>
  <action>
    Criar `MetaMediaClientTest.java` EXATAMENTE como o snippet em <interfaces>:

    Setup:
    - `@SpringBootTest(classes = WhatsAppApplication.class) @ActiveProfiles("test")`.
    - `WireMockServer wireMock` static, iniciado em `@BeforeAll` com `WireMockConfiguration.options().dynamicPort()`, parado em `@AfterAll`.
    - `@DynamicPropertySource` sobrescreve `app.modulos.whatsapp.metaApiBaseUrl` com `wireMock.baseUrl()`.
    - `@BeforeEach wireMock.resetAll()` — isolamento entre tests.

    5 tests minimo:
    1. `happy_path` — step 1 + step 2 200; assert `MetaMediaResultado` com bytes/mime/filename corretos + WireMock.verify ambas requests com `Authorization: Bearer test-access-token`.
    2. `quatrocentosequatro_step_1` — WireMock 404 em `/MEDIA-ID-EXPIRED`; `Optional.empty`; `verify(0, getRequestedFor(urlMatching("/cdn/.*")))` (step 2 NAO chamado).
    3. `quatrocentosequatro_step_2` — step 1 200 mas URL absoluta retorna 404; `Optional.empty`.
    4. `cincocentos_step_1_sem_retry` — 500; espera `HttpServerErrorException` propagada; counter == 1 (NAO retried — confirma A6 indiretamente: MetaMediaClient nao tem Resilience4j por design).
    5. `bearer_nunca_em_query_param` — apos chamar baixar, `wireMock.getAllServeEvents().forEach(... assertThat(url).doesNotContain("access_token="))`.

    Imports criticos: `com.github.tomakehurst.wiremock.WireMockServer`, `com.github.tomakehurst.wiremock.core.WireMockConfiguration`, static `com.github.tomakehurst.wiremock.client.WireMock.*`.

    Token de teste vem do `application-test.yml` linha 69: `accessToken: test-access-token`.
  </action>
  <verify>
    <automated>./mvnw test -pl api-whatsapp -Dtest=MetaMediaClientTest -q 2>&amp;1 | tail -10</automated>
    Esperado: `Tests run: 5, Failures: 0, Errors: 0`.
  </verify>
  <done>
    5 tests verdes. Bearer header sempre presente. Sem retry em 5xx (counter==1). 404 em qualquer step retorna Optional.empty.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verde do reator + commit feat + commit docs SUMMARY</name>
  <files>
    .planning/phases/03-roteamento-boundary-async/03-03-SUMMARY.md
  </files>
  <action>
    1. Rodar `./mvnw verify -pl api-whatsapp -am` — esperar BUILD SUCCESS. Total deve ser 121+ + 5 = 126+ tests verdes (zero regressao em Phase 1+2 + Wave 1+2).

    2. Criar `.planning/phases/03-roteamento-boundary-async/03-03-SUMMARY.md` documentando: MetaMediaClient + 5 tests WireMock, padrao WireMock setup com dynamicPort + @DynamicPropertySource estabelecido (sera reusado em Wave 4 e Wave 6).

    3. Commits:
       ```bash
       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "feat(api-whatsapp): MetaMediaClient com 2-step Graph API + WireMock tests (Phase 3 Wave 3)" --files \
         api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MetaMediaClient.java \
         api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MetaMediaClientTest.java

       node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(03-03): SUMMARY plan 03 + atualizar STATE/ROADMAP" --files \
         .planning/phases/03-roteamento-boundary-async/03-03-SUMMARY.md \
         .planning/STATE.md \
         .planning/ROADMAP.md
       ```

    Atualizar STATE/ROADMAP marcando 03-03 completo.
  </action>
  <verify>
    <automated>./mvnw verify -pl api-whatsapp -am -q 2>&amp;1 | tail -10</automated>
    Esperado: BUILD SUCCESS + 126+ tests.
  </verify>
  <done>
    Build verde, 5 tests novos, 2 commits.
  </done>
</task>

</tasks>

<verification>
- ROU-05 satisfeito: MetaMediaClient implementa 2-step download; 404 graceful; bytes em memoria retornados via Optional<MetaMediaResultado>.
- D-04 satisfeito: Bearer header em ambas requests; sem query param.
- Risk A4 mitigado: padrao WireMock + @DynamicPropertySource estabelecido para reuso.
- Risk A7 validado: byte[].class no RestClient funciona.
- Sem regressao em Phase 1+2 + Wave 1+2.
</verification>

<success_criteria>
- 5 tests WireMock cobrem happy path + 2 cenarios 404 + 5xx sem retry + bearer-never-in-query.
- WireMock counter assertions confirmam comportamento esperado:
  - 404 step 1 → step 2 nao chamado (counter == 0)
  - 5xx → counter == 1 (NAO retentado, sem Resilience4j neste cliente)
- Bearer accessToken sempre via header (assertion explicita C-14).
- mvnw verify BUILD SUCCESS, 126+ tests verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-roteamento-boundary-async/03-03-SUMMARY.md`
</output>
