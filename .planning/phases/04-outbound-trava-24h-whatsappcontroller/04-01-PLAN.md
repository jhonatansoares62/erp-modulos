---
phase: 04-outbound-trava-24h-whatsappcontroller
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java
  - lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java
  - api-whatsapp/src/main/resources/application.yml
  - api-whatsapp/src/test/resources/application-test.yml
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java
autonomous: true
requirements:
  - OUT-10
tags: [whatsapp, outbound, infra, error-response, resilience4j, multipart-spike]
must_haves:
  truths:
    - "ErrorResponse em lib-shared expoe campos opcionais codigo (String) e metaErrorCode (Integer) — mudanca compativel com api-email/api-storage/api-consultas (campos nullable, ignorados quando nao setados) [habilita D-02]"
    - "GlobalExceptionHandler.handleModuloException usa ErrorCarrier-style helper SEM importar exceptions de api-whatsapp em lib-shared (preserva direcao de dependencia) — propagacao via interface CodigoCarrier em lib-shared, exceptions Phase 4 implementam"
    - "application.yml e application-test.yml de api-whatsapp expoem instance Resilience4j 'whatsapp-cloud' (CB sliding-window=10/threshold=50/wait-open=60s e Retry max-attempts=3/wait=1s/multiplier=2.0/retry-exceptions completo incluindo ResourceAccessException)"
    - "MultipartUploadSpikeTest valida empiricamente em WireMock que upload multipart Spring RestClient para /v22.0/{phoneNumberId}/media envia exatamente 3 fields (messaging_product, type, file) com boundary auto e Bearer NUNCA em query param — pre-requisito para 04-04 (PITFALLS C-15 + Pitfall 5 RESEARCH)"
    - "Reator inteiro `./mvnw verify` BUILD SUCCESS apos modificacao do ErrorResponse (lib-shared) — zero regressao em api-email/api-storage/api-consultas"
  artifacts:
    - path: "lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java"
      provides: "Campo codigo (String, nullable) + metaErrorCode (Integer, nullable) + getters/setters"
      contains: "private String codigo"
    - path: "lib-shared/src/main/java/br/com/erpkit/shared/exception/CodigoCarrier.java"
      provides: "Interface marker para excecoes que carregam codigo (e opcionalmente metaErrorCode)"
      contains: "interface CodigoCarrier"
    - path: "lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java"
      provides: "Handler propaga codigo+metaErrorCode quando ex implements CodigoCarrier"
      contains: "instanceof CodigoCarrier"
    - path: "api-whatsapp/src/main/resources/application.yml"
      provides: "Instance resilience4j whatsapp-cloud (CB + Retry com retry-exceptions whitelist)"
      contains: "whatsapp-cloud:"
    - path: "api-whatsapp/src/test/resources/application-test.yml"
      provides: "Instance whatsapp-cloud test profile (wait-duration 50ms para tests rapidos)"
      contains: "whatsapp-cloud:"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java"
      provides: "Spike Wave 0 empirico — multipart Spring RestClient para WireMock validando 3 fields + boundary auto + Bearer header (PITFALLS C-15)"
      contains: "messaging_product"
  key_links:
    - from: "lib-shared/.../GlobalExceptionHandler.java"
      to: "lib-shared/.../CodigoCarrier.java"
      via: "instanceof check (sem import de api-whatsapp)"
      pattern: "instanceof CodigoCarrier"
    - from: "api-whatsapp/.../application.yml"
      to: "api-whatsapp/.../application-test.yml"
      via: "espelho da config erp-callback Phase 3 com nome whatsapp-cloud"
      pattern: "whatsapp-cloud:"
---

<objective>
Habilitar a infraestrutura compartilhada da Phase 4: campo `codigo` + `metaErrorCode` no `ErrorResponse` (lib-shared), interface `CodigoCarrier` em lib-shared para evitar acoplamento ascendente, propagacao via `GlobalExceptionHandler`, instance Resilience4j `whatsapp-cloud` em ambos `application.yml` e `application-test.yml`, e spike empirico Wave 0 que valida o multipart upload Spring RestClient → WireMock antes de 04-04 implementar.

Purpose: Sem esta wave 0, o `WhatsAppCloudClient` (04-04) seria construido contra suposicoes de RESEARCH §Pattern Multipart §Pitfall 5 — risco de descobrir tarde que `MultiValueMap<String, Object>` + `MediaType.MULTIPART_FORM_DATA` falha (boundary nao autoinjetado, `ByteArrayResource.getFilename()` ausente) ou que Bearer vaza em logs. O spike + ErrorResponse fix elimina dois bloqueadores em <30 min, libera Wave 1 (04-02 + 04-03 + 04-04) com superficie estavel.

Output: lib-shared com ErrorResponse expandido + CodigoCarrier interface + handler propagando, api-whatsapp/yml com bloco whatsapp-cloud Resilience4j, MultipartUploadSpikeTest verde provando os 3 fields obrigatorios + ausencia de Bearer em query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-CONTEXT.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@./CLAUDE.md

<interfaces>
<!-- Lib-shared current state (Read before modifying) -->

From lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java (atual):
```java
public class ErrorResponse {
    private int status;
    private String erro;
    private String mensagem;
    private LocalDateTime timestamp;
    private Map<String, String> campos;
    // ctors + getters/setters padrao bean
}
```

From lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java (atual):
```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(ModuloException.class)
    public ResponseEntity<ErrorResponse> handleModuloException(ModuloException ex) {
        ErrorResponse error = new ErrorResponse(ex.getStatus().value(),
            ex.getStatus().getReasonPhrase(), ex.getMessage());
        return ResponseEntity.status(ex.getStatus()).body(error);
    }
    // ... handleValidation + handleGeneric
}
```

From api-whatsapp/src/main/resources/application.yml (Phase 3 — bloco erp-callback ja existente, NAO MODIFICAR linhas 138-167):
```yaml
resilience4j:
  circuitbreaker:
    instances:
      erp-callback:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 60s
        permitted-number-of-calls-in-half-open-state: 3
        register-health-indicator: false
  retry:
    instances:
      erp-callback:
        max-attempts: 3
        wait-duration: 1s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2.0
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - org.springframework.web.client.ResourceAccessException
          - java.net.SocketTimeoutException
          - java.io.IOException
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01-1: ErrorResponse expandido + CodigoCarrier interface em lib-shared</name>
  <files>
    lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java,
    lib-shared/src/main/java/br/com/erpkit/shared/exception/CodigoCarrier.java,
    lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java
  </files>
  <read_first>
    - lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java (arquivo a modificar — ver shape atual nos interfaces acima)
    - lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java (arquivo a modificar)
    - lib-shared/src/main/java/br/com/erpkit/shared/exception/ModuloException.java (super-classe — sem mudanca)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md (secao "lib-shared/.../ErrorResponse.java MODIFY" + "lib-shared/.../GlobalExceptionHandler.java opcional MODIFY" — pattern de propagacao)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-CONTEXT.md (D-02 — codigos sao "JANELA_24H_FECHADA", "META_ERROR", "META_INDISPONIVEL", "META_TIMEOUT", "CIRCUIT_OPEN")
  </read_first>
  <behavior>
    - Test: ErrorResponse com codigo=null + metaErrorCode=null serializa Jackson identico ao atual (regression test em lib-shared/src/test) — campos novos quando null nao quebram outros modulos
    - Test: ErrorResponse com codigo="JANELA_24H_FECHADA" + metaErrorCode=131026 serializa ambos os campos
    - Test: GlobalExceptionHandler.handleModuloException com excecao que NAO implementa CodigoCarrier retorna ErrorResponse SEM codigo (compatibilidade Phase 1-3)
    - Test: GlobalExceptionHandler.handleModuloException com excecao que implementa CodigoCarrier devolve ErrorResponse COM codigo + metaErrorCode populados
  </behavior>
  <action>
1. Criar `lib-shared/src/main/java/br/com/erpkit/shared/exception/CodigoCarrier.java`:
```java
package br.com.erpkit.shared.exception;

/**
 * Marker para excecoes ModuloException que carregam um codigo de erro estruturado
 * (ex: "JANELA_24H_FECHADA", "META_ERROR") e opcionalmente um metaErrorCode numerico
 * (codigo numerico do provedor — ex: Meta error 131026 invalid phone).
 *
 * <p><b>Por que em lib-shared, nao em api-whatsapp:</b> permite que
 * {@link GlobalExceptionHandler} propague codigo+metaErrorCode SEM importar
 * pacotes de api-whatsapp (preservando direcao de dependencia: lib-shared NAO
 * pode depender de api-*). Phase 4 (api-whatsapp) cria
 * JanelaConversaFechadaException + MetaApiException, ambas extends ModuloException
 * implements CodigoCarrier.
 */
public interface CodigoCarrier {
    String getCodigo();
    default Integer getMetaErrorCode() { return null; }
}
```

2. MODIFICAR `lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java` — adicionar 2 fields nullable + getters/setters APOS o field `campos` existente. NAO alterar ctor existente. Usar `@JsonInclude(JsonInclude.Include.NON_NULL)` na CLASSE para garantir que codigo/metaErrorCode null nao apareca no JSON (preserva backward-compat com api-email/api-storage/api-consultas que nunca setam):
```java
package br.com.erpkit.shared.dto;

import com.fasterxml.jackson.annotation.JsonInclude;

import java.time.LocalDateTime;
import java.util.Map;

@JsonInclude(JsonInclude.Include.NON_NULL)
public class ErrorResponse {
    private int status;
    private String erro;
    private String mensagem;
    private LocalDateTime timestamp;
    private Map<String, String> campos;
    private String codigo;            // NOVO Phase 4: estruturado (ex: JANELA_24H_FECHADA)
    private Integer metaErrorCode;    // NOVO Phase 4: codigo numerico do provedor (ex: 131026 Meta)
    // ... ctors + getters/setters EXISTENTES inalterados ...

    public String getCodigo() { return codigo; }
    public void setCodigo(String codigo) { this.codigo = codigo; }

    public Integer getMetaErrorCode() { return metaErrorCode; }
    public void setMetaErrorCode(Integer metaErrorCode) { this.metaErrorCode = metaErrorCode; }
}
```

3. MODIFICAR `lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java` — propagar codigo+metaErrorCode quando ex implements CodigoCarrier (D-02). NAO importar pacotes de api-whatsapp:
```java
@ExceptionHandler(ModuloException.class)
public ResponseEntity<ErrorResponse> handleModuloException(ModuloException ex) {
    ErrorResponse error = new ErrorResponse(
            ex.getStatus().value(),
            ex.getStatus().getReasonPhrase(),
            ex.getMessage()
    );
    if (ex instanceof CodigoCarrier carrier) {
        error.setCodigo(carrier.getCodigo());
        error.setMetaErrorCode(carrier.getMetaErrorCode());
    }
    return ResponseEntity.status(ex.getStatus()).body(error);
}
```

4. Criar `lib-shared/src/test/java/br/com/erpkit/shared/exception/GlobalExceptionHandlerCodigoCarrierTest.java` — test de regressao + propagacao:
```java
package br.com.erpkit.shared.exception;

import br.com.erpkit.shared.dto.ErrorResponse;
import org.junit.jupiter.api.Test;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;

import static org.assertj.core.api.Assertions.assertThat;

class GlobalExceptionHandlerCodigoCarrierTest {
    private final GlobalExceptionHandler handler = new GlobalExceptionHandler();

    @Test
    void modulo_exception_sem_codigo_carrier_devolve_response_compativel() {
        ResponseEntity<ErrorResponse> resp = handler.handleModuloException(
            new ModuloException("teste", HttpStatus.NOT_FOUND));
        assertThat(resp.getStatusCode()).isEqualTo(HttpStatus.NOT_FOUND);
        assertThat(resp.getBody().getCodigo()).isNull();
        assertThat(resp.getBody().getMetaErrorCode()).isNull();
    }

    @Test
    void modulo_exception_com_codigo_carrier_propaga_codigo_e_meta_error_code() {
        class FakeException extends ModuloException implements CodigoCarrier {
            FakeException() { super("janela", HttpStatus.CONFLICT); }
            @Override public String getCodigo() { return "JANELA_24H_FECHADA"; }
            @Override public Integer getMetaErrorCode() { return null; }
        }
        ResponseEntity<ErrorResponse> resp = handler.handleModuloException(new FakeException());
        assertThat(resp.getBody().getCodigo()).isEqualTo("JANELA_24H_FECHADA");
        assertThat(resp.getBody().getMetaErrorCode()).isNull();
    }

    @Test
    void modulo_exception_com_meta_error_code_propaga_ambos() {
        class MetaFake extends ModuloException implements CodigoCarrier {
            MetaFake() { super("meta 4xx", HttpStatus.UNPROCESSABLE_ENTITY); }
            @Override public String getCodigo() { return "META_ERROR"; }
            @Override public Integer getMetaErrorCode() { return 131026; }
        }
        ResponseEntity<ErrorResponse> resp = handler.handleModuloException(new MetaFake());
        assertThat(resp.getBody().getCodigo()).isEqualTo("META_ERROR");
        assertThat(resp.getBody().getMetaErrorCode()).isEqualTo(131026);
    }
}
```
  </action>
  <verify>
    <automated>./mvnw -pl lib-shared test -Dtest='GlobalExceptionHandlerCodigoCarrierTest'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'private String codigo' lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java` retorna 1
    - `grep -c 'private Integer metaErrorCode' lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java` retorna 1
    - `grep -c '@JsonInclude(JsonInclude.Include.NON_NULL)' lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java` retorna 1
    - `grep -c 'interface CodigoCarrier' lib-shared/src/main/java/br/com/erpkit/shared/exception/CodigoCarrier.java` retorna 1
    - `grep -c 'instanceof CodigoCarrier' lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java` retorna 1
    - `grep 'br.com.erpkit.whatsapp' lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java` retorna VAZIO (nao importar api-whatsapp em lib-shared)
    - `./mvnw -pl lib-shared test -Dtest='GlobalExceptionHandlerCodigoCarrierTest'` exits 0
    - `./mvnw verify` reator inteiro BUILD SUCCESS — zero regressao em api-email, api-storage, api-consultas (ErrorResponse mudanca compativel)
  </acceptance_criteria>
  <done>
    ErrorResponse expandido com 2 campos opcionais marcados @JsonInclude NON_NULL, CodigoCarrier interface criada em lib-shared, handler propaga via instanceof, 3 tests verdes em lib-shared, reator inteiro verde sem regressao em outros api-*.
  </done>
</task>

<task type="auto">
  <name>Task 04-01-2: Resilience4j instance whatsapp-cloud em application.yml + application-test.yml</name>
  <files>
    api-whatsapp/src/main/resources/application.yml,
    api-whatsapp/src/test/resources/application-test.yml
  </files>
  <read_first>
    - api-whatsapp/src/main/resources/application.yml (linhas 138-167 — bloco erp-callback ja existente; bloco whatsapp-cloud sera ADICIONADO; NAO MODIFICAR erp-callback existente)
    - api-whatsapp/src/test/resources/application-test.yml (linhas 90-110 — mesmo padrao)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md (secao "Pitfall 3: Resilience4j retry-exceptions whitelist incompleta — gotcha 03-04 reaproveitado")
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md (secoes "application.yml MODIFY" + "application-test.yml MODIFY" — config exata)
  </read_first>
  <action>
1. MODIFICAR `api-whatsapp/src/main/resources/application.yml` — ADICIONAR (sem remover erp-callback) instances `whatsapp-cloud` em ambos `circuitbreaker.instances` e `retry.instances`. Espelhar erp-callback EXATAMENTE (mesma config) — semantica Phase 4 e identica:
```yaml
resilience4j:
  circuitbreaker:
    instances:
      erp-callback:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 60s
        permitted-number-of-calls-in-half-open-state: 3
        register-health-indicator: false
      whatsapp-cloud:                                       # NOVO Phase 4 (per D-02 + RESEARCH §Pattern 1)
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 60s
        permitted-number-of-calls-in-half-open-state: 3
        register-health-indicator: false
  retry:
    instances:
      erp-callback:
        max-attempts: 3
        wait-duration: 1s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2.0
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - org.springframework.web.client.ResourceAccessException
          - java.net.SocketTimeoutException
          - java.io.IOException
      whatsapp-cloud:                                       # NOVO Phase 4
        max-attempts: 3
        wait-duration: 1s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2.0
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - org.springframework.web.client.ResourceAccessException     # CRUCIAL — gotcha 03-04
          - java.net.SocketTimeoutException
          - java.io.IOException
        # 4xx (HttpClientErrorException) NAO listado — Resilience4j default NAO retenta
        # excecoes nao listadas. ResourceAccessException CRUCIAL pois RestClient
        # empacota SocketTimeoutException nele (descoberto empiricamente em Phase 3 03-04).
```

2. MODIFICAR `api-whatsapp/src/test/resources/application-test.yml` — espelhar com `wait-duration: 50ms` para tests rapidos:
```yaml
resilience4j:
  circuitbreaker:
    instances:
      erp-callback:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 1s
        permitted-number-of-calls-in-half-open-state: 3
      whatsapp-cloud:                                       # NOVO Phase 4
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 1s
        permitted-number-of-calls-in-half-open-state: 3
  retry:
    instances:
      erp-callback:
        max-attempts: 3
        wait-duration: 50ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2.0
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - org.springframework.web.client.ResourceAccessException
          - java.net.SocketTimeoutException
          - java.io.IOException
      whatsapp-cloud:                                       # NOVO Phase 4
        max-attempts: 3
        wait-duration: 50ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2.0
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - org.springframework.web.client.ResourceAccessException
          - java.net.SocketTimeoutException
          - java.io.IOException
```

NAO criar bean — instance configurada em yml. Validacao em runtime via 04-04 quando WhatsAppCloudClient anotar `@CircuitBreaker(name="whatsapp-cloud")`.
  </action>
  <verify>
    <automated>./mvnw -pl api-whatsapp test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'whatsapp-cloud:' api-whatsapp/src/main/resources/application.yml` retorna 2 (1 em circuitbreaker.instances, 1 em retry.instances)
    - `grep -c 'whatsapp-cloud:' api-whatsapp/src/test/resources/application-test.yml` retorna 2
    - `grep -c 'erp-callback:' api-whatsapp/src/main/resources/application.yml` retorna 2 (NAO removido — backwards compat com Phase 3)
    - `grep -A 8 'whatsapp-cloud:' api-whatsapp/src/main/resources/application.yml | grep -c 'ResourceAccessException'` >= 1 (gotcha Phase 3 reaproveitado — RESEARCH §Pitfall 3)
    - `./mvnw -pl api-whatsapp test` BUILD SUCCESS — Phase 1-3 tests existentes (152 tests) verdes, zero regressao
  </acceptance_criteria>
  <done>
    Bloco whatsapp-cloud existe em ambos yml (prod + test), com retry-exceptions completo incluindo ResourceAccessException, sem remover bloco erp-callback, sem regressao em tests existentes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-01-3: Spike Wave 0 empirico — multipart upload Spring RestClient → WireMock (PITFALLS C-15)</name>
  <files>api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java</files>
  <read_first>
    - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/ErpCallbackClientTest.java (Phase 3 — pattern WireMock @SpringBootTest + @DynamicPropertySource + @BeforeEach resetAll — copiar layout)
    - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MetaMediaClientTest.java (Phase 3 — pattern Bearer header per-request + getAllServeEvents.forEach validar Bearer NUNCA em query — gate C-14)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/WhatsAppApplication.java (classes do @SpringBootTest)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md (secao "Pitfall 5: Multipart Content-Type boundary missing" + "Code Examples §2 Multipart Upload Media")
    - .planning/research/PITFALLS.md (C-14 Bearer leak via query param + C-15 multipart fields se existir)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md (snippet uploadMedia em "service/WhatsAppCloudClient.java")
  </read_first>
  <behavior>
    - Test: chama `restClient.post()` para WireMock stub `/test-phone-id/media` com `MultiValueMap` 3 fields (messaging_product=whatsapp, type=application/pdf, file=ByteArrayResource("hello".getBytes(),"test.pdf")) e `Bearer test-access-token` no header — WireMock retorna 200 com JSON `{"id":"meta-media-id-123"}`
    - Assert: response body contem id = "meta-media-id-123"
    - Assert: WireMock recebeu exatamente 1 request com Content-Type starting with "multipart/form-data; boundary=" (boundary auto-injetado)
    - Assert (gate PITFALLS C-15): WireMock recebeu request com `withRequestBodyPart` matching `messaging_product`, `type`, `file` — 3 fields obrigatorios
    - Assert (gate PITFALLS C-14): `wireMock.getAllServeEvents().forEach(event -> assertThat(event.getRequest().getUrl()).doesNotContain("access_token="))` — Bearer NUNCA em query
    - Assert: WireMock recebeu Authorization header com valor `Bearer test-access-token` (header per-request, nao query)
  </behavior>
  <action>
Criar `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java` — spike empirico Wave 0 que prova **antes de 04-04** que o pattern Spring RestClient + MultiValueMap + ByteArrayResource produz multipart valido conforme RESEARCH §Pattern Multipart, §Pitfall 5, e PITFALLS C-14/C-15. Sem este spike, 04-04 implementaria upload baseado em RESEARCH textual sem prova empirica de que Boot 3.5.9 + WireMock 3.10.0 + RestClient honram boundary auto + 3 fields.

Pattern espelha `ErpCallbackClientTest.java` (Phase 3) e `MetaMediaClientTest.java` (Phase 3) literalmente:

```java
package br.com.erpkit.whatsapp.spike;

import br.com.erpkit.whatsapp.WhatsAppApplication;
import com.github.tomakehurst.wiremock.WireMockServer;
import com.github.tomakehurst.wiremock.core.WireMockConfiguration;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.core.io.ByteArrayResource;
import org.springframework.http.HttpHeaders;
import org.springframework.http.MediaType;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.util.LinkedMultiValueMap;
import org.springframework.util.MultiValueMap;
import org.springframework.web.client.RestClient;

import java.util.Map;

import static com.github.tomakehurst.wiremock.client.WireMock.*;
import static org.assertj.core.api.Assertions.assertThat;

/**
 * Spike Wave 0 (Phase 4) — valida empiricamente que Spring RestClient + MultiValueMap +
 * ByteArrayResource + MediaType.MULTIPART_FORM_DATA produz multipart corretamente para
 * o endpoint Cloud API Meta /v22.0/{phoneNumberId}/media:
 * <ul>
 *   <li>Boundary auto-injetado (PITFALLS Pitfall 5 RESEARCH §Pattern Multipart)</li>
 *   <li>3 fields obrigatorios: messaging_product, type, file (PITFALLS C-15)</li>
 *   <li>Bearer NUNCA em query param — sempre Authorization header (PITFALLS C-14)</li>
 * </ul>
 *
 * <p>Sem este spike, 04-04 (WhatsAppCloudClient.uploadMedia) seria construido contra
 * pattern textual sem prova empirica em Boot 3.5.9 + WireMock 3.10.0. Mesmo padrao
 * de Phase 1 spike OnConflictSpikeTest (Wave 0 antes de Wave 1 de Plan 02-01).
 */
@SpringBootTest(classes = WhatsAppApplication.class)
@ActiveProfiles("test")
@DisplayName("Spike Wave 0 — multipart Cloud API upload via Spring RestClient")
class MultipartUploadSpikeTest {

    private static WireMockServer wireMock;

    @BeforeAll
    static void startWireMock() {
        wireMock = new WireMockServer(WireMockConfiguration.options().dynamicPort());
        wireMock.start();
    }

    @AfterAll
    static void stopWireMock() {
        if (wireMock != null) wireMock.stop();
    }

    @BeforeEach
    void resetWireMock() {
        wireMock.resetAll();
    }

    @Test
    @DisplayName("upload multipart envia 3 fields obrigatorios + Bearer header (sem query)")
    void upload_multipart_envia_3_fields_e_bearer_header_sem_query() throws Exception {
        // Stub: Meta retorna media_id
        wireMock.stubFor(post(urlPathEqualTo("/test-phone-id/media"))
            .willReturn(aResponse()
                .withStatus(200)
                .withHeader("Content-Type", "application/json")
                .withBody("{\"id\":\"meta-media-id-123\"}")));

        // Construcao do multipart per RESEARCH §Code Examples §2
        byte[] bytes = "hello pdf bytes".getBytes();
        ByteArrayResource fileResource = new ByteArrayResource(bytes) {
            @Override public String getFilename() { return "test.pdf"; }
        };
        MultiValueMap<String, Object> parts = new LinkedMultiValueMap<>();
        parts.add("messaging_product", "whatsapp");
        parts.add("type", "application/pdf");
        parts.add("file", fileResource);

        RestClient restClient = RestClient.builder()
            .baseUrl(wireMock.baseUrl())
            .build();

        Map response = restClient.post()
            .uri("/{phoneNumberId}/media", "test-phone-id")
            .header(HttpHeaders.AUTHORIZATION, "Bearer test-access-token")
            .contentType(MediaType.MULTIPART_FORM_DATA)
            .body(parts)
            .retrieve()
            .body(Map.class);

        // ASSERTION 1: response shape correto
        assertThat(response).isNotNull();
        assertThat(response.get("id")).isEqualTo("meta-media-id-123");

        // ASSERTION 2: WireMock recebeu request com Content-Type multipart com boundary
        wireMock.verify(1, postRequestedFor(urlPathEqualTo("/test-phone-id/media"))
            .withHeader("Content-Type", matching("multipart/form-data;.*boundary=.*")));

        // ASSERTION 3 (PITFALLS C-15): 3 fields obrigatorios presentes
        wireMock.verify(1, postRequestedFor(urlPathEqualTo("/test-phone-id/media"))
            .withRequestBodyPart(aMultipart()
                .withName("messaging_product")
                .withBody(equalTo("whatsapp"))
                .build())
            .withRequestBodyPart(aMultipart()
                .withName("type")
                .withBody(equalTo("application/pdf"))
                .build())
            .withRequestBodyPart(aMultipart()
                .withName("file")
                .build()));

        // ASSERTION 4 (PITFALLS C-14): Bearer header presente, sem query param access_token=
        wireMock.verify(1, postRequestedFor(urlPathEqualTo("/test-phone-id/media"))
            .withHeader("Authorization", equalTo("Bearer test-access-token")));
        wireMock.getAllServeEvents().forEach(event ->
            assertThat(event.getRequest().getUrl())
                .as("Bearer NUNCA em query param (PITFALLS C-14)")
                .doesNotContain("access_token="));
    }
}
```
  </action>
  <verify>
    <automated>./mvnw -pl api-whatsapp test -Dtest='MultipartUploadSpikeTest'</automated>
  </verify>
  <acceptance_criteria>
    - File `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java` existe
    - `grep -c '@SpringBootTest(classes = WhatsAppApplication.class)' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java` retorna 1
    - `grep -c 'getAllServeEvents().forEach' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java` retorna 1 (gate C-14)
    - `grep -c 'aMultipart()' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java` >= 3 (3 fields C-15)
    - `./mvnw -pl api-whatsapp test -Dtest='MultipartUploadSpikeTest'` exits 0 — Tests run: 1, Failures: 0, Errors: 0
    - Tempo execucao do spike < 10s (single test, WireMock cold + 1 verify)
  </acceptance_criteria>
  <done>
    Spike empirico verde provando que pattern multipart RestClient → WireMock funciona em Boot 3.5.9 com WireMock 3.10.0: boundary auto, 3 fields obrigatorios, Bearer header per-request sem query. Bloqueador documentado em STATE.md "Phase 4 (outbound + media): confirmar field names do multipart Meta /media upload" RESOLVED empiricamente. 04-04 pode prosseguir com pattern validado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ERP -> api-whatsapp | API key ApiKeyFilter (Phase 1); JSON valid via Jakarta (Phase 4 controller) |
| api-whatsapp -> Meta Cloud API | Bearer header per-request; nunca query param (PITFALLS C-14) |
| lib-shared -> api-* | direcao de dependencia: api-* -> lib-shared (NUNCA inversa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I (Information disclosure) | ErrorResponse JSON | mitigate | `@JsonInclude(NON_NULL)` em ErrorResponse — campos codigo/metaErrorCode null sao OMITIDOS, garantindo backward compat com api-email/api-storage/api-consultas (que nunca setam) e nao expoe shape interno desnecessariamente |
| T-04-01-02 | T (Tampering) | application.yml resilience4j config | accept | Config local on-premise, nao acessivel via rede; alteracao requer acesso ao filesystem do servidor — ja coberto por trust boundary do OS Windows (RUNBOOK.md Phase 6) |
| T-04-01-03 | I (Information disclosure) | MultipartUploadSpikeTest WireMock | mitigate | Spike usa `test-access-token` (dummy), NUNCA token real Meta. WireMock baseUrl loopback (localhost dynamicPort), nao expoe externamente |
| T-04-01-04 | I (C-14 Bearer leak) | RestClient outbound calls | mitigate | Gate empirico no spike `wireMock.getAllServeEvents().forEach(event -> assertThat(event.getRequest().getUrl()).doesNotContain("access_token="))` — regression test enforcing C-14 PERMANENTLY |
| T-04-01-05 | T (Tampering) | CodigoCarrier interface | accept | Interface publica em lib-shared; qualquer ModuloException pode implementar — risco baixo (codigo e String constante por subclasse, nao input externo) |
| T-04-01-06 | C-15 (Multipart fields invalidos) | Spring RestClient multipart | mitigate | Spike valida 3 fields obrigatorios (`messaging_product`, `type`, `file`) via `withRequestBodyPart(aMultipart().withName(...))` — assertion forca regressao caso refactor Spring quebre boundary auto-injection |

## V5 Input Validation
- ErrorResponse fields nullable — sem validation Jakarta (nao e request body)
- Spike usa fixtures hardcoded (sem input externo)

## V7 Error Handling
- GlobalExceptionHandler propaga codigo+metaErrorCode estruturados (D-02), sem stack trace inteira no body — apenas message do ModuloException

## V12 File size limit
- Spike usa byte[] pequeno (15 bytes); validacao `@Size(max=18MB)` em DTOs vira em 04-05

## ASVS L1 mappings
- V5.1.5 (Verify input validation failures result in rejection): nao aplicavel (sem input request neste plano)
- V7.4.1 (Verify a generic message is shown when an unexpected error occurs): GlobalExceptionHandler.handleGeneric ja existente — Phase 4 nao altera
</threat_model>

<verification>
- ErrorResponse expandido sem quebrar api-email/api-storage/api-consultas (reator inteiro verde)
- CodigoCarrier interface em lib-shared sem importar pacotes de api-*
- Bloco whatsapp-cloud presente em ambos yml com retry-exceptions completo
- Spike multipart upload verde em <10s — bloqueador "multipart fields" RESOLVED empiricamente
- Test isolado: `./mvnw -pl lib-shared test -Dtest='GlobalExceptionHandlerCodigoCarrierTest'` verde
- Test isolado: `./mvnw -pl api-whatsapp test -Dtest='MultipartUploadSpikeTest'` verde
- Reator completo: `./mvnw verify` BUILD SUCCESS — 152 tests Phase 1-3 + 4 novos = 156 tests verdes
</verification>

<success_criteria>
- [ ] `lib-shared/src/main/java/br/com/erpkit/shared/dto/ErrorResponse.java` tem campos `codigo` (String) + `metaErrorCode` (Integer) nullable, classe anotada `@JsonInclude(NON_NULL)`
- [ ] `lib-shared/src/main/java/br/com/erpkit/shared/exception/CodigoCarrier.java` interface criada
- [ ] `lib-shared/src/main/java/br/com/erpkit/shared/exception/GlobalExceptionHandler.java` propaga codigo+metaErrorCode quando ex implements CodigoCarrier
- [ ] `lib-shared/.../GlobalExceptionHandler.java` NAO importa pacotes `br.com.erpkit.whatsapp.*` (preserva direcao de dependencia)
- [ ] `lib-shared/src/test/java/br/com/erpkit/shared/exception/GlobalExceptionHandlerCodigoCarrierTest.java` 3 tests verdes
- [ ] `api-whatsapp/src/main/resources/application.yml` tem instance whatsapp-cloud em CB + Retry com retry-exceptions completo (4 entries incluindo ResourceAccessException)
- [ ] `api-whatsapp/src/test/resources/application-test.yml` mesmo bloco com wait-duration: 50ms
- [ ] `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/spike/MultipartUploadSpikeTest.java` 1 test verde com 4 assertions (id retornado + boundary multipart + 3 fields + Bearer header sem query)
- [ ] Reator inteiro `./mvnw verify` BUILD SUCCESS — 152 tests Phase 1-3 + 3 lib-shared + 1 spike = 156+ tests verdes, zero regressao
</success_criteria>

<output>
After completion, create `.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-01-SUMMARY.md` documentando:
- Files created/modified
- Test counts (before/after)
- Spike resolution: bloqueador "multipart Meta fields" RESOLVED empiricamente
- D-02 partial: ErrorResponse infrastructure pronta para JanelaConversaFechadaException + MetaApiException (04-02 + 04-04 implementam CodigoCarrier)
- Issues/concerns para Wave 1 (04-02, 04-03, 04-04 podem rodar em paralelo)
</output>
