---
phase: 04-outbound-trava-24h-whatsappcontroller
plan: 06
type: execute
wave: 4
depends_on: [04-05]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
  - OUT-05
  - OUT-06
  - OUT-07
  - OUT-08
  - OUT-09
  - OUT-10
  - OUT-11
tags: [whatsapp, closeout, smoke-e2e, roadmap, gates-grep, summary]
must_haves:
  truths:
    - "Reator inteiro `./mvnw verify` BUILD SUCCESS — todos os 7 modulos verdes (api-email + api-storage + api-consultas + api-whatsapp + lib-shared + lib-consultas-client + root) com Phase 1-4 152+30 tests = 180+ verdes, zero regressao"
    - "Grep gate empirico SC-1 + SC-4c executados: WhatsAppCloudClient.java 0 hits para `enviarTemplate` ou `\"template\"` em codigo executavel (excluindo Javadoc); 0 hits para `access_token=` em qualquer arquivo Java em src/main"
    - "ROADMAP.md Phase 4 marcada [x] complete com count `Plans: 6 plans` + cada `04-NN-PLAN.md — {objective}` listado em ordem"
    - "REQUIREMENTS.md tabela traceability OUT-01..11 11 reqs marcadas Complete + Phase 4"
    - "STATE.md atualizado com Phase 4 closeout — completed_phases=4, plans=20+6=26 (Phase 1-3 = 20; Phase 4 = 6), velocity table com Phase 4 row"
    - "5/5 ROADMAP success criteria empiricamente cobertos por tests Phase 4 (mapping documentado em SUMMARY)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 [x] complete + Plans 6 + lista de 6 plans"
      contains: "Phase 4: Outbound + Trava 24h + WhatsAppController"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OUT-01..11 marcadas Complete na tabela traceability"
      contains: "OUT-01 | Phase 4 | Complete"
    - path: ".planning/STATE.md"
      provides: "Phase 4 closeout, performance metrics atualizadas"
      contains: "Phase 4 (outbound-trava-24h-whatsappcontroller) — COMPLETE"
    - path: ".planning/phases/04-outbound-trava-24h-whatsappcontroller/04-06-SUMMARY.md"
      provides: "Closeout SUMMARY consolidando Phase 4: 6 plans, ~30 tests novos, 5/5 SC + 11/11 reqs verdes"
      contains: "Phase 4 100% completa"
  key_links:
    - from: "Reator inteiro mvnw verify"
      to: "Zero regressao em api-email/api-storage/api-consultas (ErrorResponse mudanca compativel)"
      via: "@JsonInclude(NON_NULL) garante backward compat"
      pattern: "BUILD SUCCESS"
    - from: "Grep gate enviarTemplate"
      to: "OUT-05 trava custo zero #1"
      via: "WhatsAppCloudClient.java codigo executavel sem hits"
      pattern: "enviarTemplate"
    - from: "Grep gate access_token"
      to: "PITFALLS C-14 Bearer per-request"
      via: "src/main/java sem matches"
      pattern: "access_token"
---

<objective>
Closeout da Phase 4: validar empiricamente que toda a entrega das 5 plans anteriores (04-01..04-05) compoe um sistema funcional via reator inteiro `./mvnw verify`, executar gates de grep que provam OUT-05 + PITFALLS C-14 estatica e dinamicamente, atualizar ROADMAP/REQUIREMENTS/STATE para refletir Phase 4 fechada, e produzir 04-06-SUMMARY consolidando o trabalho da phase para handoff ao gsd-verify-phase.

Purpose: Garantir que nao ha regressao silenciosa em outros api-* modulos apos modificacao do `ErrorResponse` (lib-shared e shared dependency); confirmar que os 2 gates arquiteturais centrais da phase (sem `enviarTemplate` em codigo + sem Bearer em query param) sao realmente impossiveis de regredir; e marcar Phase 4 como pronta para `/gsd-verify-phase` validar com 5/5 SC + 11/11 reqs verdes.

Output: 1 commit final consolidando Phase 4; ROADMAP/REQUIREMENTS/STATE com state correto; SUMMARY rico documentando metricas (testes, files, decisions implementadas, issues/concerns); checkpoint human-verify antes do verifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-CONTEXT.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-VALIDATION.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-01-SUMMARY.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-02-SUMMARY.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-03-SUMMARY.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-04-SUMMARY.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 04-06-1: Reator E2E smoke + grep gates SC-1 (no template) + SC-4c (no access_token query)</name>
  <files>
    .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-06-SUMMARY.md
  </files>
  <read_first>
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java (04-04 — verificar)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-VALIDATION.md (Test Map)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-04-SUMMARY.md (test counts)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-05-SUMMARY.md (test counts)
  </read_first>
  <action>
1. **Reator inteiro smoke E2E:** Executar `./mvnw verify` na raiz do projeto. Validar:
   - BUILD SUCCESS
   - Todos os 7 modulos compilam: lib-shared, api-email, api-storage, api-consultas, lib-consultas-client, api-whatsapp, root
   - Sumario por modulo no final do log: "Tests run: N, Failures: 0, Errors: 0"
   - Esperado: ~180 tests verdes (152 Phase 1-3 + ~30 Phase 4 distribuidos: 04-01 ~4, 04-02 ~6, 04-03 ~4, 04-04 ~13, 04-05 ~13)

2. **Grep gate SC-1 (OUT-05 trava custo zero #1):**
```bash
# Excluir Javadoc (linhas iniciando com * em block comment) — match apenas codigo executavel
grep -n -v '^\s*\*' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java \
  | grep -v '^\s*//' \
  | grep -E 'enviarTemplate|"template"' \
  | wc -l
# ESPERADO: 0
```
Documentar comando + output em SUMMARY. Se >0 (incluiria casos legitimos como `"interactive_button"` ou similar), inspecionar manualmente — apenas hits para `enviarTemplate` (metodo) ou string literal `"template"` em payload Cloud API sao bug.

3. **Grep gate SC-4c (PITFALLS C-14 Bearer NUNCA em query):**
```bash
grep -rn 'access_token=' api-whatsapp/src/main/java | wc -l
# ESPERADO: 0
```
Documentar.

4. **Grep gate D-03 — todos os 4 metodos publicos do WhatsAppCloudClient anotados @JanelaProtegida:**
```bash
grep -c '@JanelaProtegida' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java
# ESPERADO: 4
```

5. **Grep gate fallbackMethod NO @Retry (gotcha 03-04 reaproveitado em Phase 4):**
```bash
grep -c 'fallbackMethod = "fallbackEnviar' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java
# ESPERADO: 4 (todos no @Retry, NAO no @CircuitBreaker)
```

6. **Criar 04-06-SUMMARY.md** consolidando Phase 4. Estrutura:
```markdown
# Phase 4: Outbound + Trava 24h + WhatsAppController — Closeout SUMMARY

**Completed:** 2026-05-05 (or actual date)
**Plans:** 6 / 6 complete
**Tests:** ~180 verdes (152 Phase 1-3 + ~30 Phase 4)
**Reator:** BUILD SUCCESS

## Plans Delivered

| Plan | Files | Tests | Time | Decisions Implemented |
|------|-------|-------|------|----------------------|
| 04-01 | 5 (lib-shared ErrorResponse + CodigoCarrier + handler + 2 yml + spike) | 4 | ~Xmin | Habilita D-02; valida multipart C-15 |
| 04-02 | 7 (repo+exception+service+annotation+aspect+2 tests) | 6 | ~Xmin | D-03 trava 24h aspect HIGHEST_PRECEDENCE |
| 04-03 | 2 (service + test) | 4 | ~Xmin | D-04 TTL estrito 30d |
| 04-04 | 7 (cliente+exception+4 DTOs+test) | 13 | ~Xmin | D-02 mapping + OUT-05 trava custo zero |
| 04-05 | 7 (5 DTOs+controller+test) | 13 | ~Xmin | D-01 base64 + D-04 status minimal |
| 04-06 | 1 (SUMMARY + ROADMAP/REQS/STATE) | 0 | ~Xmin | Closeout |

## Success Criteria Mapping (5/5)

| ROADMAP SC | Implementing Test | Status |
|-----------|-------------------|--------|
| SC-1: WhatsAppCloudClient sem `enviarTemplate` | WhatsAppCloudClientTest.metodos_publicos_nao_inclui_template (reflection) + grep gate | green |
| SC-2: Janela 24h hard 409 | WindowEnforcementServiceTest (3 tests) + JanelaEnforcementAspectTest (counter==1 prova HIGHEST_PRECEDENCE) + WhatsAppControllerTest.janela_fechada_retorna_409 | green |
| SC-3: MediaCacheService TTL + reupload | MediaCacheServiceTest (4 tests: hit/miss/expirado/race) + WhatsAppCloudClientTest.enviarDocumento_cache_hit_pula_upload | green |
| SC-4: 4xx no retry + 5xx retry + Bearer nao em log | WhatsAppCloudClientTest.quatrocentos_no_retry + cinquecentos_recupera_counter_3 + bearer_nunca_em_query_param | green |
| SC-5: Outbound persistido direcao=out + 200 com wamid | WhatsAppCloudClientTest.enviarTexto_happy_path_persiste + WhatsAppControllerTest.enviar_texto_happy_200 | green |

## Requirements Coverage (11/11)

| Req | Plan | Status |
|-----|------|--------|
| OUT-01 enviarTexto | 04-04 | Complete |
| OUT-02 enviarDocumento + uploadMedia | 04-04 | Complete |
| OUT-03 enviarBotoes max 3 | 04-04 + 04-05 | Complete |
| OUT-04 enviarLista max 10 itens | 04-04 + 04-05 | Complete |
| OUT-05 sem enviarTemplate | 04-04 | Complete (gate dual: reflection test + grep) |
| OUT-06 WindowEnforcementService | 04-02 | Complete |
| OUT-07 aspect interceptor | 04-02 | Complete (counter==1 empirico) |
| OUT-08 MediaCacheService | 04-03 | Complete |
| OUT-09 mensagens_log direcao=out | 04-04 | Complete |
| OUT-10 4xx no retry / 5xx retry | 04-04 (yml + classificar) | Complete |
| OUT-11 5 endpoints REST | 04-05 | Complete |

## Decisions Implemented

- **D-01 (JSON+base64 entre ERP e api-whatsapp):** EnviarDocumentoRequest record com mediaBase64 String + @Size(max=18MB); controller decodifica + try/catch IllegalArgumentException → 400
- **D-02 (traducao Meta → ERP com codigo):** ErrorResponse expandido + CodigoCarrier interface (lib-shared 04-01); MetaApiException com 4 tipos enum HTTP+codigo; classificar(Throwable) cobre 4 categorias
- **D-03 (annotation marker @JanelaProtegida + telefone args[0] + HIGHEST_PRECEDENCE):** annotation marker + aspect; counter==1 em 3 retries valida empiricamente
- **D-04 (MediaCache TTL estrito 30d + status minimal):** Duration.ofDays(30) sem sliding; StatusResponse minimal (status + cb state + phoneNumberId)

## Empirical Validations (gotchas resolvidos)

- **PITFALLS C-15 (multipart 3 fields):** spike Wave 0 04-01 + test upload_media_envia_3_fields_obrigatorios em 04-04 → boundary auto + 3 fields garantidos via WireMock withRequestBodyPart
- **PITFALLS C-14 (Bearer NUNCA em query):** getAllServeEvents().forEach gate em 04-04; grep gate em 04-06; Bearer per-request explicito em 5 calls
- **Gotcha 03-04 (fallbackMethod no @Retry, nao @CircuitBreaker):** counter==3 em 5xx retry valida; 4 fallbacks no @Retry
- **Aspect order HIGHEST_PRECEDENCE:** counter==1 em 3 retries no JanelaEnforcementAspectTest

## Files Inventory

### main/
- lib-shared: ErrorResponse (mod), CodigoCarrier (new), GlobalExceptionHandler (mod)
- api-whatsapp:
  - controller: WhatsAppController (new)
  - service: WhatsAppCloudClient (new), MediaCacheService (new), WindowEnforcementService (new)
  - aspect: JanelaProtegida (new), JanelaEnforcementAspect (new)
  - exception: JanelaConversaFechadaException (new), MetaApiException (new)
  - dto: EnvioResponse + StatusResponse + EnviarTextoRequest + EnviarDocumentoRequest + EnviarBotoesRequest + EnviarListaRequest + BotaoDto + ItemDto + SecaoDto (9 new)
  - repository: ClienteZapRepository (mod — adicionado buscarUltimaMensagemEm)
  - resources: application.yml + application-test.yml (mod — bloco whatsapp-cloud)

### test/
- lib-shared: GlobalExceptionHandlerCodigoCarrierTest (new)
- api-whatsapp:
  - spike: MultipartUploadSpikeTest (new)
  - service: WhatsAppCloudClientTest, MediaCacheServiceTest, WindowEnforcementServiceTest (new)
  - aspect: JanelaEnforcementAspectTest (new)
  - controller: WhatsAppControllerTest (new)

## Issues / Concerns

- **Phase 5 dependency:** lib-whatsapp-client (LIB-01..08) consumira contratos REST do controller — DTOs request estaveis. Phase 5 NAO deve quebrar contrato.
- **Phase 6 dependency:**
  - SpringDoc OpenAPI fica em Phase 6 (QA-05) — Phase 4 nao adiciona @Operation/@Schema explicito (fica para Phase 6 cobrir todos os 5 endpoints)
  - subscribed_apps validation no /status (PITFALLS C-12) — Phase 6 expansao
- **Spring servlet multipart:** default 10MB pode precisar override em prod se piloto MUDAS enviar PDFs >15MB (RUNBOOK Phase 6 documenta `spring.servlet.multipart.max-request-size: 20MB`)
- **Risk A3 (CB shared singleton):** mitigado em todos os tests via cbRegistry.find().reset() @BeforeEach
- **Sem regressao detectada** em api-email/api-storage/api-consultas apos modificacao do ErrorResponse — `@JsonInclude(NON_NULL)` garantiu backward compat

## Pronto Para

`/gsd-verify-phase 4-outbound-trava-24h-whatsappcontroller` — todos os 5 SC empiricamente validados, todos os 11 reqs com test correspondente, reator inteiro verde, gates de regressao em codigo (grep + reflection) garantem OUT-05 + PITFALLS C-14 nao podem regredir silenciosamente.
```

Salvar em `.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-06-SUMMARY.md`.
  </action>
  <verify>
    <automated>./mvnw verify</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw verify` (raiz) BUILD SUCCESS — 7 modulos verdes
    - `find . -name "*.java" -path "*/api-whatsapp/src/main*" -exec grep -l 'access_token=' {} +` retorna VAZIO (gate C-14)
    - `grep -E 'enviarTemplate|"template"' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java | grep -v '^\s*\*' | grep -v '^\s*//'` retorna VAZIO (gate OUT-05)
    - `grep -c '@JanelaProtegida' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java` retorna 4
    - `grep -c 'fallbackMethod = "fallbackEnviar' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java` retorna 4
    - File `.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-06-SUMMARY.md` existe
    - SUMMARY contem secoes: "Plans Delivered" + "Success Criteria Mapping (5/5)" + "Requirements Coverage (11/11)" + "Decisions Implemented" + "Empirical Validations"
    - Test count total >= 180 (152 + ~30) — verificar no log do `mvnw verify`
  </acceptance_criteria>
  <done>
    Reator inteiro verde, 4 grep gates passando (sem enviarTemplate em codigo executavel + sem access_token em src/main + 4 @JanelaProtegida + 4 fallbackMethod no @Retry). SUMMARY rico documentando 5/5 SC + 11/11 reqs + 4 decisoes + gotchas resolvidos.
  </done>
</task>

<task type="auto">
  <name>Task 04-06-2: Update ROADMAP.md + REQUIREMENTS.md + STATE.md (Phase 4 closeout)</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (atual — Phase 4 com `Plans: TBD` + nao [x])
    - .planning/REQUIREMENTS.md (atual — OUT-01..11 marcadas Pending na tabela traceability)
    - .planning/STATE.md (atual — frontmatter com completed_phases: 3)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-06-SUMMARY.md (recem criado)
  </read_first>
  <action>
1. **MODIFICAR `.planning/ROADMAP.md`:**

   - Linha `- [ ] **Phase 4: Outbound...**` → `- [x] **Phase 4: Outbound...**`
   - Em "Phase Details" → "### Phase 4", linha `**Plans**: TBD` → `**Plans**: 6 plans`
   - APOS `**Plans**: 6 plans` adicionar lista (mesmo padrao de Phase 3):
     ```
       - [x] 04-01-PLAN.md — lib-shared ErrorResponse + CodigoCarrier + bloco resilience4j whatsapp-cloud + spike multipart
       - [x] 04-02-PLAN.md — JanelaEnforcementAspect (HIGHEST_PRECEDENCE) + WindowEnforcementService + JanelaConversaFechadaException
       - [x] 04-03-PLAN.md — MediaCacheService (TTL estrito 30d, race save+catch)
       - [x] 04-04-PLAN.md — WhatsAppCloudClient (4 envios + uploadMedia) + MetaApiException + WhatsAppCloudClientTest WireMock
       - [x] 04-05-PLAN.md — WhatsAppController + 5 DTOs request + StatusResponse + WhatsAppControllerTest @WebMvcTest
       - [x] 04-06-PLAN.md — Reator E2E smoke + grep gates SC-1/SC-4c + ROADMAP/REQUIREMENTS/STATE closeout
     ```
   - Em tabela "Progress" linha `| 4. Outbound...` → `| 4. Outbound + Trava 24h + WhatsAppController | 6/6 | Complete (awaiting verifier) | YYYY-MM-DD |`

2. **MODIFICAR `.planning/REQUIREMENTS.md`:**

   - Tabela Traceability: trocar todas as linhas `| OUT-01..11 | Phase 4 | Pending |` para `| OUT-01..11 | Phase 4 | Complete |` (11 linhas)

3. **MODIFICAR `.planning/STATE.md`:**

   Frontmatter:
   - `status: Phase 03 closeout — ...` → `status: Phase 04 closeout — 6/6 plans + 5/5 ROADMAP SC + 11/11 OUT reqs verdes; trava custo zero arquitetural validada via grep+reflection gates`
   - `stopped_at: Phase 4 contexto capturado...` → `stopped_at: Phase 04 closeout — pronto para gsd-verify-phase`
   - `completed_phases: 3` → `completed_phases: 4`
   - `total_plans: 20` → `total_plans: 26`
   - `completed_plans: 20` → `completed_plans: 26`
   - `last_activity:` atualizar com Phase 4 closeout

   "Current Position":
   - `Phase: 03 (roteamento-boundary-async) — COMPLETE` → `Phase: 04 (outbound-trava-24h-whatsappcontroller) — COMPLETE (awaiting verifier)`
   - `Plan: 6 of 6 COMPLETE` → `Plan: 6 of 6 COMPLETE`
   - Status linha similar atualizada

   "Performance Metrics" → "By Phase" tabela: adicionar linha `| 04 | 6/6 | ~XX min | ~XX min |` (preencher com tempos reais agregados ou estimados)

   "Performance Metrics" → "Recent Trend": atualizar para refletir 04-NN plans

   "Performance Metrics" tabela detalhada: adicionar 6 linhas para 04-01..06 com plan duration e file/test counts (preencher do SUMMARY)

   "Decisions": adicionar bloco com decisoes Phase 4:
   - [04-01]: ErrorResponse @JsonInclude(NON_NULL) garante backward compat com api-email/api-storage/api-consultas
   - [04-01]: CodigoCarrier interface em lib-shared evita acoplamento ascendente lib-shared → api-whatsapp
   - [04-02]: @Order(HIGHEST_PRECEDENCE) validado empiricamente via counter==1 em 3 retries (Pitfall 1 RESEARCH RESOLVED)
   - [04-03]: TTL estrito 30d sem sliding — turnover natural, tabela bounded
   - [04-04]: Reflection test metodos_publicos_nao_inclui_template + grep gate dual mitigation OUT-05
   - [04-04]: Multipart spike Wave 0 (04-01) provou boundary auto + 3 fields ANTES de 04-04 implementar
   - [04-05]: @AssertTrue isTotalItensValido cross-secao em record + @JsonIgnore impede field aparecer em JSON

   "Blockers/Concerns": atualizar — confirmar field names multipart RESOLVED via spike Wave 0; remover linha sobre WireMock 3.10.0 (RESOLVIDO Phase 3 — manter so se existir nova concern Phase 5+)

   "Session Continuity": atualizar com timestamp + resume file = 04-06-SUMMARY.md
  </action>
  <verify>
    <automated>./mvnw -pl lib-shared,api-whatsapp test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*Phase 4:' .planning/ROADMAP.md` retorna 1
    - `grep -c '04-01-PLAN.md\|04-06-PLAN.md' .planning/ROADMAP.md` >= 2 (lista de plans em "Phase Details")
    - `grep -c '| 4\. Outbound' .planning/ROADMAP.md` >= 1 (linha tabela Progress)
    - `grep -c '6/6' .planning/ROADMAP.md` >= 2 (Phase 1, 3 ja tem 6/6 ou 7/7; Phase 4 deve aparecer)
    - `grep -E 'OUT-01.*Complete|OUT-11.*Complete' .planning/REQUIREMENTS.md` retorna >= 2 linhas (11 esperadas)
    - `grep -c '| OUT-.*| Phase 4 | Pending' .planning/REQUIREMENTS.md` retorna 0 (todos Pending viraram Complete)
    - `grep -c 'completed_phases: 4' .planning/STATE.md` retorna 1
    - `grep -c 'total_plans: 26' .planning/STATE.md` retorna 1
    - `grep -c 'Phase 04' .planning/STATE.md` >= 2 (status + Current Position)
    - `grep -c '04-01\|04-02\|04-03\|04-04\|04-05\|04-06' .planning/STATE.md` >= 6 (Decisions block)
  </acceptance_criteria>
  <done>
    ROADMAP marca Phase 4 [x] complete + lista 6 plans, REQUIREMENTS.md tem 11 OUT marcadas Complete na tabela, STATE.md frontmatter com completed_phases=4 + total_plans=26 + Decisions Phase 4 documentadas + Performance Metrics atualizadas. Test smoke (lib-shared + api-whatsapp) ainda verde, zero regressao.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-06-3: Human verify Phase 4 closeout antes do gsd-verify-phase</name>
  <what-built>
    Phase 4 inteira:
    - 6 plans entregues (04-01 a 04-06)
    - ~30 tests novos verdes em <2min total
    - Reator inteiro `./mvnw verify` BUILD SUCCESS (180+ tests across 7 modulos)
    - 4 decisoes locked (D-01..D-04) implementadas conforme CONTEXT.md
    - 11 OUT requirements satisfeitos com test correspondente
    - 5 ROADMAP SC empiricamente validados
    - 4 grep gates passando (no enviarTemplate, no access_token, 4 @JanelaProtegida, 4 fallbackMethod)
    - ROADMAP marcada [x] complete + 6/6 plans listados
    - REQUIREMENTS.md OUT-01..11 marcadas Complete
    - STATE.md frontmatter atualizado (completed_phases=4, total_plans=26)
    - 6 SUMMARY files documentando cada plan + 1 consolidando Phase 4
  </what-built>
  <how-to-verify>
    1. **Reator inteiro verde:**
       ```bash
       ./mvnw verify | tail -30
       ```
       Esperado: `BUILD SUCCESS` + 7 modulos com `Tests run: N, Failures: 0, Errors: 0`

    2. **Phase 4 specific:**
       ```bash
       ./mvnw -pl api-whatsapp test 2>&1 | grep -E 'Tests run|ERROR|FAILED'
       ```
       Esperado: ~70-80 tests run (Phase 1+2+3 com ~150 originais + Phase 4 com ~30)

    3. **Grep gates manualmente:**
       - `grep -E 'enviarTemplate|"template"' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java | grep -v '^\s*\*' | grep -v '^\s*//'` → vazio
       - `grep -rn 'access_token=' api-whatsapp/src/main/java` → vazio
       - `grep -c '@JanelaProtegida' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java` → 4

    4. **Smoke endpoint manual (opcional):**
       Iniciar `mvnw -pl api-whatsapp spring-boot:run` (com env vars dummy) e curl:
       - `curl -X GET http://localhost:9193/api/whatsapp/status -H "X-API-Key: test-key"` → 200 com JSON contendo `circuitBreakerState`
       - `curl -X POST http://localhost:9193/api/whatsapp/enviar-texto -H "X-API-Key: test-key" -H "Content-Type: application/json" -d '{"telefone":"","texto":"x"}'` → 400 com campos.telefone

    5. **Confirmar artefatos de planning:**
       - `cat .planning/ROADMAP.md | grep -A 2 'Phase 4:'` → marca [x] complete
       - `cat .planning/STATE.md | head -20` → completed_phases: 4

    6. **Read SUMMARY files** em `.planning/phases/04-outbound-trava-24h-whatsappcontroller/`:
       - 04-01-SUMMARY.md ate 04-06-SUMMARY.md devem existir e descrever entrega de cada plan
  </how-to-verify>
  <resume-signal>
    Type "approved" para confirmar Phase 4 pronta para `/gsd-verify-phase`.
    Type "issue: <descricao>" se algum gate ou test falhar — vai dar trigger em loop de fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reator E2E build -> ambient artifacts | Confidence em test counts + BUILD SUCCESS antes de marcar Phase 4 complete |
| Grep gates -> codigo executavel | gates estaticos garantem regressao impossivel sem refactor consciente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | T (Falso closeout — marca complete sem rodar verify) | mvnw verify | mitigate | Acceptance criteria forca BUILD SUCCESS antes de modificar ROADMAP/STATE; sem 7 modulos verdes, plan 04-06 nao termina |
| T-04-06-02 | I (SUMMARY falso — nao documenta gotchas resolvidos) | 04-06-SUMMARY.md | mitigate | Acceptance criteria lista secoes obrigatorias (5/5 SC + 11/11 reqs + 4 D-XX + Empirical Validations) |
| T-04-06-03 | E (Bypass do checkpoint — Claude marca approved sozinho) | checkpoint:human-verify | mitigate | gate=blocking; resume-signal explicito requer "approved" do user |
| T-04-06-04 | T (REQUIREMENTS.md status divergente do truth) | grep gate em acceptance criteria | mitigate | Forca `Pending` count zero apos modificacao para todas as 11 OUT |

## ASVS L1 mappings
- N/A — este plan e auditoria/closeout, nao implementa feature
</threat_model>

<verification>
- Reator inteiro `./mvnw verify` BUILD SUCCESS sem regressao em api-email/api-storage/api-consultas
- Grep gates passam (sem enviarTemplate em codigo, sem access_token em src/main, 4 @JanelaProtegida, 4 fallbackMethod no @Retry)
- ROADMAP marca Phase 4 [x] + lista 6 plans
- REQUIREMENTS.md OUT-01..11 todas Complete
- STATE.md atualizado (completed_phases=4, total_plans=26, decisoes Phase 4 documentadas)
- 04-06-SUMMARY.md consolidando Phase 4 inteira (5/5 SC + 11/11 reqs + 4 D-XX + Empirical Validations)
- Human checkpoint aprovado antes do verifier
</verification>

<success_criteria>
- [ ] `./mvnw verify` reator inteiro BUILD SUCCESS
- [ ] Test count total >= 180 (152 Phase 1-3 + ~30 Phase 4)
- [ ] Grep gate enviarTemplate em codigo executavel: 0 hits
- [ ] Grep gate access_token em src/main: 0 hits
- [ ] Grep gate @JanelaProtegida em WhatsAppCloudClient.java: 4 hits
- [ ] Grep gate fallbackMethod em @Retry: 4 hits
- [ ] ROADMAP.md marca `[x] **Phase 4:**` + lista 6 plans + `6/6 Complete`
- [ ] REQUIREMENTS.md tabela traceability OUT-01..11 todas Complete
- [ ] STATE.md frontmatter `completed_phases: 4` + `total_plans: 26`
- [ ] 04-06-SUMMARY.md cobre todas as secoes obrigatorias
- [ ] Human verify checkpoint aprovado
- [ ] User pronto para rodar `/gsd-verify-phase 4-outbound-trava-24h-whatsappcontroller`
</success_criteria>

<output>
After completion, the developer can run `/gsd-verify-phase 4-outbound-trava-24h-whatsappcontroller` para validacao formal:
- 5/5 ROADMAP SC empiricamente verdes
- 11/11 OUT reqs Complete na traceability
- Reator BUILD SUCCESS + zero regressao
- 4 D-XX implementados conforme CONTEXT.md

Phase 4 transition para Phase 5 (lib-whatsapp-client) — DTOs do controller (EnviarTextoRequest etc.) ficam estaveis para Phase 5 espelhar contratos.
</output>
