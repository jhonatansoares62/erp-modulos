---
phase: 04-outbound-trava-24h-whatsappcontroller
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarTextoRequest.java
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarDocumentoRequest.java
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarBotoesRequest.java
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusResponse.java
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java
autonomous: true
requirements:
  - OUT-03
  - OUT-04
  - OUT-11
tags: [whatsapp, outbound, controller, rest-api, dto-validation, jakarta]
must_haves:
  truths:
    - "WhatsAppController expoe 5 endpoints: POST /api/whatsapp/enviar-{texto,documento,botoes,lista} + GET /api/whatsapp/status — todos sob /api/whatsapp e protegidos por ApiKeyFilter (Phase 1 SecurityConfig herdado) [implementa OUT-11]"
    - "EnviarBotoesRequest tem @Size(max=3) em List<BotaoDto> botoes — falha early 400 antes de chamar Cloud Client (OUT-03)"
    - "EnviarListaRequest tem @AssertTrue isTotalItensValido() somando itens cross-secoes — falha 400 se total > 10 (OUT-04 Cloud API limit)"
    - "EnviarDocumentoRequest tem mediaBase64 String com @NotBlank + @Size(max=18_000_000) (PDF ate ~13MB binario apos decode) + @Pattern em mimeType + @NotBlank em filename — controller faz Base64.getDecoder().decode + try/catch IllegalArgumentException → 400 mediaBase64 invalido (D-01)"
    - "GET /status retorna StatusResponse{status, circuitBreakerState, phoneNumberId} — minimal v1 per D-04; le state via cbRegistry.find(\"whatsapp-cloud\") (Phase 6 expansao)"
    - "WhatsAppControllerTest @WebMvcTest cobre 11+ scenarios: 5 happy path 200 + validation 400 (telefone vazio, base64 invalido, 4 botoes, lista 11 itens) + 409 janela_fechada + 422/502/504/503 erros Meta + status endpoint"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarTextoRequest.java"
      provides: "record com telefone (Pattern digitos 10-15) + texto (max 4096)"
      contains: "record EnviarTextoRequest"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarDocumentoRequest.java"
      provides: "record com telefone + mediaBase64 (max 18MB) + mimeType + filename + caption (D-01)"
      contains: "mediaBase64"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarBotoesRequest.java"
      provides: "record com telefone + texto + List<BotaoDto> com @Size(max=3) (OUT-03)"
      contains: "@Size(max = 3"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java"
      provides: "record com @AssertTrue isTotalItensValido cross-secao (OUT-04)"
      contains: "@AssertTrue"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusResponse.java"
      provides: "record(status, circuitBreakerState, phoneNumberId) — minimal D-04"
      contains: "record StatusResponse"
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java"
      provides: "5 endpoints @RestController @RequestMapping(\"/api/whatsapp\")"
      contains: "/api/whatsapp"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java"
      provides: "@WebMvcTest com @MockBean cloudClient — 11+ tests cobrindo OUT-11 + SC-2 + SC-5"
      min_tests: 11
  key_links:
    - from: "WhatsAppController.enviarDocumento"
      to: "Base64.getDecoder().decode + cloudClient.enviarDocumento(byte[])"
      via: "controller decodifica antes de delegar (D-01)"
      pattern: "Base64.getDecoder"
    - from: "WhatsAppController.enviar*"
      to: "WhatsAppCloudClient.enviar*"
      via: "thin wrapper Spring conventions"
      pattern: "cloudClient.enviar"
    - from: "GlobalExceptionHandler"
      to: "JanelaConversaFechadaException + MetaApiException"
      via: "lib-shared 04-01 propaga codigo+metaErrorCode"
      pattern: "implements CodigoCarrier"
    - from: "GET /status"
      to: "CircuitBreakerRegistry.find(\"whatsapp-cloud\")"
      via: "Resilience4j Spring Boot CB state introspection"
      pattern: "cbRegistry.find"
---

<objective>
Empacotar o `WhatsAppCloudClient` (04-04) atras de 5 endpoints REST documentados (`POST /api/whatsapp/enviar-{texto,documento,botoes,lista}` + `GET /api/whatsapp/status`) com Jakarta Bean Validation forcando os limites Cloud API (3 botoes, 10 itens, 18MB base64) ANTES de qualquer chamada externa, e implementar o decode base64 → byte[] no controller per D-01 (JSON+base64 entre ERP e api-whatsapp).

Purpose: O controller e a fronteira ERP → api-whatsapp; e onde o ERP precisa de feedback claro (200 com wamid em sucesso, 400 em validation, 409 em janela fechada, 422/502/504/503 em falhas Meta). O `GlobalExceptionHandler` (lib-shared 04-01) ja propaga `codigo+metaErrorCode` automaticamente para `JanelaConversaFechadaException` e `MetaApiException` (ambas implementam CodigoCarrier). Falta apenas: thin wrapper + DTOs com validation + test @WebMvcTest cobrindo todos os caminhos. Status endpoint minimal (D-04) — Phase 6 expandira se feedback piloto pedir.

Output: 5 DTOs request + 1 DTO response (StatusResponse) + 1 controller + 1 test class com 11+ tests; Phase 1-4 anteriores intactos; Phase 4 quase fechada — falta apenas 04-06 (closeout + smoke E2E reator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-CONTEXT.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-VALIDATION.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-04-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- 04-04 entregou — interfaces a consumir -->

From api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java (04-04):
```java
public class WhatsAppCloudClient {
    public EnvioResponse enviarTexto(String telefone, String texto);
    public EnvioResponse enviarDocumento(String telefone, byte[] bytes, String filename, String mimeType, String caption);
    public EnvioResponse enviarBotoes(String telefone, String texto, List<BotaoDto> botoes);
    public EnvioResponse enviarLista(String telefone, String texto, List<SecaoDto> secoes);
}
```

From api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ (04-04):
```java
public record EnvioResponse(String wamid);
public record BotaoDto(String id, String title);  // @NotBlank + @Size
public record SecaoDto(String titulo, List<ItemDto> itens);
public record ItemDto(String id, String title, String description);
```

From api-whatsapp/src/main/java/br/com/erpkit/whatsapp/exception/ (04-02 + 04-04):
```java
public class JanelaConversaFechadaException extends ModuloException implements CodigoCarrier;  // 409 + JANELA_24H_FECHADA
public class MetaApiException extends ModuloException implements CodigoCarrier;                 // 422/502/504/503
```

From api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java:
```java
public String getPhoneNumberId();   // status endpoint
```

From lib-shared SecurityConfig (Phase 1):
- /api/whatsapp/* exige X-API-Key header (NAO publico como /webhook/*)
- @WebMvcTest desabilita ApiKeyFilter via addFilters=false (pattern Phase 1+2)

From api-whatsapp Phase 1+2 WebhookControllerTest (analog):
- @WebMvcTest(WhatsAppController.class) + @AutoConfigureMockMvc(addFilters = false)
- @MockBean para cada bean usado pelo controller (cloudClient, properties, cbRegistry)

From CircuitBreakerRegistry (Resilience4j Spring Boot 3):
```java
Optional<CircuitBreaker> find(String name);
// CB.getState().name() → "CLOSED"|"OPEN"|"HALF_OPEN"|...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-05-1: 5 DTOs request/response (EnviarTexto/Documento/Botoes/Lista + StatusResponse)</name>
  <files>
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarTextoRequest.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarDocumentoRequest.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarBotoesRequest.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java,
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusResponse.java
  </files>
  <read_first>
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/BotaoDto.java (04-04 — pattern record + Jakarta + Size)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ItemDto.java (04-04)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/SecaoDto.java (04-04)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/ComandoCallbackDTO.java (Phase 3 — record output pattern)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md (secoes "EnviarTextoRequest" + "EnviarDocumentoRequest" + "EnviarBotoesRequest" + "EnviarListaRequest" + "StatusResponse" — pattern direto)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-CONTEXT.md (D-01 mediaBase64 String + D-04 StatusResponse minimal)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md (secao "Code Examples §6 Validation Bean")
  </read_first>
  <action>
1. Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarTextoRequest.java`:
```java
package br.com.erpkit.whatsapp.dto;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;

/**
 * Request body para POST /api/whatsapp/enviar-texto (OUT-01 + OUT-11).
 * <ul>
 *   <li>{@code telefone}: 10-15 digitos (formato Cloud API E.164 sem +)</li>
 *   <li>{@code texto}: max 4096 chars (Cloud API limit text/body)</li>
 * </ul>
 */
public record EnviarTextoRequest(
        @NotBlank(message = "telefone obrigatorio")
        @Pattern(regexp = "^\\d{10,15}$", message = "telefone deve conter apenas digitos (10-15)")
        String telefone,

        @NotBlank(message = "texto obrigatorio")
        @Size(max = 4096, message = "texto excede 4096 chars (Cloud API limit)")
        String texto
) {
}
```

2. Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarDocumentoRequest.java`:
```java
package br.com.erpkit.whatsapp.dto;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;

/**
 * Request body para POST /api/whatsapp/enviar-documento (OUT-02 + OUT-11). Per
 * D-01 CONTEXT.md: ERP envia bytes via {@code mediaBase64} String dentro de JSON
 * regular — NAO multipart. Controller decodifica via {@code Base64.getDecoder().decode}
 * antes de delegar a {@code WhatsAppCloudClient.enviarDocumento(byte[])}.
 *
 * <p><b>Limites:</b>
 * <ul>
 *   <li>{@code mediaBase64}: max 18MB (PDF ~13MB binario × 1.33 inflation)</li>
 *   <li>{@code mimeType}: pattern {@code type/subtype} basico (ex: application/pdf)</li>
 *   <li>{@code filename}: max 255 chars (filesystem-safe)</li>
 *   <li>{@code caption}: opcional, max 1024 chars (Cloud API limit)</li>
 * </ul>
 */
public record EnviarDocumentoRequest(
        @NotBlank(message = "telefone obrigatorio")
        @Pattern(regexp = "^\\d{10,15}$", message = "telefone deve conter apenas digitos (10-15)")
        String telefone,

        @NotBlank(message = "mediaBase64 obrigatorio")
        @Size(max = 18_000_000, message = "mediaBase64 excede limite (~13MB binario apos decode)")
        String mediaBase64,

        @NotBlank(message = "mimeType obrigatorio")
        @Pattern(regexp = "^[a-zA-Z]+/[a-zA-Z0-9.+\\-]+$", message = "mimeType invalido (ex: application/pdf)")
        String mimeType,

        @NotBlank(message = "filename obrigatorio")
        @Size(max = 255, message = "filename max 255 chars")
        String filename,

        @Size(max = 1024, message = "caption max 1024 chars (Cloud API limit)")
        String caption
) {
}
```

3. Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarBotoesRequest.java`:
```java
package br.com.erpkit.whatsapp.dto;

import jakarta.validation.Valid;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;

import java.util.List;

/**
 * Request body para POST /api/whatsapp/enviar-botoes (OUT-03 + OUT-11). Cloud
 * API limits:
 * <ul>
 *   <li>{@code texto}: max 1024 chars (interactive body text)</li>
 *   <li>{@code botoes}: max 3 botoes (Cloud API hard limit) — {@code @Size(max=3)} forca early 400</li>
 * </ul>
 */
public record EnviarBotoesRequest(
        @NotBlank(message = "telefone obrigatorio")
        @Pattern(regexp = "^\\d{10,15}$", message = "telefone deve conter apenas digitos (10-15)")
        String telefone,

        @NotBlank(message = "texto obrigatorio")
        @Size(max = 1024, message = "texto max 1024 chars (Cloud API limit)")
        String texto,

        @NotEmpty(message = "botoes nao pode ser vazio")
        @Size(max = 3, message = "Maximo 3 botoes (Cloud API limit)")
        @Valid
        List<BotaoDto> botoes
) {
}
```

4. Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java`:
```java
package br.com.erpkit.whatsapp.dto;

import com.fasterxml.jackson.annotation.JsonIgnore;
import jakarta.validation.Valid;
import jakarta.validation.constraints.AssertTrue;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;

import java.util.List;
import java.util.Objects;

/**
 * Request body para POST /api/whatsapp/enviar-lista (OUT-04 + OUT-11). Cloud
 * API limit hard: total de itens TODAS secoes &le; 10 (Meta hard-rejects > 10).
 * Validacao cross-secao via {@link #isTotalItensValido()} {@code @AssertTrue}.
 *
 * <p>{@code @JsonIgnore} no metodo para evitar Jackson incluir o boolean no JSON
 * de response (record toString OK; campo "totalItensValido" nao deve aparecer).
 */
public record EnviarListaRequest(
        @NotBlank(message = "telefone obrigatorio")
        @Pattern(regexp = "^\\d{10,15}$", message = "telefone deve conter apenas digitos (10-15)")
        String telefone,

        @NotBlank(message = "texto obrigatorio")
        @Size(max = 1024, message = "texto max 1024 chars (Cloud API limit)")
        String texto,

        @NotEmpty(message = "secoes nao pode ser vazio")
        @Size(max = 10, message = "Maximo 10 secoes")
        @Valid
        List<SecaoDto> secoes
) {

    /**
     * Soma cross-secao de itens deve ser &le; 10 (Cloud API hard limit). Falha
     * 400 via Bean Validation se total > 10.
     */
    @AssertTrue(message = "Total de itens em todas as secoes excede 10 (Cloud API limit)")
    @JsonIgnore
    public boolean isTotalItensValido() {
        if (secoes == null) return true;
        int total = secoes.stream()
                .filter(Objects::nonNull)
                .mapToInt(s -> s.itens() == null ? 0 : s.itens().size())
                .sum();
        return total <= 10;
    }
}
```

5. Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusResponse.java`:
```java
package br.com.erpkit.whatsapp.dto;

/**
 * Response do GET /api/whatsapp/status (OUT-11 + D-04 minimal CONTEXT.md).
 * Phase 6 pode expandir se feedback do piloto pedir (subscribed_apps via
 * Graph API — PITFALLS C-12).
 *
 * @param status              "UP" | "DOWN" — saude geral simples
 * @param circuitBreakerState "CLOSED" | "OPEN" | "HALF_OPEN" | "UNKNOWN"
 * @param phoneNumberId       sanity check para operador comparar com env var
 */
public record StatusResponse(
        String status,
        String circuitBreakerState,
        String phoneNumberId
) {
}
```
  </action>
  <verify>
    <automated>./mvnw -pl api-whatsapp compile</automated>
  </verify>
  <acceptance_criteria>
    - 5 files criados em `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/`
    - `grep -c '@Size(max = 3' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarBotoesRequest.java` retorna 1 (OUT-03 hard limit)
    - `grep -c '@AssertTrue' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java` retorna 1 (OUT-04 cross-secao)
    - `grep -c 'isTotalItensValido' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java` retorna 1
    - `grep -c '@JsonIgnore' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java` retorna 1
    - `grep -c '@Size(max = 18_000_000' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarDocumentoRequest.java` retorna 1 (D-01 mediaBase64 ~13MB binary)
    - `grep -c 'Pattern.*10,15' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarTextoRequest.java` retorna 1 (telefone formato)
    - `grep -c 'public record StatusResponse' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusResponse.java` retorna 1
    - `grep -c 'circuitBreakerState' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusResponse.java` retorna 1
    - `./mvnw -pl api-whatsapp compile` exits 0
  </acceptance_criteria>
  <done>
    5 DTOs records criados com Jakarta Bean Validation forcando limites Cloud API antes de qualquer chamada externa. Compilacao limpa. Pronto para 04-05-2 (controller).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-05-2: WhatsAppController com 5 endpoints</name>
  <files>api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java</files>
  <read_first>
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WebhookController.java (Phase 1+2 — pattern @RestController + @RequestMapping)
    - api-email/src/main/java/br/com/erpkit/email/controller/EmailController.java (monorepo pattern thin wrapper @Valid @RequestBody → service.delegate → ResponseEntity.ok)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/WhatsAppCloudClient.java (04-04 — interface a consumir)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarTextoRequest.java (recem criado)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarDocumentoRequest.java
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarBotoesRequest.java
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnviarListaRequest.java
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/StatusResponse.java
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/dto/EnvioResponse.java (04-04)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/config/WhatsAppProperties.java (Phase 1+3 — getPhoneNumberId)
    - lib-shared/src/main/java/br/com/erpkit/shared/exception/ModuloException.java (super para BAD_REQUEST custom)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md (secao "controller/WhatsAppController.java" — pattern direto)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md (secao "Pattern 5: WhatsAppController Thin Wrapper")
  </read_first>
  <action>
Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java`:

```java
package br.com.erpkit.whatsapp.controller;

import br.com.erpkit.shared.exception.ModuloException;
import br.com.erpkit.whatsapp.config.WhatsAppProperties;
import br.com.erpkit.whatsapp.dto.EnviarBotoesRequest;
import br.com.erpkit.whatsapp.dto.EnviarDocumentoRequest;
import br.com.erpkit.whatsapp.dto.EnviarListaRequest;
import br.com.erpkit.whatsapp.dto.EnviarTextoRequest;
import br.com.erpkit.whatsapp.dto.EnvioResponse;
import br.com.erpkit.whatsapp.dto.StatusResponse;
import br.com.erpkit.whatsapp.service.WhatsAppCloudClient;
import io.github.resilience4j.circuitbreaker.CircuitBreakerRegistry;
import jakarta.validation.Valid;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Base64;

/**
 * Endpoints REST para o ERP enviar mensagens outbound (OUT-11). 5 endpoints sob
 * {@code /api/whatsapp/*} — protegidos por {@code ApiKeyFilter} (Phase 1
 * SecurityConfig herdado). Webhook publico {@code /webhook/whatsapp/*} fica a
 * parte (HMAC, Phase 1).
 *
 * <p><b>Thin wrapper (pattern monorepo):</b> cada endpoint valida via
 * {@code @Valid @RequestBody}, delega ao {@link WhatsAppCloudClient}, retorna
 * {@code ResponseEntity}. Excecoes ({@code JanelaConversaFechadaException} →
 * 409, {@code MetaApiException} → 422/502/504/503, {@code MethodArgumentNotValidException}
 * → 400) sao capturadas pelo {@code GlobalExceptionHandler} (lib-shared 04-01)
 * que propaga {@code codigo+metaErrorCode} via {@code CodigoCarrier}.
 *
 * <p><b>Decodificacao base64 (D-01):</b> {@code enviarDocumento} decodifica
 * {@code mediaBase64} via {@code Base64.getDecoder().decode} dentro de try/catch
 * {@code IllegalArgumentException} → 400 com mensagem clara. Bytes resultantes
 * sao passados para {@code WhatsAppCloudClient.enviarDocumento(byte[])} que
 * cuida do cache + upload multipart Meta-side.
 *
 * <p><b>Status endpoint minimal (D-04):</b> retorna {@code phoneNumberId} (sanity
 * check vs env var) + {@code circuitBreakerState} (operador diagnostica circuit
 * aberto). Sem subscribed_apps validation — Phase 6 territory (PITFALLS C-12).
 */
@RestController
@RequestMapping("/api/whatsapp")
public class WhatsAppController {

    private final WhatsAppCloudClient cloudClient;
    private final WhatsAppProperties properties;
    private final CircuitBreakerRegistry cbRegistry;

    public WhatsAppController(WhatsAppCloudClient cloudClient,
                              WhatsAppProperties properties,
                              CircuitBreakerRegistry cbRegistry) {
        this.cloudClient = cloudClient;
        this.properties = properties;
        this.cbRegistry = cbRegistry;
    }

    @PostMapping("/enviar-texto")
    public ResponseEntity<EnvioResponse> enviarTexto(@Valid @RequestBody EnviarTextoRequest req) {
        return ResponseEntity.ok(cloudClient.enviarTexto(req.telefone(), req.texto()));
    }

    @PostMapping("/enviar-documento")
    public ResponseEntity<EnvioResponse> enviarDocumento(@Valid @RequestBody EnviarDocumentoRequest req) {
        byte[] bytes;
        try {
            bytes = Base64.getDecoder().decode(req.mediaBase64());
        } catch (IllegalArgumentException e) {
            // D-01: ERP envia base64 dentro de JSON — falha de decode = bug do ERP, 400
            throw new ModuloException("mediaBase64 invalido: " + e.getMessage(), HttpStatus.BAD_REQUEST);
        }
        return ResponseEntity.ok(cloudClient.enviarDocumento(
                req.telefone(), bytes, req.filename(), req.mimeType(), req.caption()));
    }

    @PostMapping("/enviar-botoes")
    public ResponseEntity<EnvioResponse> enviarBotoes(@Valid @RequestBody EnviarBotoesRequest req) {
        return ResponseEntity.ok(cloudClient.enviarBotoes(req.telefone(), req.texto(), req.botoes()));
    }

    @PostMapping("/enviar-lista")
    public ResponseEntity<EnvioResponse> enviarLista(@Valid @RequestBody EnviarListaRequest req) {
        return ResponseEntity.ok(cloudClient.enviarLista(req.telefone(), req.texto(), req.secoes()));
    }

    @GetMapping("/status")
    public ResponseEntity<StatusResponse> status() {
        String state = cbRegistry.find("whatsapp-cloud")
                .map(cb -> cb.getState().name())
                .orElse("UNKNOWN");
        return ResponseEntity.ok(new StatusResponse("UP", state, properties.getPhoneNumberId()));
    }
}
```
  </action>
  <verify>
    <automated>./mvnw -pl api-whatsapp test -Dtest='WhatsAppPropertiesHappyPathTest'</automated>
  </verify>
  <acceptance_criteria>
    - File `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` existe
    - `grep -c '@RestController' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 1
    - `grep -c '@RequestMapping("/api/whatsapp")' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 1
    - `grep -c '@PostMapping' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 4 (texto+documento+botoes+lista)
    - `grep -c '@GetMapping("/status")' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 1
    - `grep -c '@Valid @RequestBody' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 4 (forca Bean Validation em todos os POST)
    - `grep -c 'Base64.getDecoder().decode' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 1 (apenas em enviarDocumento)
    - `grep -c 'cbRegistry.find("whatsapp-cloud")' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 1 (status endpoint)
    - `grep -c 'HttpStatus.BAD_REQUEST' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java` retorna 1 (base64 invalido)
    - `./mvnw -pl api-whatsapp compile` exits 0
    - `./mvnw -pl api-whatsapp test -Dtest='WhatsAppPropertiesHappyPathTest'` verde — Spring context carrega controller sem erro
  </acceptance_criteria>
  <done>
    Controller com 5 endpoints thin wrapper, decode base64 + try/catch em enviarDocumento (D-01), cbRegistry.find no status (D-04 minimal). Compilacao limpa. Phase 1-3 tests inalterados.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-05-3: WhatsAppControllerTest @WebMvcTest com 11+ scenarios</name>
  <files>api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java</files>
  <read_first>
    - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WebhookControllerTest.java (Phase 1 — pattern @WebMvcTest + @MockBean)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/controller/WhatsAppController.java (recem criado — methods a testar)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/exception/JanelaConversaFechadaException.java (04-02 — para mock throw)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/exception/MetaApiException.java (04-04 — para mock throw 4 tipos)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md (secao "WhatsAppControllerTest.java" — 10 scenarios obrigatorios)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-VALIDATION.md (Test Map OUT-11 + SC-2 + SC-5)
  </read_first>
  <behavior>
    - 11+ tests scenarios:
      1. enviar_texto_happy_200: when(cloudClient).thenReturn(EnvioResponse) → 200 com `wamid` no body
      2. enviar_texto_validation_400_telefone_vazio: body `{"telefone":"","texto":"hi"}` → 400 + campos.telefone
      3. enviar_texto_validation_400_telefone_letras: telefone com letras → 400 + Pattern message
      4. enviar_documento_base64_invalido_400: mediaBase64 com chars invalidos `"!!!"` → 400 + "mediaBase64 invalido"
      5. enviar_botoes_happy_200: 2 botoes → 200
      6. enviar_botoes_validation_400_4_botoes: 4 botoes → 400 + campos.botoes "Maximo 3 botoes"
      7. enviar_lista_validation_400_total_11_itens: 1 secao com 11 itens → 400 + isTotalItensValido violation message
      8. enviar_lista_happy_200: 2 secoes 5+5 itens → 200
      9. janela_fechada_retorna_409_codigo_janela: when(cloudClient).thenThrow(JanelaConversaFechadaException) → 409 + body.codigo="JANELA_24H_FECHADA"
      10. meta_4xx_retorna_422_codigo_meta_error: thenThrow MetaApiException(CATEGORIA_4XX, 131026) → 422 + codigo="META_ERROR" + metaErrorCode=131026
      11. meta_5xx_retorna_502: thenThrow MetaApiException(INDISPONIVEL_5XX) → 502 + codigo="META_INDISPONIVEL"
      12. circuit_open_retorna_503: thenThrow MetaApiException(CIRCUIT_OPEN) → 503 + codigo="CIRCUIT_OPEN"
      13. status_endpoint_retorna_state_cb: cbRegistry.find("whatsapp-cloud") mock returns CB CLOSED → 200 + circuitBreakerState="CLOSED" + phoneNumberId
  </behavior>
  <action>
Criar `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java`:

```java
package br.com.erpkit.whatsapp.controller;

import br.com.erpkit.whatsapp.config.WhatsAppProperties;
import br.com.erpkit.whatsapp.dto.EnvioResponse;
import br.com.erpkit.whatsapp.exception.JanelaConversaFechadaException;
import br.com.erpkit.whatsapp.exception.MetaApiException;
import br.com.erpkit.whatsapp.service.WhatsAppCloudClient;
import com.fasterxml.jackson.databind.ObjectMapper;
import io.github.resilience4j.circuitbreaker.CircuitBreaker;
import io.github.resilience4j.circuitbreaker.CircuitBreakerRegistry;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;

import java.util.List;
import java.util.Map;
import java.util.Optional;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyList;
import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@WebMvcTest(WhatsAppController.class)
@AutoConfigureMockMvc(addFilters = false)  // bypass ApiKeyFilter no @WebMvcTest
class WhatsAppControllerTest {

    @Autowired MockMvc mvc;
    @Autowired ObjectMapper om;

    @MockBean WhatsAppCloudClient cloudClient;
    @MockBean WhatsAppProperties properties;
    @MockBean CircuitBreakerRegistry cbRegistry;

    @BeforeEach
    void setupDefaults() {
        when(properties.getPhoneNumberId()).thenReturn("test-phone-id");
    }

    @Test
    @DisplayName("POST /enviar-texto happy path retorna 200 + wamid")
    void enviar_texto_happy_200() throws Exception {
        when(cloudClient.enviarTexto(anyString(), anyString())).thenReturn(new EnvioResponse("wamid-tx-001"));

        mvc.perform(post("/api/whatsapp/enviar-texto")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"554784178525\",\"texto\":\"Olá\"}"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.wamid").value("wamid-tx-001"));
    }

    @Test
    @DisplayName("POST /enviar-texto com telefone vazio retorna 400 + campos.telefone")
    void enviar_texto_validation_400_telefone_vazio() throws Exception {
        mvc.perform(post("/api/whatsapp/enviar-texto")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"\",\"texto\":\"hi\"}"))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.campos.telefone").exists());
    }

    @Test
    @DisplayName("POST /enviar-texto com telefone com letras retorna 400")
    void enviar_texto_validation_400_telefone_letras() throws Exception {
        mvc.perform(post("/api/whatsapp/enviar-texto")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"abc123\",\"texto\":\"hi\"}"))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.campos.telefone").exists());
    }

    @Test
    @DisplayName("POST /enviar-documento com mediaBase64 invalido retorna 400")
    void enviar_documento_base64_invalido_400() throws Exception {
        mvc.perform(post("/api/whatsapp/enviar-documento")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"554784178525\",\"mediaBase64\":\"!!!\",\"mimeType\":\"application/pdf\",\"filename\":\"a.pdf\"}"))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.mensagem").value(org.hamcrest.Matchers.containsString("mediaBase64 invalido")));
    }

    @Test
    @DisplayName("POST /enviar-botoes happy path com 2 botoes retorna 200")
    void enviar_botoes_happy_200() throws Exception {
        when(cloudClient.enviarBotoes(anyString(), anyString(), anyList())).thenReturn(new EnvioResponse("wamid-btn-1"));

        String body = "{\"telefone\":\"554784178525\",\"texto\":\"Aprovar?\",\"botoes\":[" +
                "{\"id\":\"aprovar\",\"title\":\"Aprovar\"}," +
                "{\"id\":\"recusar\",\"title\":\"Recusar\"}]}";

        mvc.perform(post("/api/whatsapp/enviar-botoes")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(body))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.wamid").value("wamid-btn-1"));
    }

    @Test
    @DisplayName("POST /enviar-botoes com 4 botoes retorna 400 (Cloud API limit OUT-03)")
    void enviar_botoes_validation_400_4_botoes() throws Exception {
        String body = "{\"telefone\":\"554784178525\",\"texto\":\"x\",\"botoes\":[" +
                "{\"id\":\"a\",\"title\":\"A\"},{\"id\":\"b\",\"title\":\"B\"}," +
                "{\"id\":\"c\",\"title\":\"C\"},{\"id\":\"d\",\"title\":\"D\"}]}";

        mvc.perform(post("/api/whatsapp/enviar-botoes")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(body))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.campos.botoes").exists());
    }

    @Test
    @DisplayName("POST /enviar-lista com total 11 itens cross-secao retorna 400 (@AssertTrue OUT-04)")
    void enviar_lista_validation_400_total_11_itens() throws Exception {
        StringBuilder sb = new StringBuilder("{\"telefone\":\"554784178525\",\"texto\":\"x\",\"secoes\":[{\"titulo\":\"S1\",\"itens\":[");
        for (int i = 0; i < 11; i++) {
            sb.append("{\"id\":\"i").append(i).append("\",\"title\":\"T").append(i).append("\"}");
            if (i < 10) sb.append(",");
        }
        sb.append("]}]}");

        mvc.perform(post("/api/whatsapp/enviar-lista")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(sb.toString()))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.campos.totalItensValido").exists());
    }

    @Test
    @DisplayName("POST /enviar-lista com 2 secoes 5+5 itens retorna 200")
    void enviar_lista_happy_200() throws Exception {
        when(cloudClient.enviarLista(anyString(), anyString(), anyList())).thenReturn(new EnvioResponse("wamid-list-1"));

        String body = "{\"telefone\":\"554784178525\",\"texto\":\"Escolha\",\"secoes\":[" +
                "{\"titulo\":\"S1\",\"itens\":[{\"id\":\"i1\",\"title\":\"T1\"},{\"id\":\"i2\",\"title\":\"T2\"}]}," +
                "{\"titulo\":\"S2\",\"itens\":[{\"id\":\"i3\",\"title\":\"T3\"}]}]}";

        mvc.perform(post("/api/whatsapp/enviar-lista")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(body))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.wamid").value("wamid-list-1"));
    }

    @Test
    @DisplayName("Janela 24h fechada propaga 409 + codigo=JANELA_24H_FECHADA")
    void janela_fechada_retorna_409_codigo_janela() throws Exception {
        when(cloudClient.enviarTexto(anyString(), anyString()))
                .thenThrow(new JanelaConversaFechadaException("554784178525", null));

        mvc.perform(post("/api/whatsapp/enviar-texto")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"554784178525\",\"texto\":\"hi\"}"))
                .andExpect(status().isConflict())
                .andExpect(jsonPath("$.codigo").value("JANELA_24H_FECHADA"));
    }

    @Test
    @DisplayName("Meta 4xx propaga 422 + codigo=META_ERROR + metaErrorCode")
    void meta_4xx_retorna_422_codigo_meta_error() throws Exception {
        when(cloudClient.enviarTexto(anyString(), anyString()))
                .thenThrow(new MetaApiException(MetaApiException.Tipo.CATEGORIA_4XX, 131026, "Recipient phone not on WhatsApp"));

        mvc.perform(post("/api/whatsapp/enviar-texto")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"554784178525\",\"texto\":\"hi\"}"))
                .andExpect(status().isUnprocessableEntity())
                .andExpect(jsonPath("$.codigo").value("META_ERROR"))
                .andExpect(jsonPath("$.metaErrorCode").value(131026));
    }

    @Test
    @DisplayName("Meta 5xx propaga 502 + codigo=META_INDISPONIVEL")
    void meta_5xx_retorna_502() throws Exception {
        when(cloudClient.enviarTexto(anyString(), anyString()))
                .thenThrow(new MetaApiException(MetaApiException.Tipo.INDISPONIVEL_5XX, null, "Meta down"));

        mvc.perform(post("/api/whatsapp/enviar-texto")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"554784178525\",\"texto\":\"hi\"}"))
                .andExpect(status().isBadGateway())
                .andExpect(jsonPath("$.codigo").value("META_INDISPONIVEL"));
    }

    @Test
    @DisplayName("Circuit aberto propaga 503 + codigo=CIRCUIT_OPEN")
    void circuit_open_retorna_503() throws Exception {
        when(cloudClient.enviarTexto(anyString(), anyString()))
                .thenThrow(new MetaApiException(MetaApiException.Tipo.CIRCUIT_OPEN, null, "circuit open"));

        mvc.perform(post("/api/whatsapp/enviar-texto")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{\"telefone\":\"554784178525\",\"texto\":\"hi\"}"))
                .andExpect(status().isServiceUnavailable())
                .andExpect(jsonPath("$.codigo").value("CIRCUIT_OPEN"));
    }

    @Test
    @DisplayName("GET /status retorna circuitBreakerState + phoneNumberId (D-04 minimal)")
    void status_endpoint_retorna_state_cb() throws Exception {
        CircuitBreaker cb = org.mockito.Mockito.mock(CircuitBreaker.class);
        when(cb.getState()).thenReturn(CircuitBreaker.State.CLOSED);
        when(cbRegistry.find("whatsapp-cloud")).thenReturn(Optional.of(cb));

        mvc.perform(get("/api/whatsapp/status"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.status").value("UP"))
                .andExpect(jsonPath("$.circuitBreakerState").value("CLOSED"))
                .andExpect(jsonPath("$.phoneNumberId").value("test-phone-id"));
    }
}
```
  </action>
  <verify>
    <automated>./mvnw -pl api-whatsapp test -Dtest='WhatsAppControllerTest'</automated>
  </verify>
  <acceptance_criteria>
    - File `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` existe
    - `grep -c '@DisplayName' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` >= 13 (13 tests)
    - `grep -c '@WebMvcTest(WhatsAppController.class)' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` retorna 1
    - `grep -c '@AutoConfigureMockMvc(addFilters = false)' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` retorna 1 (bypass ApiKeyFilter no test layer)
    - `grep -c 'jsonPath("$.codigo")' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` >= 4 (janela + meta_error + indisponivel + circuit)
    - `grep -c 'jsonPath("$.metaErrorCode")' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` >= 1 (meta_4xx)
    - `grep -c 'jsonPath("$.campos' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` >= 3 (validation 400 paths)
    - `grep -c 'JanelaConversaFechadaException' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` >= 1
    - `grep -c 'CATEGORIA_4XX\|INDISPONIVEL_5XX\|CIRCUIT_OPEN' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/controller/WhatsAppControllerTest.java` >= 3
    - `./mvnw -pl api-whatsapp test -Dtest='WhatsAppControllerTest'` exits 0 — Tests run: >= 13, Failures: 0
    - Tempo execucao do test class < 10s (sem WireMock, apenas @WebMvcTest)
  </acceptance_criteria>
  <done>
    13 tests verdes cobrindo OUT-11 + SC-2 + SC-5 + validation 400 paths (telefone vazio, telefone letras, base64 invalido, 4 botoes, 11 itens) + erro Meta 4 tipos (409/422/502/503) + status endpoint. Phase 1-4 anteriores intactos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ERP -> WhatsAppController | API key (Phase 1 SecurityConfig) + Bean Validation forca limites Cloud API |
| Controller -> WhatsAppCloudClient | Java direct call; @JanelaProtegida aspect intercepta |
| Base64 decode | Controller responsabilidade (D-01) — ERP envia base64, controller produz byte[] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | T (Tampering — payload Cloud API hard limits violados) | EnviarBotoes/Lista DTO | mitigate | @Size(max=3) em botoes + @AssertTrue(total<=10) em lista — falha early 400 ANTES de Cloud Client; sem isso, Meta hard-rejects com mensagem confusa |
| T-04-05-02 | I (Information disclosure — body grande exausta memoria) | EnviarDocumento mediaBase64 | mitigate | @Size(max=18_000_000) limita request body; Spring `spring.servlet.multipart.max-request-size` tambem aplica (Phase 6 RUNBOOK documenta override se piloto MUDAS pedir PDFs >15MB) |
| T-04-05-03 | T (Base64 invalido cinge controle) | enviarDocumento | mitigate | try/catch IllegalArgumentException → 400 com mensagem clara; ERP corrige imediatamente vs erro confuso de Meta downstream |
| T-04-05-04 | I (Telefone com letras processado) | telefone field | mitigate | @Pattern("^\\d{10,15}$") rejeita early 400 ANTES de chegar a TelefoneBR.normalizar (Phase 2) ou WindowEnforcementService (04-02) |
| T-04-05-05 | A (Status endpoint expoe info sensitiva) | StatusResponse | accept | Apenas phoneNumberId + state CB — phoneNumberId e configuracao publica (visivel em URL Meta /v22.0/{phoneNumberId}/messages); CB state e operacional, nao secret |
| T-04-05-06 | I (Bearer leak via test profile content) | WhatsAppControllerTest | mitigate | @WebMvcTest NAO carrega WhatsAppCloudClient real (apenas mock) — sem chamadas Cloud API, sem Bearer no test |
| T-04-05-07 | E (Bypass de validation se @Valid omitido) | WhatsAppController POST methods | mitigate | Acceptance criteria forca @Valid em todos os 4 POST (`grep -c '@Valid @RequestBody' == 4`) |
| T-04-05-08 | T (CodigoCarrier nao implementado em sub-exception) | JanelaConversaFechadaException + MetaApiException | mitigate | 04-02 + 04-04 acceptance criteria ja forcam `implements CodigoCarrier`; teste `meta_4xx_retorna_422_codigo_meta_error` valida propagacao end-to-end (DTO → controller → handler → JSON) |

## V5 Input Validation
- 5 DTOs com Jakarta @NotBlank + @Pattern + @Size + @AssertTrue + @Valid em listas aninhadas
- Bean Validation rejeitada via MethodArgumentNotValidException → GlobalExceptionHandler → 400 + campos map
- Base64 decode protege contra payload nao-base64 (try/catch → 400)

## V7 Error Handling
- 5 codigos estruturados (D-02): JANELA_24H_FECHADA (409), META_ERROR (422), META_INDISPONIVEL (502), META_TIMEOUT (504), CIRCUIT_OPEN (503)
- VALIDATION_ERROR para 400 paths (Spring default, com campos map detalhando field-level errors)

## V12 File size limit
- @Size(max=18_000_000) em mediaBase64 (~13MB binario)
- Phase 6 RUNBOOK documenta `spring.servlet.multipart.max-request-size` override se necessario

## ASVS L1 mappings
- V4.1.1 (Verify access control via API key): /api/whatsapp/* protegido por ApiKeyFilter (Phase 1)
- V5.1.1 (Verify input validation at trust boundaries): @Valid forca Jakarta validation
- V5.1.5 (Verify input validation failures result in rejection): GlobalExceptionHandler → 400
- V7.4.1 (Verify error messages do not include sensitive data): codigo estruturado, sem stack trace inteira
</threat_model>

<verification>
- 5 endpoints REST documentados (POST x4 + GET x1) sob /api/whatsapp/*
- Bean Validation rejeita early 400 com campos map em @WebMvcTest
- Base64 decode + try/catch em enviarDocumento (D-01)
- Status endpoint minimal (D-04) le CB state via cbRegistry.find
- 13 tests verdes em <10s (sem WireMock — @WebMvcTest com @MockBean)
- GlobalExceptionHandler propaga codigo+metaErrorCode end-to-end via test scenarios
- Phase 1-4 anteriores tests inalterados
- Reator inteiro `./mvnw verify` BUILD SUCCESS
</verification>

<success_criteria>
- [ ] 5 DTOs records criados com Jakarta Bean Validation
- [ ] EnviarBotoesRequest com @Size(max=3) em List<BotaoDto> (OUT-03)
- [ ] EnviarListaRequest com @AssertTrue isTotalItensValido cross-secao (OUT-04)
- [ ] EnviarDocumentoRequest com mediaBase64 String + @Size(max=18_000_000) (D-01)
- [ ] StatusResponse minimal (status, circuitBreakerState, phoneNumberId) (D-04)
- [ ] WhatsAppController @RestController @RequestMapping("/api/whatsapp") com 5 endpoints
- [ ] enviarDocumento decodifica mediaBase64 via Base64.getDecoder().decode + try/catch → 400
- [ ] /status le state via cbRegistry.find("whatsapp-cloud")
- [ ] WhatsAppControllerTest 13 tests verdes cobrindo todos os scenarios
- [ ] `./mvnw -pl api-whatsapp verify` BUILD SUCCESS — Phase 1-4 + 04-05 = todos verdes
- [ ] `./mvnw verify` reator inteiro BUILD SUCCESS
</success_criteria>

<output>
After completion, create `.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-05-SUMMARY.md`:
- Files created (5 DTOs + 1 controller + 1 test = 7 files)
- 13 tests added cobrindo OUT-11 + SC-2 + SC-5 completamente
- D-01 implementado (JSON+base64 ERP → api-whatsapp)
- D-02 propagacao end-to-end validada (codigo+metaErrorCode no body do JSON)
- D-04 status minimal implementado
- OUT-03 + OUT-04 limites cross-list/cross-secao validados via Jakarta
- Phase 4 quase fechada — falta apenas 04-06 (closeout + smoke E2E + ROADMAP)
</output>
