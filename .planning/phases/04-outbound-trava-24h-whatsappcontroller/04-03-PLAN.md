---
phase: 04-outbound-trava-24h-whatsappcontroller
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java
  - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MediaCacheServiceTest.java
autonomous: true
requirements:
  - OUT-08
tags: [whatsapp, outbound, media-cache, sha256, race-protection, ttl-30d]
must_haves:
  truths:
    - "MediaCacheService.buscarMediaId(byte[] bytes) calcula sha256 hex via HexFormat.of().formatHex (Java 17+) + chama findByArquivoHashAndExpiraEmAfter(hash, Instant.now()) — retorna Optional<String> com media_id se hit nao expirado, empty se miss ou expirado [implementa OUT-08 + D-04 TTL estrito]"
    - "MediaCacheService.registrarUpload(byte[] bytes, String mediaId) calcula sha256, deleta entrada antiga (se expirada) + save com expira_em = now() + 30d; race em concurrent reupload silenciado via try/catch DataIntegrityViolationException (PK gate atomico, pattern Phase 2 IdempotencyService)"
    - "TTL ESTRITO 30d sem sliding (D-04): hit nao estende expira_em — turnover natural reupload no 31o dia renova TTL"
    - "MediaCacheServiceTest cobre 4 cenarios: hit dentro do TTL, miss quando hash inexistente, miss quando entrada expirada (force expira_em < now via JdbcTemplate), race silenciado em concurrent registrarUpload — todos os 4 verdes em <5s"
  artifacts:
    - path: "api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java"
      provides: "buscarMediaId(byte[]) → Optional<String> + registrarUpload(byte[], String) → void; constantes TTL = Duration.ofDays(30)"
      contains: "Duration.ofDays(30)"
    - path: "api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MediaCacheServiceTest.java"
      provides: "@SpringBootTest+H2 — 4 tests scenarios per OUT-08 + Pitfall 7 (TTL boundary) + race"
      min_tests: 4
  key_links:
    - from: "MediaCacheService.buscarMediaId"
      to: "MediaCacheRepository.findByArquivoHashAndExpiraEmAfter"
      via: "Phase 2 metodo ja existente (sem mudanca de repo)"
      pattern: "findByArquivoHashAndExpiraEmAfter"
    - from: "MediaCacheService.registrarUpload"
      to: "DataIntegrityViolationException catch + log.debug"
      via: "Pattern Phase 2 IdempotencyService — UNIQUE PK arquivo_hash gate"
      pattern: "DataIntegrityViolationException"
    - from: "sha256 calculation"
      to: "java.util.HexFormat (JDK 17+)"
      via: "MessageDigest.getInstance(SHA-256).digest(bytes) + HexFormat.of().formatHex"
      pattern: "HexFormat.of()"
---

<objective>
Implementar `MediaCacheService` que cumpre OUT-08 (cache `media_id` por sha256, TTL estrito 30d, race protection) reutilizando o pattern save+catch de Phase 2 (`IdempotencyService`) e o repository `findByArquivoHashAndExpiraEmAfter` ja existente em Phase 2.

Purpose: Sem este service, `enviarDocumento` (04-04) faria upload do mesmo PDF a cada chamada — alem de desperdicio de banda Meta, ha risco de rate limit 131053 ("invalid media id") quando Meta detecta multiplos uploads identicos do mesmo conteudo. TTL estrito (NAO sliding, per D-04) evita que cache cresca monotonicamente E garante que cache nunca tenha entries staler que TTL real do Meta (PITFALLS C-07).

Output: 1 service + 1 test class com 4 cenarios; Phase 1-3 tests existentes inalterados; 04-04 ja consegue chamar `mediaCacheService.buscarMediaId(bytes)` + `registrarUpload(bytes, mediaId)` no `enviarDocumento`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-CONTEXT.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-RESEARCH.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md
@.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Phase 2 assets (sem mudanca) -->

From api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MediaCacheRepository.java (Phase 2):
```java
public interface MediaCacheRepository extends JpaRepository<MediaCache, String> {
    Optional<MediaCache> findByArquivoHashAndExpiraEmAfter(String arquivoHash, Instant agora);
    // (herdados): save, findById, deleteById, etc.
}
```

From api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java (Phase 2):
```java
@Entity @Table(schema = "whatsapp", name = "media_cache")
public class MediaCache {
    @Id @Column(name="arquivo_hash", length=64, columnDefinition="CHAR(64)") private String arquivoHash;
    @Column(name="media_id", nullable=false, length=255) private String mediaId;
    @Column(name="criado_em", nullable=false, insertable=false, updatable=false) private Instant criadoEm;
    @Column(name="expira_em", nullable=false) private Instant expiraEm;
    public MediaCache(String arquivoHash, String mediaId, Instant expiraEm); // ctor publico Phase 2
    // getters + setters
}
```

From api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/IdempotencyService.java (Phase 2 — pattern save+catch):
```java
public boolean tentarPersistir(...) {
    try {
        repository.save(new MensagemLog(...));
        return true;
    } catch (DataIntegrityViolationException e) {
        log.debug("Idempotencia: wamid={} ja existe", wamid);
        return false;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-03-1: MediaCacheService.java + MediaCacheServiceTest.java (4 cenarios)</name>
  <files>
    api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java,
    api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MediaCacheServiceTest.java
  </files>
  <read_first>
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/IdempotencyService.java (Phase 2 — pattern save+catch DataIntegrityViolationException, copiar tom + Logger declaration)
    - api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/IdempotencyServiceTest.java (Phase 2 — pattern @SpringBootTest+H2 + race scenario via ExecutorService+CountDownLatch)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/repository/MediaCacheRepository.java (Phase 2 — sem mudanca, apenas consumir findByArquivoHashAndExpiraEmAfter)
    - api-whatsapp/src/main/java/br/com/erpkit/whatsapp/model/MediaCache.java (Phase 2 — sem mudanca, ctor (arquivoHash, mediaId, expiraEm) ja existe)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-PATTERNS.md (secao "service/MediaCacheService.java" — pattern direto + scenarios obrigatorios)
    - .planning/phases/04-outbound-trava-24h-whatsappcontroller/04-CONTEXT.md (D-04 — TTL estrito 30d sem sliding)
    - .planning/research/PITFALLS.md (C-07 TTL 30d off-by-one)
  </read_first>
  <behavior>
    - MediaCacheServiceTest scenarios:
      - hit_dentro_do_ttl_retorna_media_id: registrarUpload(bytes, "id-123"); buscarMediaId(bytes) retorna Optional containing "id-123"
      - miss_quando_hash_nao_existe_retorna_empty: buscarMediaId(otherBytes) com nada gravado retorna Optional.empty()
      - miss_quando_expirado_retorna_empty: insert direto via JdbcTemplate com expira_em=now()-1s; buscarMediaId(bytes) retorna Optional.empty() (TTL estrito — entrada nao retornada apesar de existir no DB)
      - race_em_registrar_silencia_data_integrity_violation: ExecutorService(2 threads) + CountDownLatch start gate; ambos chamam registrarUpload(bytes, "id-A"); apos completion, COUNT=1 row em media_cache (UNIQUE PK gate); nenhuma excecao propagada
  </behavior>
  <action>
1. Criar `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java`:
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.model.MediaCache;
import br.com.erpkit.whatsapp.repository.MediaCacheRepository;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.dao.DataIntegrityViolationException;
import org.springframework.stereotype.Service;

import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;
import java.time.Duration;
import java.time.Instant;
import java.util.HexFormat;
import java.util.Optional;

/**
 * Cache de {@code media_id} do Meta por sha256 do conteudo binario (OUT-08 + D-04
 * CONTEXT.md + PITFALLS C-07).
 *
 * <p><b>TTL ESTRITO 30 dias (D-04):</b> hit NAO estende {@code expira_em} (sem
 * sliding). Reupload natural quando entrada expira renova TTL para
 * {@code now + 30d}. Tabela {@code media_cache} bounded — turnover de 30 em 30
 * dias garantido. Meta documenta {@code media_id} valido por ate 30 dias —
 * sliding mascara expiracao real, levando a 4xx Meta surpresa.
 *
 * <p><b>Race protection:</b> {@code registrarUpload} envolve {@code repository.save}
 * em try/catch {@link DataIntegrityViolationException} — UNIQUE PK
 * {@code arquivo_hash} e o gate atomico portavel H2/PostgreSQL. Pattern Phase 2
 * (IdempotencyService) — concurrent upload do mesmo arquivo de 2 threads:
 * uma vence (INSERT), outra catch + log.debug (silenciada). Proxima leitura ve
 * registro existente.
 *
 * <p><b>sha256 hex:</b> {@code MessageDigest} + {@code HexFormat.of().formatHex}
 * (Java 17+ standard). PK {@code arquivo_hash CHAR(64)} aceita exatamente 64
 * chars hex.
 */
@Service
public class MediaCacheService {

    private static final Logger log = LoggerFactory.getLogger(MediaCacheService.class);
    private static final Duration TTL = Duration.ofDays(30);

    private final MediaCacheRepository repository;

    public MediaCacheService(MediaCacheRepository repository) {
        this.repository = repository;
    }

    /**
     * Busca {@code media_id} cacheado por sha256(bytes). Hit somente se
     * {@code expira_em > now()} — entrada expirada retorna empty (TTL estrito).
     *
     * @param bytes conteudo binario do arquivo (PDF/imagem/etc.)
     * @return media_id valido se hit dentro do TTL; empty se miss ou expirado
     */
    public Optional<String> buscarMediaId(byte[] bytes) {
        String hash = sha256Hex(bytes);
        return repository.findByArquivoHashAndExpiraEmAfter(hash, Instant.now())
                .map(MediaCache::getMediaId);
    }

    /**
     * Registra novo upload no cache com {@code expira_em = now + 30d}. Race em
     * concurrent reupload do mesmo arquivo silenciado — UNIQUE PK
     * {@code arquivo_hash} e o gate (Phase 2 pattern).
     *
     * <p>Se ja existe entrada (mesmo expirada) com o mesmo hash, faz UPSERT:
     * delete + save. Garante que a entrada apos chamada esta com
     * {@code expira_em} renovado.
     *
     * @param bytes   conteudo binario (mesmo passado a {@code uploadMedia} no Cloud API)
     * @param mediaId media_id retornado pelo Meta
     */
    public void registrarUpload(byte[] bytes, String mediaId) {
        String hash = sha256Hex(bytes);
        Instant expira = Instant.now().plus(TTL);
        try {
            // Upsert simples: deleta antigo (se houver) + save novo. Atomico do ponto
            // de vista do thread; race com outro thread silenciada via catch abaixo.
            repository.findById(hash).ifPresent(repository::delete);
            repository.save(new MediaCache(hash, mediaId, expira));
            log.debug("MediaCache registrado: hash={} mediaId={} expira={}", hash, mediaId, expira);
        } catch (DataIntegrityViolationException e) {
            // Race: outro thread fez upload do mesmo arquivo concorrentemente. PK
            // arquivo_hash disparou. Silenciar — proxima leitura ve registro existente.
            // Pattern Phase 2 (IdempotencyService.tentarPersistir).
            log.debug("MediaCache race em registrarUpload: hash={} — outro thread ja registrou", hash);
        }
    }

    private static String sha256Hex(byte[] bytes) {
        try {
            byte[] digest = MessageDigest.getInstance("SHA-256").digest(bytes);
            return HexFormat.of().formatHex(digest);
        } catch (NoSuchAlgorithmException e) {
            // SHA-256 e built-in da JDK desde Java 1.4 — nunca acontece em prod
            throw new IllegalStateException("SHA-256 indisponivel na JVM", e);
        }
    }
}
```

2. Criar `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MediaCacheServiceTest.java`:
```java
package br.com.erpkit.whatsapp.service;

import br.com.erpkit.whatsapp.WhatsAppApplication;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.test.context.ActiveProfiles;

import java.security.MessageDigest;
import java.sql.Timestamp;
import java.time.Instant;
import java.util.HexFormat;
import java.util.Optional;
import java.util.concurrent.CountDownLatch;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;
import java.util.concurrent.atomic.AtomicInteger;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(classes = WhatsAppApplication.class)
@ActiveProfiles("test")
class MediaCacheServiceTest {

    @Autowired MediaCacheService service;
    @Autowired JdbcTemplate jdbc;

    @AfterEach
    void cleanup() {
        jdbc.update("DELETE FROM whatsapp.media_cache");
    }

    private static String sha256Hex(byte[] bytes) throws Exception {
        return HexFormat.of().formatHex(MessageDigest.getInstance("SHA-256").digest(bytes));
    }

    @Test
    @DisplayName("buscarMediaId apos registrarUpload retorna media_id (hit dentro do TTL)")
    void hit_dentro_do_ttl_retorna_media_id() {
        byte[] bytes = "PDF orcamento 1234".getBytes();
        service.registrarUpload(bytes, "meta-id-abc-123");

        Optional<String> result = service.buscarMediaId(bytes);

        assertThat(result).contains("meta-id-abc-123");
    }

    @Test
    @DisplayName("buscarMediaId com hash inexistente retorna empty (miss)")
    void miss_quando_hash_nao_existe_retorna_empty() {
        Optional<String> result = service.buscarMediaId("sem-cache".getBytes());
        assertThat(result).isEmpty();
    }

    @Test
    @DisplayName("buscarMediaId com entry expirada (expira_em < now) retorna empty (TTL estrito)")
    void miss_quando_expirado_retorna_empty() throws Exception {
        byte[] bytes = "PDF expirado".getBytes();
        String hash = sha256Hex(bytes);
        Instant haUmSegundo = Instant.now().minusSeconds(1);

        // Insert direto via JdbcTemplate forcando expira_em no passado
        jdbc.update("INSERT INTO whatsapp.media_cache (arquivo_hash, media_id, expira_em) VALUES (?, ?, ?)",
                hash, "meta-id-old", Timestamp.from(haUmSegundo));

        Optional<String> result = service.buscarMediaId(bytes);

        // TTL estrito (D-04) — entry existe no DB mas expira_em < now → empty
        assertThat(result).isEmpty();
    }

    @Test
    @DisplayName("registrarUpload concurrent silencia DataIntegrityViolationException (race PK)")
    void race_em_registrar_silencia_data_integrity_violation() throws Exception {
        byte[] bytes = "PDF concorrente".getBytes();
        String hash = sha256Hex(bytes);

        ExecutorService pool = Executors.newFixedThreadPool(2);
        CountDownLatch start = new CountDownLatch(1);
        CountDownLatch done = new CountDownLatch(2);
        AtomicInteger erros = new AtomicInteger();

        Runnable upload = () -> {
            try {
                start.await();
                service.registrarUpload(bytes, "meta-id-race-" + Thread.currentThread().getId());
            } catch (Exception e) {
                erros.incrementAndGet();
            } finally {
                done.countDown();
            }
        };

        pool.submit(upload);
        pool.submit(upload);
        start.countDown();
        done.await();
        pool.shutdown();

        assertThat(erros.get())
                .as("Nenhum thread deve propagar excecao — DataIntegrityViolationException silenciado")
                .isZero();

        Integer count = jdbc.queryForObject(
                "SELECT COUNT(*) FROM whatsapp.media_cache WHERE arquivo_hash = ?",
                Integer.class, hash);
        assertThat(count)
                .as("UNIQUE PK garante exatamente 1 row apos race")
                .isEqualTo(1);
    }
}
```
  </action>
  <verify>
    <automated>./mvnw -pl api-whatsapp test -Dtest='MediaCacheServiceTest'</automated>
  </verify>
  <acceptance_criteria>
    - File `api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java` existe
    - File `api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MediaCacheServiceTest.java` existe
    - `grep -c 'Duration TTL = Duration.ofDays(30)' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java` retorna 1
    - `grep -c 'HexFormat.of().formatHex' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java` retorna 1
    - `grep -c 'DataIntegrityViolationException' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java` retorna 1
    - `grep -c 'findByArquivoHashAndExpiraEmAfter' api-whatsapp/src/main/java/br/com/erpkit/whatsapp/service/MediaCacheService.java` retorna 1
    - `grep -c '@DisplayName' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MediaCacheServiceTest.java` retorna 4 (4 tests)
    - `grep -c 'CountDownLatch' api-whatsapp/src/test/java/br/com/erpkit/whatsapp/service/MediaCacheServiceTest.java` >= 1 (race scenario presente)
    - `./mvnw -pl api-whatsapp test -Dtest='MediaCacheServiceTest'` exits 0 — Tests run: 4, Failures: 0
  </acceptance_criteria>
  <done>
    Service implementa hit/miss/expirado/race per OUT-08 + D-04 + Pattern Phase 2; 4 tests verdes em <5s; Phase 1-3 tests inalterados; 04-04 pode importar `MediaCacheService` no `WhatsAppCloudClient.enviarDocumento`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaCacheService -> repository | sha256 hash como PK; UNIQUE constraint do DB e gate atomico portavel H2/PostgreSQL |
| MediaCacheService -> bytes | bytes nao logados; apenas hash + size em log debug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | I (Information disclosure — bytes em log) | log.debug em registrarUpload/buscarMediaId | mitigate | Log apenas `hash={}` + `mediaId={}` + `expira={}` — NUNCA `bytes={}` (seria binario inteiro ou [B@hash sem valor diagnostico). Pattern alinhado RESEARCH §Anti-Patterns "Bytes serializados em log" |
| T-04-03-02 | T (Tampering — race em concurrent reupload) | registrarUpload | mitigate | UNIQUE PK arquivo_hash + try/catch DataIntegrityViolationException — PK e gate atomico portavel H2/PostgreSQL (validado empiricamente Phase 2 IdempotencyService) |
| T-04-03-03 | I (sha256 collision — diferentes bytes mesmo hash) | sha256Hex | accept | sha256 colisao computacionalmente impraticavel (2^128 birthday bound). Trade-off aceito vs MD5/SHA-1. Pattern alinhado com WhatsApp registered "common files" via SHA-256 |
| T-04-03-04 | A (Availability — banco enchendo) | media_cache table growth | mitigate | TTL estrito 30d sem sliding (D-04) — turnover natural; tabela bounded |
| T-04-03-05 | I (PII — conteudo de PDF/imagem em RAM longa) | byte[] parametros | accept | byte[] vive no escopo do metodo (stack/heap garbage collected); MediaCacheService NAO armazena bytes — apenas hash + media_id |
| T-04-03-06 | C-07 (TTL boundary off-by-one) | buscarMediaId | mitigate | findByArquivoHashAndExpiraEmAfter usa `expira_em > now()` (exclusive boundary; entrada com expira_em == now() retorna empty); test `miss_quando_expirado_retorna_empty` valida com `now() - 1s` |

## V5 Input Validation
- byte[] nao validado (controller 04-05 ja aplicou @Size(max=18MB) no mediaBase64)
- mediaId String nao validado (vem do Meta — confianca por design)

## V7 Error Handling
- NoSuchAlgorithmException: convertido para IllegalStateException (impossivel em prod — SHA-256 built-in JDK)
- DataIntegrityViolationException: silenciado em registrarUpload (race esperado), log.debug

## V12 File size limit
- byte[] passa pelo controller (04-05) que valida @Size(max=18_000_000) na mediaBase64; service NAO valida tamanho

## ASVS L1 mappings
- V8.1.1 (Verify cached information is encrypted/integrity-checked): media_id e public reference, sha256 garante integridade do conteudo cacheado
</threat_model>

<verification>
- MediaCacheService cumpre OUT-08 + D-04 (TTL estrito 30d sem sliding)
- Race protection via save+catch alinha com Phase 2 pattern
- 4 tests cobrem hit, miss, expirado, race — todos verdes em <5s
- Phase 1-3 tests inalterados (152 tests verdes mantidos)
- Reator inteiro `./mvnw verify` passa
</verification>

<success_criteria>
- [ ] `MediaCacheService.buscarMediaId(byte[]) → Optional<String>` retorna media_id se expira_em > now(), empty caso contrario
- [ ] `MediaCacheService.registrarUpload(byte[], String) → void` upsert com expira_em = now + 30d, race silenciado
- [ ] `Duration TTL = Duration.ofDays(30)` constante (D-04 sem sliding)
- [ ] sha256 calculado via `MessageDigest.getInstance("SHA-256").digest(bytes)` + `HexFormat.of().formatHex` (Java 17+ standard)
- [ ] try/catch `DataIntegrityViolationException` silencia race em concurrent registrarUpload (Phase 2 pattern)
- [ ] `MediaCacheServiceTest` 4 cenarios verdes (hit, miss inexistente, miss expirado, race com COUNT==1)
- [ ] `./mvnw -pl api-whatsapp test -Dtest='MediaCacheServiceTest'` exits 0
- [ ] Phase 1-3 tests existentes verdes (152 tests inalterados)
- [ ] Reator inteiro `./mvnw verify` BUILD SUCCESS
</success_criteria>

<output>
After completion, create `.planning/phases/04-outbound-trava-24h-whatsappcontroller/04-03-SUMMARY.md`:
- File created (1 service + 1 test)
- 4 tests added cobrindo OUT-08 completamente
- D-04 TTL estrito implementado
- Phase 2 pattern (save+catch) reaproveitado pela 3a vez consecutiva (IdempotencyService → ClienteZapService.identificar → MediaCacheService.registrarUpload)
- Pronto para 04-04 consumir
</output>
